LC76 OVERLAND REFERENCE LIBRARY — PROJECT INSTRUCTIONS
═════════════════════════════════════════════════════════════════

Last updated: 24 September 2026, sixty-second session (PI RESTRUCTURE SESSION A — MECHANICAL MOVE, ZERO INFORMATION LOSS. Executed per LC76_PI_Restructure_Spec.txt [approved 24 Sep 2026; the sixty-first session wrote and approved the spec only and has no header block of its own] against HEAD 15584033. PRE-FLIGHT: a first run at 31b62aea STOPPED at P-2 — the committed spec was the earlier copy [41,466 bytes, O1 still 'awaiting owner']; the owner committed the delivered spec and all five files then matched on bytes and sha256. PROMOTIONS FIRST [spec §6], each after a body-presence test found it absent: P1 cost is not a materiality test [Owner values]; P2 install facts confirmed against the vehicle [numeric traceability (e)]; P3 index-card house rule, no live figures; P4 R8 hybrid regeneration; P5 ledger is VAT-inclusive; P6 build vs service by primary purpose; P7 presence is not visibility [validation gates]. Also added: two do-not-re-flag items [the user guide's deliberate Last-updated divergence; mary-ann-70/], MODEL ROUTING restated by class [D5], and the HEADER-HOUSEKEEPING standing rule [current + two priors]. OBLIGATIONS CAPTURED BEFORE ANYTHING MOVED [spec §7]: OI-110 opened [owner-initiated post-Karoo snag session — OneX mirror + Manjaro worklist]; OI-111 opened [EX_Register RA-1/TA-002 flip; the declined R43 red-header card noted on it]; OI-112 opened and closed [IERCC contradiction reconciled in Facts §11C and the VEHICLE satcomms entry to the 8 Aug owner decision on OI-3; fact block byte-identical, validator PASS vs baseline]. MOVED VERBATIM, every block sha256-verified at its destination: 33 header blocks [fifty-eighth back to twenty-sixth] and the closed-workstreams span to LC76_Session_History.txt, whose intro now carries the archive index extended by 33 mechanically built lines; the five programme charters [EX, Audit of Truth, RA-1..10, route map M1–M4, profile §06 retrofit] to the NEW LC76_Programmes_Reference.txt, where C1 then corrected the RA-1 weigh date [weighed 10 Aug 2026, recorded 15 Sep]. DELETED: the Library Register mirror, after a presence test that caught three genuine gaps in the MASTER and fixed them in the Register first — profiles 009 Zimbabwe and 010 Kenya had NO Register entry, the off-repo experimental-route artifacts were unrecorded, and the R35 rename history lived only in the mirror. C2: the drivetrain-oil interval query is RESOLVED — the owner confirmed 24 Sep 2026 gearbox, transfer case, front and rear diff 40,000 km and engine oil 5,000 km, exactly as already recorded; no figure changed. PI 249,895 -> 89,122 bytes; byte-accounting residual 0. NO RULE WAS REWORDED OR CONDENSED — that is Session B [owner-reviewed diff]. Open 70 -> 72; next free OI-113; next free R-number R44; CACHE STAYS v28. FILES THIS ROUND: LC76_Project_Instructions.txt [also paste into the project field], LC76_Session_History.txt, LC76_Programmes_Reference.txt [NEW], LC76_Open_Items_Register.txt, LC76_Open_Items_Dashboard.html, LC76_Facts.txt, LC76_Library_Register.txt.)

PRIOR (24 September 2026, sixtieth session (OIL-LOGGING RULING + LIBRARY REGISTER PASS. [1] OWNER RULING: the overdue drivetrain oils are logged as SEPARATE Services rows, one per oil, never a bundled line — recorded as OI-108's closure condition and generalised into the service-ledger conventions as the SEPARATE-FLUID RULE, because each oil runs its own EVENT clock in the fact store and a bundled row would stop the gate resetting them independently. INTERVAL QUERY RAISED, NOT ACTED ON: the owner states the oils have DIFFERENT intervals, but the library records 40,000 km for all four (Facts §2 interval= fields AND the Service Checklist, the schedule master); what differs is the last-change point (gearbox/transfer Feb 2025, rear diff Apr 2025, front diff Jan 2026). No interval changed without a source (numeric traceability) — awaiting the owner's figures and where they come from. [2] LIBRARY REGISTER PASS (flagged in the fifty-ninth session) executed grep-first against the xlsx at HEAD fcc45745: Services 310 rows / R816,890.60; Ledger_Current 231 / R684,039.29; Build_Accessories 97 items + base = R960,007.01 TCV, build & kit R566,007.01, fitted R563,586.01; R8 TCO R1,730,187.41 -> R1,776,897.61; the workbook entry now records the line-level Manjaro itemisation and the OI-8 closure; the Mozambique profile description's 'tyres in 265/70R16' corrected to the fitted 265/75R16 [the 22 Sep profile sweep had changed the files but not their Register descriptions]; the currency-gate description reworded so it no longer embeds a retired brand as its example. Register now PASSES the currency gate [0 violations, 0 review] with zero stale figures. MIRRORED HERE: the same figures in this file's LIBRARY REGISTER block; the retired pre-split workbook total and its LiFePO4-TBC caveat replaced; R31's '265/75 PLANNED row' annotated FITTED; and the do-not-re-flag note on the Tyre Pressure Calculator corrected [265/75 is now marked Current, not 265/70 — a stale exemption note misleads future sweeps]. Historical narrative [EX-12 build record, archived session blocks] left intact. Dashboard OI-108 card rebuilt from the register [carries the closure rule]; depth 0/0, node clean, 70 cards reconcile 1:1. OIR validator PASS, next free OI-110, warns unchanged [11]. No OI opened or closed; open 70. CACHE STAYS v28. Session History NOT touched. FILES THIS ROUND: LC76_Library_Register.txt, LC76_Open_Items_Register.txt, LC76_Open_Items_Dashboard.html, plus this file.))

PRIOR (24 September 2026, fifty-ninth session (OPEN ITEMS DASHBOARD VISIBILITY FIX + OI-8 CLOSED. Owner reported two dashboard faults after the fifty-eighth session's regeneration. [1] OVERDUE OILS 'NOT SHOWN': OI-108 [gearbox / transfer / rear-diff oils over their 40,000 km intervals] WAS on the dashboard but effectively invisible — card 37 of 71, filed in the generic Live band among home-admin items, titled 'gearbox-area oils' [the diff never named], owner recorded as a non-standard 'OWNER' so the parser never tagged it into the 'Next workshop visit' filter [13 -> the one view taken to the workshop omitted it], and gated 'pre-departure' although already overdue on a daily-driven vehicle. FIXED AT SOURCE in the register [the dashboard is a derived view]: title/WHAT name all three oils; owner NEIL (workshop); banded SAFETY-adjacent mobility on the OI-71 precedent [a failed gearbox or diff strands the vehicle]; RE-GATED to 'next workshop visit — already overdue' [owner may revert]. Re-confirmed still open: no gearbox/transfer/rear-diff oil change on the Services sheet since Apr 2025. LESSON: a card that exists is not a card that is seen — the 1:1 id reconciliation passed while the item was mis-banded, mis-tagged and mis-titled; reconciliation proves PRESENCE, not VISIBILITY. [2] OI-8 CLOSED on verified evidence, not on report: invoice LC76SW004 is in LC_Service_history_FINAL.xlsx at HEAD 8170dbb4 — 61 itemised Build_Accessories rows [incl. the Enertec 210 Ah LiFePO4 house battery R16,577.25] plus the crank battery in Services; the install is capital build so it lands in Build_Accessories, the item's 'service ledger' wording predating the 6 Aug split. E1 + E2, strict-tier clean. DASHBOARD UPDATED from the register: 70 cards [4 safety / 58 live / 8 waiting; workshop 14], reconciled 1:1 to the register open set; OI-8 removed; OI-108 fourth in Safety First; KPI tiles, chips and masthead counts corrected; the 'About This View' provenance block, still reading 'Generated 30 July 2026 / commit 12ca17fc' after the 22 Sep regeneration, corrected to 24 Sep / built on 8170dbb4. VALIDATION: dashboard depth div/span/article/button/header/nav 0/0, node --check clean, ids unique, TOC 5/5 resolving, no browser storage; OIR validator SCHEMA PASS on 70 entries, next-free reconciles OI-110, warn count unchanged [11]; paren balance preserved. FLAGGED-NOT-FIXED [scope]: this file's LIBRARY REGISTER description of the service workbook still carries 'Recorded total R810,838.32 excl. the LiFePO4 install entry (date/cost TBC)' — superseded by the 6 Aug split and the 22 Sep itemisation; correct in a Library Register pass. Open count 71 -> 70; next free stays OI-110. CACHE STAYS v28 — the dashboard is a pre-existing precached file, edited not added. Session History NOT touched. FILES THIS ROUND: LC76_Open_Items_Register.txt, LC76_Open_Items_Dashboard.html, plus this file.))

SESSION HISTORY — every session block older than the two priors above, and
the one-line archive index, live in LC76_Session_History.txt.

Maintained by: Neil / Mary-Ann
═══════════════════════════════════════════════════════════════════════


▼▼▼ START — PROJECT INSTRUCTIONS ▼▼▼

This project builds and maintains the LC76 Overland Reference Library for Neil
& Mary-Ann's 2009 Toyota Land Cruiser 76 Series SW (HZJ76R-RKMRS), based in
South Africa. The vehicle is used for serious overlanding across Southern and
East Africa, with a planned two-year, 60,000+ km expedition in 2028–2030 (distance
figure confirmed authoritative 9 Jul 2026 — supersedes the ~40,000 km working
figure). En-route service policy (owner, 9 Jul 2026): follow the service
schedule at Toyota dealers where the route allows; oil changes are never
deferred — every en-route oil change is also an RA-2 oil-analysis sampling point.

EXPEDITION ROUTE (working position, 13 Jul 2026): OPTION A adopted PENDING
EX-9 ratification — a two-year continuous arc, Namibia-first opening leg,
eastern corridor (Mozambique coast last) as the southbound return; no SA
return. Ride the rising dry season south-to-north, coastal return ahead of
the November rains. Seasonal anchors: Zambezi/Zambia Aug–Oct; N. Tanzania/
Kenya Jun–Oct or Ndutu calving Jan–Feb; Moz coast May–Oct (cyclone-safe).
The route is developed as an EXPERIMENTAL DRAFT outside the library until
EX-9 ratifies it (see WORKING CONVENTIONS + OI-26). Top open constraint:
the Carnet 12-month interlock on a no-return two-year trip (M4, EX-9).
NEW EX-9 INPUT (17 Jul 2026): licence-card dates pull on departure timing
in opposite directions — earlier departure worsens Mary-Ann's mid-trip
card expiry (mid-2029), later departure tightens Neil's post-return
margin (card expires early 2031).

Library hosted at: overlandslabs.github.io/Consumption
Repo: github.com/Overlandslabs/Consumption

INDEX LAYOUT (restructured 21 Jul 2026 — situation-based): index.html is
organised by reader situation, urgency-descending, EIGHT sections:
1 Emergency & Safety · 2 Find Anything (R30) · 3 Field Tools (R1 +
calculators) · 4 Vehicle (four h3 sub-groups: Engine & Mechanical /
Drivetrain & Tyres / Battery & Electrical / Spares & Service Record) ·
5 The Expedition · 6 Country Profiles & Trips · 7 Philosophy & Ethos ·
8 Governance. Principles: crisis content first (stress-state design);
R30 owns find-by-topic, the index owns browse-by-situation; governance
de-emphasised last. New cards are placed by SITUATION, not by build
order — Emergency section order is by worst-day priority (medical →
vehicle-dead → stuck → fire → security → wildlife → health). SECTION
COLOUR SYSTEM (21 Jul 2026): colour follows section — red full card
headers RESERVED for Emergency & Safety (all 7 cards); every other
section carries its --sec-* token as a 4px card top-border + card-code
eyebrow + h2 underline only (tokens in the DESIGN SYSTEM section). New
cards inherit colour from their section grid automatically. The 13 Jul
"R30 immediately after Philosophy" placement is superseded: R30 stays
SECOND on the page and in the TOC, now after Emergency & Safety.

VEHICLE
- 1HZ 4.2L inline-6 diesel, SAC low-boost turbo (reconned Aug 2023 @ 224,406
  km; boost confirmed 0.6 bar Jun 2026), intercooler (verified good, Udel
  Jun 2026). 314,546 km current odometer (read 15 Sep 2026).
- EGT (pyrometer) and boost instrumentation FITTED (confirmed by owner,
  8 Jul 2026).
- Timing belt last replaced Apr 2025 @ 273,023 km — full kit: belt, tensioner,
  idler, cover (per R23/R8). 1HZ is an INTERFERENCE ENGINE — belt failure bends
  valves. Any document stating otherwise is in error. 100,000 km interval falls
  due ~373,000 km — inside the expedition window. RA-3 pre-departure replacement
  (belt + tensioner, ~T-3 months) CONFIRMED 9 Jul 2026 (Gate 1 = A); removed
  belt becomes the carried emergency spare; valve-clearance re-check combined
  at the same rocker-cover-off event.
- Fuel-injection overhaul Jun 2026 @ 310,412 km (Udel): Bosch VE pump rebuilt
  & bench-calibrated, 6 injectors reconditioned (new nozzles), new filter
  housing, all 12 valve clearances set (first recorded adjustment).
- R151F 5-speed manual, part-time 4WD, F&R diff locks
- Terrain Tamer heavy-duty clutch (Feb 2026)
- Free-flow exhaust: 76 mm bore, 409-grade stainless (Powerflow, Bellville,
  May 2026 @ 309,968 km)
- OME medium suspension, 2" lift — OME 859 front coils, N181 NitroCharger+
  front shocks, CS058R multi-leaf rear springs, 63111 NitroCharger+ rear shocks
- GVM 3,000 kg (Toyota-confirmed Jul 2026, authoritative — supersedes the
  2,850 kg figure previously recorded here; Facts §2 is the source). Kerb
  2,190 kg factory (Toyota-confirmed); as-built kerb ~2,317 kg ESTIMATE.
  DATA-PLATE (7 Aug 2026, E1): GVM 3,000 kg now plate-verified, and the
  plate adds GCM 4,500 kg + per-axle maxima (front 1,480 / rear 2,180 kg)
  to Facts §1 — so RA-1 is a PER-AXLE weighbridge, the REAR axle the
  binding constraint (it can be exceeded while still legal on GVM);
  figures in Facts §1, doctrine on OI-1.
  WEIGHBRIDGE DONE 10 Aug 2026 (E1, loaded per-axle, full expedition trim —
  full fuel / 60 L water / all gear): front 1,160 kg · rear 2,120 kg · GROSS
  3,280 kg -> OVER the 3,000 kg GVM by 280 kg (9.3%) at full replenishment,
  though legal on each axle alone (rear 2,120/2,180 the binding axle, 60 kg
  headroom; front 1,160/1,480). RA-1/OI-1 and TA-002 CLOSED; figures in
  Facts §1. THE OVERLOAD IS A DECISION — payload reduction vs certified GVM
  re-rating (OI-103); the rear-sag / extra-leaf question is a coupled symptom,
  not the fix (a spring restores stance, not the legal ceiling). As-built kerb
  (~2,317 kg) and payload (~683 kg) remain ESTIMATES — a loaded pass does not
  isolate the unladen weight; a clean split would need a separate empty
  weighing (optional, not blocking). Measured-figure propagation to
  R11/R12/R29/R28 tracked as OI-104 (CDR sweep, owner-sequenced).
- 145 L main tank + 2 × 20 L Desert Fox collapsible auxiliary cells
- Current tyres: 265/75R16 BFG All-Terrain KO3 (load range D, 119/116 S; fitted
  17 Sep 2026 @ 314,624 km) · Spare: 265/75R16 KO3 (matching) · Rims: 16" STEEL
  · Tubeless-only doctrine — NO TUBES carried (owner decision, 10 Jul 2026;
  redundancy = carried carcass + plug kit, per R40 §5).
- Tyre policy (RA-9, reframed 8 Jul 2026; FITTED 17 Sep 2026): the expedition
  departs on a fresh, PROVEN 265/75R16 set with ≥5,000 proving km. FITTED
  17 Sep 2026 — BFG All-Terrain KO3 265/75R16 load range D (119/116 S) @
  314,624 km, invoice 20391 — RA-9 pulled forward, forced by a third Maxxis
  sidewall failure; proving now under way. The 285/75R16 option is RETIRED
  (owner, Sep 2026). LR-E 123/120S is NOT offered in 16", so LR-D is the
  correct-and-only KO3 spec (load-adequate over the measured axle loads; lighter,
  which helps the over-GVM situation, OI-103). Choice criteria: casing
  strength + regional availability. Second-spare / casing-repair strategy for
  East Africa is an open owner decision (one spare is thin north of the
  Zambezi). Field wheel/tyre procedures (wheel change, bead breaking, manual
  carcass swap, re-seat) are owned by R40. R40 kit gaps chartered to the R3
  ledger (EX-8): long tyre levers ×3, rim protectors ×3, spare valve stems ×4
  + TPMS grommet/seal kit, bead lube; OPEN owner decision: clamp-type bead
  breaker (recommended buy). Carcass carried on the EXPEDITION ONLY (owner,
  10 Jul 2026) — not on domestic trips; R40 drill is pre-departure on the
  265/75 set. Wheel-nut torque recorded as 131 Nm
  (standard 70-Series steel-rim spec) — VERIFY against handbook at next
  service and correct R40 §6 if it differs.
- Satellite comms (status per owner, 11 Jul 2026; UPDATED 7 Aug 2026):
  Garmin inReach Mini 2 OWNED (tracking, GPS coordinates, SOS).
  Satellite voice phone OWNED — Inmarsat IsatPhone 2.1 + hardcase,
  purchased 6 Aug 2026 from Global Sat4Rent (Pty) Ltd (invoice 8984,
  R21,924.25 incl. VAT). Satellite number (MSISDN): +870 777 751 134.
  Plan: IsatPhone Pro Prepay Regional 2018, 20 units, expires 7 Aug 2027.
  OI-18 (purchase) CLOSED 7 Aug 2026. IERCC registration is DROPPED for
  the satphone (owner decision 8 Aug 2026, recorded on OI-3): satphone
  emergencies are dialled to the crew's own SA provider medevac stack
  (R29 §6 master; method R43). The inReach SOS -> Garmin IERCC path is the
  inReach's own, built in and unchanged (GEOS acquired by Garmin —
  geosalliance.com is dead). Still open under OI-3: the Assistance-Button
  target decision, a documented live SOS-provider test-dial from the
  handset, and the expedition airtime/coverage plan. R29 §7/§11 is
  authoritative for the activation doctrine. (Reconciled 24 Sep 2026,
  OI-112.)
- Licences & documentation (CORRECTED 17 Jul 2026 — exact dates off the
  physical cards/disc; supersedes the 13 Jul resolution): Driving-licence
  CARDS — Mary-Ann's expires mid-2029, INSIDE the expedition window
  (~month 9–11 of a mid-2028 departure) → MANDATORY pre-departure early
  renewal (a SA card cannot be renewed abroad or by proxy — in-person
  biometrics; expiry kills her IDPs, is an insurance repudiation ground,
  and deletes RA-8 solo-driver capability); fallback = fly-home renewal
  ~Apr/May 2029 clustered with the OI-25 disc waypoint. Neil's expires
  early 2031 — ~5 months AFTER a mid-2030 return → renewal margin-driven,
  NOT mandatory; renew in the same sweep if the DLTC accepts. Doctrine
  basis: WC government — renewal allowed AT ANY TIME before expiry, new
  card valid 5 years from renewal date; CONFIRMED IN WRITING by the WCG
  Contact Centre 17 Jul 2026, ref 260717-000921 (OI-27 closed same day;
  carry the reference to the DLTC when booking — OI-24 un-gated). Start
  the card sweep ~T-6 months (production backlogs routine).
  An IDP never outlives the card it is issued against — issue both IDP
  types against the fresh card(s). VEHICLE LICENCE DISC: current disc
  expires mid-2027 — one ordinary renewal Jul 2027, then early renewal
  in the FINAL WEEK before the 2028 departure (SA early renewal resets
  the cycle pro-rata; full 12 months on the windscreen); one
  mid-expedition renewal at ~month 12 (~2029) handled by RSA PROXY (not
  either crew) under Mary-Ann's letter of authority + certified copies,
  couriered to a route address (Windhoek / Arusha / Nairobi). Actions:
  OI-24 (pre-departure sweep, both cards + disc) / OI-25 (mid-trip disc
  waypoint); OI-27 (DLTC window verification) CLOSED 17 Jul. Doctrine:
  R29 pre-departure checklist + R39 §4; dates: Facts §11.
  OWNER'S-AUTHORITY AFFIDAVIT (R39 §3): print-ready SAPS template built
  20 Jul 2026 as an R39 supplement — complete in ink before commissioning;
  affidavits are commonly treated as current ~3 months only, and
  re-certification is SAPS-only (impossible abroad) → OI-30, EX-9 input,
  decide alongside OI-24/OI-25 (commission in the final pre-departure
  week with the OI-24 sweep).
- Recovery: electric winch fitted + full kit (bow/soft shackles, tree
  protector, kinetic rope, snatch strap, damper, 2× MaxTrax, hydraulic +
  bottle jack, shovel, machete, compressor, deflators, gauge, tyre repair
  kit, ground sheet, PPE). No hi-lift (deliberate — winch + MaxTrax).
  Inventory & crew skills: Facts §11B, R9, R37.
- Electrical (AS FITTED — all-Victron LiFePO4; installed by Manjaro Industries,
  invoice LC76SW004). Dual-chemistry: LiFePO4 house + lead-acid crank.
  · House battery: Enertec Power+ 12.8V 210Ah LiFePO4 (15BLI-12V210-BLP) —
    internal BMS + Bluetooth, IP65, 20 kg, 105A max continuous discharge.
  · Crank battery: Enertec 650B-BLS Premium Plus Silver (SMF sealed calcium,
    92 Ah, 750 CCA SAE / 680 EN) — replaced the Willard 650 on 4 Aug 2026 @
    311,263 km (R32 / Facts §8).
  · DC-DC: Victron Orion XS 12/12-50A Non-isolated — cap output to ~30A
    in VictronConnect to protect the 1HZ alternator.
  · Inverter/charger: Victron MultiPlus 12/500/20-16 230V (500VA / ~430W
    continuous, 20A charger, 16A transfer) — replaces Sinetech 1200W.
  · Solar: Victron SmartSolar MPPT 100/30; Hardkorr 200W portable panel
    carried over (confirmed Jul 2026; was not itemised in the quote).
  · Monitor: Victron BMV-712 Smart (round LCD display head + 500A shunt +
    Bluetooth; second voltage input wired to the crank) — replaces BM2000.
  · Load protection: Victron Smart BatteryProtect 65A.
  · Shore power: 16A EZ-Lock SS inlet + 15 m 16A lead — mains charge via
    the MultiPlus.
  · Distribution: Fuse Block Safety Hub 150 + 8-circuit blade + M-series
    battery switch — replaces Lynx Distributor.
  Charge profile: per Enertec Power+ manual; Victron LiFePO4 preset as
  fallback (absorption 14.2–14.4V, float 13.5V, temp-comp OFF, equalise OFF;
  low-temp charge cutoff handled by the battery BMS). The 105A discharge
  ceiling is the total continuous 12V load budget.
- No dedicated crank-battery mains maintainer is fitted (DEFA removed at
  install; MultiPlus and solar serve the house bank only) — crank charging
  is alternator-while-driving. See R32 §6–§7 for the standing routine.
  Rotating-electrics redundancy (spare alternator or brush/regulator kit,
  starter kit, belts) chartered as RA-4 — feeds the R3 ledger (EX-8).
- Electrical install complete: R26 finalised (INTERIM → FINAL); R6 retired
  (folded into R7); R33 retired (superseded by R26); R1/R5/R7/R30 updated to
  the fitted spec. R32 rebuilt to dual-chemistry as-fitted (v2.0, Jul 2026).
- Vehicle registered to Mary-Ann (also title holder); SA-plated RHD.
  RC1 registration-certificate values are HELD OFFLINE — PRIVATE ANNEX
  §1 (removed from the public library 28 Jul 2026; DO NOT RESTORE).
  Tare 2,118 kg is RETAINED in Facts §1 as a technical weight figure
  and RA-1 reconciliation input. Model year 2009 per marketing
  descriptor, plate confirmation pending (OI-31). Formal documents
  (affidavit etc.) describe the vehicle EXACTLY as the RC1 does — no
  model year, no chassis code; take the values from the annex or the
  certificate, not from this library.

LIBRARY REGISTER — POINTER (the full mirror formerly carried here was
deleted 24 Sep 2026, PI restructure Session A, after a presence test against
the master)
LC76_Library_Register.txt is the MASTER OF WHAT EXISTS — every library file,
its R-number, class, precache status and description, plus the known non-
library artifacts. R-numbers are authoritative and are checked there before
any index or register work; never assume the project file set or index.html
reflects the live GitHub state. Next free R-number: R44.

PROGRAMMES — POINTER (moved 24 Sep 2026, PI restructure Session A)
The programme CHARTERS and definitions — the Expedition Readiness Programme
EX-1..EX-12, the Audit of Truth running order, the Readiness Actions
RA-1..RA-10, the route evaluation change map M1–M4, and the profile §06
retrofit + freshness-marker charter — live in LC76_Programmes_Reference.txt
(moved verbatim). The STATUS of every programme item is mastered by
LC76_Open_Items_Register.txt and the working-papers EX_Register.

CLOSED WORKSTREAMS — POINTER: the R29 evaluation map M1–M5, the mobile v1.7
propagation, the 12 Jul follow-ups, footer standardisation and the 7 Jul
defect-fix notes moved verbatim to LC76_Session_History.txt ('CLOSED
WORKSTREAMS' section), 24 Sep 2026. Their one live line — R40 wheel-nut
torque and the bead-breaker decision — is tracked as OI-7 and OI-17.

DESIGN SYSTEM (mandatory — full spec in LC76_Design_Guidelines.html, now v1.9)
- Fonts: Bebas Neue (display), Oswald / 'Oswald','Arial Narrow',sans-serif
  (body), IBM Plex Mono (labels/mono). The whole library is on this single
  on-system set — no off-system faces anywhere.
- Reference doc tokens: --ink:#0d0e12, --paper:#dad2c0, --paper2:#edeae0,
  --amber:#b86800, --amber2:#e07800, --rule:#d4cfc4, --steeldk:#2a2f3a,
  --steelmd:#6a7080, --red-warn:#B91C1C, --green-ok:#059669
- Calculator tokens (warm palette): --clay, --ochre, --sand, --cream, --ink,
  --bark, --smoke
- Dark masthead + light content area pattern
- Masthead standard (DG v1.6, section S10) — six slots, fixed order:
  back-link (⌂ Index) · eyebrow (Collection · ID) · title (Title Case) ·
  one-line subtitle (≤~12 words) · invariant identity line · Last-updated date.
  Identity line, identical on every file: "2009 LC76 SW · 1HZ 4.2L Diesel ·
  low-boost turbo (0.6 bar) · intercooler". NEVER in a masthead: odometer, GVM,
  per-document component specs, the EX-programme tag, or a vehicle code in the
  eyebrow. Closed collection vocabulary (six): Reference · Country Profile ·
  Terrain Profile · Field Tool · Philosophy · Itinerary. Profiles carry ONE
  extra element — native official name as a de-emphasised line under the title;
  their entry/visa/plating strip moves to the body. Exempt: calculators (no
  back-link/identity/date, eyebrow "Field Tool") and governance/infrastructure
  (index.html, Design Guidelines, Profile Template, audit HTML). Date form:
  "Last updated: D Month YYYY" (lowercase, full month, no zero-pad).
  R8-supplement eyebrow form: "Reference · R8 Supplement".
  PHILOSOPHY — ONE EXTRA ELEMENT (DG v1.9, adopted 29 Jul 2026). The four
  philosophy documents carry an editorial gloss as a de-emphasised italic
  .std-local line placed BELOW THE SUBTITLE and above the identity line —
  deliberately NOT the profile placement (profiles put .std-local directly
  under the title), because the philosophy .std-sub is a small mono amber
  line, typographically the same instrument as the profile .std-local, and
  the profile placement stacks two near-identical lines. Slots 5 and 6 are
  unaffected. The philosophy batch of the S10 migration completed 29 Jul
  2026; the itinerary half ran 28 Jul.
- Sticky nav: masthead and TOC MUST be wrapped together in a single sticky
  container (position:sticky; top:0; z-index:100). Never separate.
- TOC IS ONE ROW AT EVERY WIDTH (mandatory, DG v1.8, adopted 29 Jul 2026).
  The single-row horizontal scroller lives in the BASE rule, never in a
  max-width media query: flex-wrap:nowrap; overflow-x:auto;
  overscroll-behavior-x:contain; -webkit-overflow-scrolling:touch;
  scrollbar-width:none — plus .toc-bar::-webkit-scrollbar{display:none} at
  base. The child selector needs white-space:nowrap ONLY; no flex
  declarations. THE SCROLLBAR STAYS HIDDEN on desktop too (owner ruling,
  29 Jul) — do NOT add a @media (pointer:fine) scrollbar to individual
  files. REASONING: the TOC is inside the sticky unit, so a wrapped row
  costs viewport on every screen of the document, permanently. A TOC that
  wraps is LATENT until someone adds a section — the Karoo file was fine
  at 17 buttons and became a three-row header at 19. If a row gets
  tediously long, SHORTEN THE LABELS (the section h2 carries the full
  title), do not widen the bar.
- Content width: max-width:100% with percentage padding
  (padding: 28px 5% 80px). NEVER a fixed pixel max-width cap. Headers,
  footers, sticky nav always full browser width.
- TOC navigation: JS scrollIntoView / computed-offset scrollTo (not href
  anchors). Scroll offset must account for full sticky container height.
  IntersectionObserver for active-state highlighting.
- TOC INDEX CONTRACT (mandatory, adopted 22 Jul 2026 from OI-38;
  CORRECTED the same day after the first version of this rule caused three
  regressions). THE CONTRACT HAS THREE PARTS, NOT TWO: the sectionIds
  array, the TOC buttons, AND THE TOGGLE EXPRESSION that compares them.
  Read the toggle in the file before judging the pairing — do not assume it.
  Under toggle i===idx the array must mirror the TOC buttons exactly (same
  entries, same order, same length, including any alerts and changelog
  entries). Under toggle (i-1)===idx the array must be the TOC list MINUS
  its leading button, and an array that looks "short" is correct. Both
  conventions existed in this library and a rule written from one of them
  broke three files built on the other. STANDARDISED 22 Jul 2026: all ten
  profiles and the template now use i===idx with a 1:1 array, and any new
  profile inherits that from the template. If a future file is found on the
  offset convention, normalise it rather than adjusting the array. A mismatch does not throw; it silently highlights
  the wrong button, so it will never be caught by node --check or a depth
  walk. Two mirror-image failures were found in the profile family: an
  'alerts' id in the array with no button (003), and an "! Alerts" button
  absent from the array (006 and the TEMPLATE). ADD TO THE STANDARD
  VALIDATION SET: assert len(sectionIds) == len(tocLinks), assert every
  sectionIds entry resolves to a real element id, and verify the pairing in
  order. Whenever a section is added or removed, both lists change together.
  HOW TO EXTRACT THE TOC SIDE (learned the hard way, 22 Jul 2026 — two
  extractor versions gave false readings before the third was trusted):
  compare sectionIds against the getElementById TARGETS taken from every
  element carrying class="toc-link". Do NOT compare against button LABELS;
  do NOT assume the element is an <a>, because 009 Zimbabwe uses
  <span class="toc-link" onclick=...> and read as zero buttons; and do NOT
  anchor the pattern on attribute ORDER, because a class-first regex misses
  every file where class is the last attribute and will report most of the
  family as broken. The safe form is: find all tags matching
  <[a-zA-Z]+[^>]*class="toc-link"[^>]*>, then pull getElementById('X') from
  each tag. Both false readings were caught before being reported — working
  convention (d) doing exactly what it exists for.
- Footer convention (post-standardisation, 8 Jul 2026):
  · Reference docs, profiles (body), philosophy, itinerary: NO footer.
  · Profiles: single-line sources-attribution footer only.
  · Calculators: functional footer RETAINED (live JS readout values).
  · Audit reports: provenance footer RETAINED (governance).
  · index.html: minimal brand line RETAINED.
  · Do NOT add footers to new documents unless they carry genuinely unique
    functional content.
- v1.7 mobile PWA pattern (MANDATORY): html
  {overflow-x:hidden; overflow-x:clip; overscroll-behavior-x:none} AND body
  {overflow-x:clip; overscroll-behavior-x:none} — body must use CLIP, never
  HIDDEN. CONFORMS: entire library (62 in-library files, 11 Jul 2026;
  WORKSTREAM CLOSED). Also: a ::before slab on the sticky-nav unit
  (bottom:100%; height:100vh) with an opaque background; safe-area-inset-top
  padding; overscroll-behavior-x:contain on inner horizontal scrollers; a
  print guard resetting the sticky unit to position:static and suppressing the
  ::before slab.
- Print CSS: Design Guidelines Section 16 print standard — 7mm page margins,
  7.5pt body, no forced page breaks by default, break-inside:avoid on small
  elements only, break-inside:auto + thead repeat on large tables (15+ rows).
  ** THE 7.5pt BODY RULE HAS NEVER TAKEN EFFECT ANYWHERE — see OI-60. Sizes in
  this library are declared in rem, which resolves against the ROOT element;
  the print blocks set BODY. Diagnostic 29 Jul 2026: zero of 71 files set the
  root size in print, 1,547 rem declarations sit in base CSS, and printed pages
  have been coming out at screen size throughout. The remedy is a single root
  declaration inside @media print plus rem heading overrides that COMPRESS the
  hierarchy — prototyped on LC76_How_To_Use_This_Library.html (knob 11.6px =
  8pt body, owner-ruled off a real printout after 12pt was trialled and
  rejected). DG §16 must be corrected BEFORE any propagation; until it is,
  §16 specifies a form that cannot work. DO NOT record a file as §16-compliant
  on the strength of the rule being present. **
  Firefox: redefine CSS custom properties as hex with !important inside
  @media print — var(--token) does not resolve reliably in Firefox print.
- Calculator shared files (embedded in other pages) must have the index nav
  button removed so they remain standalone-shareable.
- 60-30-10 colour rule and WCAG AA compliance apply throughout.
- index.html section layout: situation-based, eight sections (see INDEX
  LAYOUT block above). h3.sec-sub sub-group headers (mono, uppercase,
  amber left rule) are index-only; they carry NO ids so the h2[id]
  IntersectionObserver contract is untouched.
- index.html section colour tokens (21 Jul 2026, index-only):
  --sec-emergency #B91C1C · --sec-find #e07800 · --sec-tools #3E6B4F ·
  --sec-vehicle #2a2f3a · --sec-expedition #6B6B3A · --sec-profiles
  #8A4B2A · --sec-philosophy #6E5B46 · --sec-governance #6a7080.
  Red = Emergency ONLY (full headers, white titles); all other sections
  border/eyebrow/underline accents only. The old per-card tool/green
  header treatment and the --green-field token are RETIRED.

OWNER VALUES
- Safety, simplicity, reliability. "Eco not ego."
- Direct, action-oriented writing — no unnecessary jargon.
- Field documents must print well and be readable in harsh conditions.
- No over-engineered solutions — good quality, cost-effective, appropriate.
- Ground tents (OzTent RV4), no sleeping in vehicle, no trailer.
- No-trace camp doctrine (standing, 20 Jul 2026): pack-out sanitation and
  raised fire pan + ground sheet on no-dig/sensitive terrain; ash packed
  out on sensitive ground. Doctrine R17 §5 / R42 §4; kit R11; consumables
  via EX-8.
- Vehicle also used as daily run-around at home.
- Cost is not a materiality test (owner ruling D1, 30 Jul 2026; promoted
  24 Sep 2026 from archived narrative, PI restructure P1). A job vital to
  safety, legality or mobility is done regardless of price and is never
  deferred or downgraded for cost; cost is an INPUT to a decision item,
  never a rating driver. (Also recorded in the Audit of Truth Charter §4.3,
  but it is owner doctrine, not audit method.)

WORKING CONVENTIONS
- Ownership-first propagation (adopted 22 Jul 2026, from the content-audit
  seam pattern — the sharpest of the three structural findings). Before
  editing any topic, look it up in R30 and identify the OWNING document.
  Edit the owner FIRST, then every document that delegates to it, in the
  SAME session — never the reverse. R42-then-R10 is the failure mode that
  produced F38 (R42 corrected while R10 §3, which R42 delegates to, still
  carried superseded fire doctrine); R10-then-R42 would not have produced
  it. SESSION-CLOSE COMPLETENESS ASSERTION: for each topic touched, list
  the documents that delegate to it and assert each was visited, or
  explicitly deferred to a numbered open item — same control class as the
  COUNTIF baseline on the working papers. An R30 OWNER-flag column, which
  would convert this discipline from an act of vigilance into a lookup, is
  approved in principle and DEFERRED until after the Karoo loop.
- Report only what was actually executed (standing, 21 Jul 2026, ninth
  session). Claude states what it has ACTUALLY executed in the current
  session, verified against tool output, and never reports a session or step
  as complete from expectation (the ninth session opened with a prior turn
  wrongly asserting completed work that had not been done).
- Status-bearing content carries provenance (standing, adopted 29 Jul 2026,
  from OI-67). Any array, table or list in this library that asserts a live
  STATUS — outstanding actions, monitors, flags, "no record" claims — must
  carry a source field naming where each entry came from (manual, service
  history, owning R-doc, workshop advisory). A list without provenance cannot
  be audited, therefore is not maintained, therefore drifts — the Service
  Checklist's OUTSTANDING array carried two items that had been settled weeks
  earlier, on a document with a print-and-take-to-the-workshop function. The
  convention already existed one array away in the same file. COROLLARY FOR
  RECONCILIATION: a completeness gate must sweep for status-bearing content
  WHEREVER IT LIVES, not only in instruments that look like registers. The
  29 Jul gate checked spreadsheets and audit reports, declared the register
  substantially complete, and missed both R8 and its supplement.
- Numeric traceability, self-audit, and verify-before-you-report (adopted
  22 Jul 2026 from the content-audit programme close). (a) Any specific
  numeric specification introduced into a library document must be
  traceable to Facts, the owning R-doc, or a manual — a plausible number
  reads as authority and is the most dangerous thing an assistant can add
  to a field reference (F65, a self-introduced defect). (b) A self-audit
  of prior-session Claude edits is a standing step in every audit session
  — it is the only control that caught F65. (c) A candidate finding is
  VERIFIED before it is written down; a false finding costs the owner's
  trust in the whole register (F74's dropped candidate). (d) Completeness
  checklists are written BEFORE the file is opened, never after. (e) Install
  facts are CONFIRMED AGAINST THE VEHICLE, never inferred from a
  manufacturer's typical layout (promoted 24 Sep 2026, PI restructure P2,
  from the fortieth/forty-first sessions — the MultiPlus switch order was
  inferred from the generic Victron layout and shipped wrong).
- Experimental / pre-EX-9 route planning (adopted 13 Jul 2026): the expedition
  route is developed as an EXPERIMENTAL DRAFT that lives OUTSIDE the library
  until EX-9 ratifies it — kept in ordinary chats, NOT the project, so a fluid
  draft is never mistaken for committed fact. Source of truth = the working
  brief (LC76_Experimental_Route_OptionA_WORKING.docx / .md); the owner edits
  in place, and a "CHANGES THIS ROUND" list at the top of the doc is the
  reconciliation signal on re-upload (read it first, reconcile the body against
  it). The route MAP (…_MAP.html or the inline map tool) is a VIEW re-rendered
  from the brief on demand, NOT a persistent document — feedback goes to the
  brief, the map is redrawn (the inline map tool's "Day 1–6" labels are its
  fixed vocabulary; the HTML file says "Phase" and is the reference version).
  Iteration loop: edit brief in chat → when a version stabilises it becomes the
  EX-9 seasonal/route input → EX-9 ratifies → ONLY THEN propagate into the
  library (R38 §4, R39 §1, profile route framing). Nothing route-related commits
  to GitHub / the library pre-EX-9. These experimental artifacts are NOT library
  files, NOT precached, NEVER trigger a CACHE bump. Tracked as OI-26. Word docs
  are heavier to round-trip than markdown/txt — for fast iteration markdown or
  plain txt is the smoother medium; use Word only when the owner prefers the
  editing feel.
- Changelog discipline (owner ruling, 22 Jul 2026). (a) EVERY masthead
  "Last updated" bump carries a matching changelog entry — the masthead is
  the claim, the changelog is the evidence, and a claim without evidence is
  the defect class the content audit spent five sessions removing. This
  applies to silent fixes too: "TOC highlighting corrected, no content
  changed" is a valid entry. EXEMPTION, ADOPTED 29 JUL 2026 (owner ruling):
  rule (a) does NOT apply to non-profile R-docs, which carry no changelog
  section. The EVIDENCE INSTRUMENT FOR THAT CLASS IS NAMED INSTEAD —
  LC76_Session_History.txt and LC76_Library_Register.txt, which record what
  changed, when and why, per session. The principle is unchanged: a masthead
  claim still requires evidence; the evidence lives centrally rather than in
  the file. THE ALTERNATIVE WAS MEASURED AND REJECTED: adding changelog
  sections to ~40 R-docs would touch the three-part TOC index contract in
  every one of them — the structure that produced three regressions on
  22 Jul — and the back-catalogue could not be populated without
  reconstructing history, which doctrine forbids; each file would have
  gained a changelog whose only entry recorded its own creation. Profiles
  keep rule (a) because they are perishable, country-specific and read
  standalone by someone deciding whether a fee is current. THIS IS AN
  EXEMPTION, NOT AN OVERSIGHT — recorded so a later session does not "fix"
  it. (b) Every profile carries a changelog.
  (c) Changelog dates are ISO (2026-07-22) — sortable and unambiguous.
  (d) The content-currency badge ("Compiled 7 Jul 2026") is NOT a changelog
  date and is never normalised or bumped with these edits — it is
  document-level provenance and has its own convention.
  STRUCTURAL DIVERGENCE, UNRESOLVED (OI-39): 001–008 carry the changelog as
  an UNNUMBERED section (id="changelog") with .changelog-entry grid rows;
  009 and 010 carry it as the FINAL NUMBERED SECTION (§19 / §20) as a
  revision table. Both are valid and both now satisfy (a)–(c). Harmonising
  would renumber two files a second time, so it is parked for EX-10.
  PROBE WARNING: testing for id="changelog" reports 009 and 010 as having
  NO changelog. They have one. Test for a changelog SECTION by title, not
  by that id.
- Masthead migration & the 80k rule: a masthead-only migration is a 3–4
  string anchored str.replace + whole-file validation walk — safe at any file
  size, EXEMPT from the "80k+ = own-session" rule (owner, 7 Jul 2026). The
  own-session rule still applies to heavy/rebuild edits of large files.
- File naming: LC76_Title_Case.html. No legacy patterns (hzj76r-, 1hz-,
  service-history-).
- Bar is the primary pressure unit on all displays; PSI is derived/secondary.
- Weights in kg, distances in km, pressures in bar (PSI shown alongside).
- R-numbering is authoritative — cross-check LC76_Library_Register.txt before
  building index updates; never assume the project file set reflects the live
  GitHub state.
- Index card placement (adopted 21 Jul 2026): new library files are placed
  on index.html by SITUATION per the INDEX LAYOUT block — not appended to
  a build-order grid. Always inspect the actual markup of the adjacent card
  before inserting.
  INDEX CARD HOUSE RULE (forty-fifth session, 7 Aug 2026; promoted 24 Sep
  2026, PI restructure P3): a card answers one question — what is in here and
  when would I reach for it — in plain field-voice, one to two sentences, no
  internal codes, and NO LIVE FIGURES: every number on a browse card is a
  second unmaintained home for a fact.
- Service ledger cost conventions (10 Jul 2026, EX-12 S1): cost 0 = child line
  item of the parent invoice row immediately above (parent carries the full
  amount) — child rows EXCLUDED from per-item averaging, never double-counted.
  No cost=1 placeholders. Classification is rule-based (see EX-12), not
  per-record tagged. Blank-cost milestone rows render as em-dash, contribute
  nothing to totals. SEPARATE-FLUID RULE (owner, 24 Sep 2026): every fluid or
  consumable with its own change clock — gearbox, transfer case, front diff,
  rear diff, coolant, brake fluid — gets its OWN Services row with its own date
  and odometer, never a bundled 'drivetrain oils' line, so each fact-store EVENT
  (and its OVERRUN warn) can be reset independently.
  The ledger is VAT-INCLUSIVE — 'the price we pay, we don't claim VAT' (owner,
  forty-third session, 6 Aug 2026; promoted 24 Sep 2026, PI restructure P5).
  BUILD vs SERVICE is classified by PRIMARY PURPOSE (same session, P6):
  restoring function is service even with an uprated part; adding capability
  is build, and lands in Build_Accessories.
- Service entry workflow (EX-12 S3): new entries made in
  LC_Service_history_FINAL.xlsx FIRST (always ≥200,000 km era), then
  LC76_Service_Ledger_Current.html refreshed by a data-only edit of its
  embedded JSON const + §3 era totals; the R8 dashboard aggregates refreshed
  the same session if numbers move materially. The Archive ledger is a closed
  era — never touched.
  R8 HYBRID REGENERATION (forty-fourth session, 6 Aug 2026; promoted 24 Sep
  2026, PI restructure P4): deterministic fields (totals, record counts,
  rand-per-km, cumulative, bands) regenerate directly from the Services sheet;
  classification-dependent fields (pareto, scheduled, driveline, yearly) are
  adjusted by the itemised deltas ONLY and never reclassified — the original
  category classifier is not reproducible from the sheet, so a fresh
  classifier would silently restate the history.
- For large files (R4, R8, and any 80k+ HTML): start a fresh chat, upload the
  live HTML from GitHub, provide a change spec, execute surgical edits. Never
  rebuild from project-knowledge fragments.
- For structural or cross-file changes: propose first, get approval, then
  build. Flag the full required change map when a literal edit would create
  internal contradictions elsewhere in the document.
- Before batch-editing a UI element across files, ask Neil to identify a
  confirmed working file as the reference template. Never conclude an element
  is absent without careful full-file inspection.
- Update index.html AND LC76_Library_Register.txt when adding new documents.
- Open Items Register (standing, adopted 12 Jul 2026): LC76_Open_Items_
  Register.txt is the SINGLE authoritative list of everything open. Every
  session close: update it (close items with date, add new findings, re-date
  the header) and present it alongside the updated Project Instructions. Closed
  items move to the CLOSED LOG (never deleted). The header narrative records
  what happened; the Open Items Register records what is still open — do not
  duplicate open-item detail here beyond a pointer. Governance file: GitHub +
  project knowledge; NOT a library file, NOT precached, NEVER a CACHE bump.
- Project Instructions upkeep (standing): whenever a session changes anything
  recorded in this file, Claude updates it to match and PRESENTS the full
  updated text as a deliverable for the owner to paste into the project field
  and commit to GitHub. Do this automatically, every time, and bump the
  "Last updated" header to the session date.
- Header housekeeping (standing, adopted 24 Sep 2026 — PI restructure spec
  §10 / ruling D4). At every session close the PI header keeps the current
  block plus the two most recent priors; older blocks move verbatim to
  LC76_Session_History.txt in the SAME session — a governance-file edit, not
  a separate pass. The one-line archive index lives in that file's intro.
- Service-worker cache versioning (Claude-owned): whenever a library file is
  ADDED, REMOVED, or RENAMED, Claude bumps the CACHE constant in sw.js. Plain
  edits self-refresh via background revalidation and do NOT require a bump.
  CLARIFIED 21 Jul 2026 (audit Gate B, evidence-based): GOVERNANCE files —
  audit HTML reports, the Open Items Register, working papers — are
  index-linked but NEVER precached in sw.js and NEVER trigger a CACHE bump,
  even when newly added. Verified against sw.js: zero references to either
  prior audit HTML or the OIR. The bump rule applies to LIBRARY files only.
  Current CACHE constant: lc76-library-v28 (bumped 8 Aug 2026 —
  LC76_IsatPhone_Operating_Guide.html ADDED and precached, forty-ninth
  session). PRIOR: lc76-library-v27 (bumped 6 Aug 2026 — LC76_Build_Register.html
  ADDED and precached, forty-fourth session). PRIOR: lc76-library-v26 (bumped 30 Jul 2026 — the Open Items
  Dashboard was wired and precached, Session 6). PRIOR NOTE 29 Jul 2026: LC76_Open_
  Items_Dashboard.html is on GitHub but is NOT a precached library file —
  it is deliberately unwired (owner ruling) and DID NOT trigger a bump.
  The bump to v26 belongs to the session that wires it. (v25 bumped
  29 Jul 2026 — the user guide LC76_How_To_Use_This_Library.html ADDED. Prior: v24, 21 Jul 2026 — R42 Fire
  Safety Guide added AND R35 renamed to LC76_Security_Scams_Guide.html;
  the later 21 Jul scam-expansion and index-restructure sessions, and the
  22 Jul decision session, were content-only and did not bump it).
  SESSION 8 CONFIRMATION: content-only across OIR + working papers +
  charter + dashboard + Library Register — no library file added, removed
  or renamed, CACHE STAYS v26.
- Automatic date bumps: on every edited document that carries a masthead
  "Last updated" line, bump it to the session date. Bump index.html's
  header-meta "Last Updated" whenever any library file changes.
- Monthly odometer update: if a session opens in a NEW calendar month and no
  odometer reading has been logged, ask Neil for the current reading before
  proceeding; then update LC76_Facts.txt §2 and propagate current-odometer
  references library-wide (NEVER touch historical event mileages). Current
  reading: 314,546 km (read 15 Sep 2026).
- All country/terrain profiles are on GitHub (incl. Angola). The register's
  "NOT ON GITHUB" section is empty — nothing is intentionally excluded.
- Chrome extension get_page_content is unreliable — prefer web_fetch with a
  direct URL, or ask Neil to paste the URL.
- Supplier naming discipline: forward-looking check/action items must use
  generic terms ("the workshop," "a diesel/turbo workshop"). Historical
  service records may retain supplier names. Dirk Du Plessis / Dup Diesel
  is no longer the preferred turbo/fuel-system workshop.
- MASTERSHIP MAP — which instrument is master of what (standing, owner
  ruling 29 Jul 2026). Three instruments were each behaving as master,
  which is how the working papers came to carry RA-4 as "OPEN — owner-
  gated" five days after the ruling that closed it. There is now ONE
  master per fact class:
    LC76_Open_Items_Register.txt   MASTER OF STATUS — what is open, who
                                   owns it, what closes it, on what
                                   evidence. The database everything
                                   else feeds.
    LC_Service_history_FINAL.xlsx  MASTER OF WORK DONE — Services sheet is
                                   the maintenance/repair ledger of record.
      · ADVISORIES sheet           CAPTURE POINT for workshop-flagged
                                   findings; feeds the register, never
                                   competes with it.
      · Build_Accessories sheet    MASTER OF FITTED KIT & ACQUISITION COST
                                   (capitalised build/accessory items). The
                                   insurance valuation is its derived VALUE
                                   view (filters by fitment type / status).
                                   Feeds insurance; never competes. 6 Aug 2026.
    LC76_Facts.txt                 MASTER OF VEHICLE FIGURES.
    Working papers .xlsx           AUDIT EVIDENCE — method, locator,
                                   external source. Feeds the register.
                                   NOT a parallel open-items list.
    LC76_Service_Checklist.html    MASTER OF THE PRESCRIBED SCHEDULE.
    LC76_Library_Register.txt      MASTER OF WHAT EXISTS.
    R-documents                    MASTER OF DOCTRINE in their topic.
  ONE MASTER, SEVERAL FEEDERS, NONE ARGUING. THIS MAP MUST BE VISIBLE,
  NOT MERELY RECORDED — it is carried here, in the Library Register, and
  it is a Session 0 action to surface it in LC76_How_To_Use_This_Library.
  html. A governance rule the owner cannot see is not governing anything.

- The register is a database with a schema (standing, adopted 29 Jul
  2026). Every open entry carries, in fixed order: the technical title;
  a «WHAT:» line in plain English with no codes; OWNER · GATE · SOURCE;
  and a STATUS line standing alone. The WHAT line is not decoration —
  register titles are written in project shorthand (RA-, EX-, TA-, R-
  numbers, § refs) which is efficient for whoever wrote them and opaque
  to everyone else, INCLUDING THE SECOND CREW MEMBER ON WHOM RA-8 SOLO
  CAPABILITY DEPENDS. A SCHEMA IS ONLY REAL IF SOMETHING REJECTS
  VIOLATIONS: a session-close validator that parses every entry and
  fails loudly on non-conformance is a Session 5 deliverable, and until
  it exists the schema is a wish, not a control.

- A source is a place, not a moment (standing, adopted 29 Jul 2026).
  Every open item names a file plus a locator someone can open —
  document and section, sheet and row, a commit, a supplier reference,
  a physical artifact. "Consultant review 8 Jul" is not a source; the
  conversation no longer exists. MEASURED AT ADOPTION: 26 of 40 entries
  failed this, 13 passed, 1 had no source at all. Those items were not
  badly recorded — they were BORN WITHOUT EVIDENCE, in conversation,
  because the assistant-proposal origination stream has no capture point.

- Evidence grades apply at ORIGINATION as well as closure (standing,
  29 Jul 2026). E1 physical artifact · E2 system-verifiable measurement ·
  E3 third-party written confirmation · E4 recorded owner ruling ·
  E5 assistant narrative or recollection. E5 IS NOT EVIDENCE, AND THAT
  INCLUDES CLAUDE'S OWN REPORT OF ITS OWN WORK: "validation clean, depth
  walks 0/0/0" is E2 only if the script and its output are recorded and
  re-runnable, otherwise it is an assertion. Full definitions and the
  per-item-type closure tests: LC76_Audit_of_Truth_Charter.txt §8–§9.

- Governance files delivered whole, never as patches (standing, 26 Jul
  2026; HARDENED 30 Jul 2026 after a repeated failure). Governance .txt files
  (PI, OIR, Facts, Register) are delivered as COMPLETE, WHOLE FILES — every
  single time, no exceptions. THE OWNER DOES NOT HAND-APPLY ANYTHING. Claude
  MUST NEVER: produce a delta / diff / patch file; produce a partial file with
  a "continue in the next session" stop-note; ask the owner to paste, splice,
  merge, or insert snippets "here and there"; or hand back anything the owner
  must assemble himself. The owner's workflow is RECEIVE-COMPLETE-FILE →
  UPLOAD/COMMIT IT. A patch file is also an orphan of the OI-32/OI-42 class —
  it updates neither the GitHub file nor the PI field. THE OBJECTION THAT
  STOPPED THIS BEFORE — "transcribing 150 KB from context risks silent drift" —
  IS NOT A LICENCE TO DELIVER A PARTIAL FILE. It is a reason to BUILD THE WHOLE
  FILE THE SAFE WAY: assemble it in parts on disk (create_file per part),
  concatenate with bash, then RECONCILE programmatically before presenting —
  assert the structural invariants (one masthead date line, exactly one START
  marker line and one END marker line, no stop-note fragments, clean seams, the
  cross-file next-free == OIR max + 1). That is the same anchored-str.replace +
  before/after reconciliation discipline used on every other governance file;
  it removes the drift risk WITHOUT ever shipping a partial. When the PI lives
  on disk (GitHub / a gist / an upload), edit it in place with count==1
  str.replace asserts; when it lives only in context, rebuild-and-reconcile as
  above. Either way the deliverable is ONE COMPLETE FILE.
- Facts carry intervals; the gate flags overruns; overruns become tracked jobs
  (G2, standing, adopted 19 Sep 2026). A service-interval fact in LC76_Facts.txt
  carries an interval= field; lc76_fact_gate.py's OVERRUN tier flags any derived
  km-since that meets or exceeds its interval; every overrun is captured as an
  Advisories row (LC_Service_history_FINAL.xlsx) and, where a job is due, an OI.
  Maintenance monitoring is thereby an ENFORCED control, not a matter of vigilance.
- Release syncs facts, not conclusions (G3, standing, adopted 19 Sep 2026).
  lc76_fact_release.py updates fact VALUES only; it does NOT update the prose
  CONCLUSIONS drawn from them. A number crossing a threshold silently falsifies the
  sentence around it (in-interval becomes overdue). After ANY fact sweep,
  threshold-bearing conclusions MUST be reviewed; the OVERRUN gate names which facts
  crossed so the review is targeted. Case: the gearbox-oil "within interval" the
  odometer sync falsified, 18 Sep 2026.
- Sweep by corpus-wide enumeration against the master, never by value-hunting
  (G4, standing, adopted 19 Sep 2026). A fact is frozen on several stale bases
  across the corpus; searching for known stale VALUES misses copies on other bases
  (measured: a first R23 sweep was ~35% incomplete; the handover's file-list was
  ~half the real scope). THE CORPUS-WIDE GREP IS THE INVENTORY AND RUNS FIRST,
  before any tagging — a handed-down file list is not an inventory. Map every figure
  to its source EVENT; verify last-changed / work-done against the service ledger
  (master of work done), not a downstream doc; re-grep to zero at the end,
  documented exemptions excepted.
- Systemic verification, not human vigilance (G5, standing, adopted 19 Sep 2026).
  Verification of maintenance status must be SYSTEMIC — a rejecter/gate — never
  dependent on a person noticing (extends "a schema is only real if something
  rejects violations"). Case study, logged honestly: the odometer single-source
  sweep surfaced 3 overdue drivetrain oils a stale document was actively calling
  "within interval"; the first sweep was itself incomplete and the owner caught it;
  the OVERRUN gate + corpus-grep-first now catch this class automatically.
- Content-currency is gated, not remembered (standing, adopted 22 Sep 2026).
  The fact gate catches NUMERIC drift; depth-walks catch STRUCTURE; NEITHER can
  see stale PROSE — a part swapped on the vehicle (Racor->Flashlube) or a status
  that turned (planned->fitted). The rejecter for that class is
  lc76_currency_gate.py, driven by lc76_supersessions.txt: every real-world change
  that retires a term in the corpus is ONE old->new row in the log, and ADDING that
  row is what ARMS the gate against the old term — so a new change cannot slip past
  without arming the check. This is the answer to "a hardcoded term-list fails the
  moment something new changes": the list is DATA in the log, not code in a script,
  and the log row is the same line you write to record the change. WORKFLOW: on any
  real-world entity/status change, add the supersession row FIRST, then fix the
  prose, then run the gate to zero fail-tier hits (warn-tier is surfaced for review
  — historical ledgers, comparison tables and correction notes are legitimate).
  HONEST LIMIT: it catches SUPERSESSIONS that go through the log, not brand-new
  fabrications; log-first is the discipline. It is the enforcement arm of the
  Controlled Duplication Register for named-entity/status currency, and runs at
  session close beside the fact gate. THE LESSON THAT FORCED IT: the tyre and
  Flashlube sweeps used FILE LISTS not a corpus-wide grep (the G4 failure mode) and
  the fact gate could not see the stale names, so "Racor"/"planned"/a layout bug
  survived every automated check until the owner caught them. A gate the owner has
  to run in his own head is not a gate.
- Roll-forward reconciliation on governance files (standing, adopted
  24 Jul 2026). The validation pipeline was built from HTML defects and is
  made of HTML instruments; the governance .txt files — the Open Items
  Register, Facts, the Library Register, this file — are spliced by the
  same str.replace technique and had NO structural gate at all. A
  duplicated "CLOSED LOG" header survived two commits because depth walks
  and node --check cannot see it. The fix is deliberately NOT a checklist:
  a checklist only catches the failure modes someone anticipated, and
  nobody anticipated that one. Instead, reconcile the file the way a
  ledger is reconciled — opening balance, declared movements, closing
  balance, and anything unexplained is a finding.
  METHOD: before editing, snapshot the file's structural invariants; state
  the intended delta; recompute after; assert the difference equals
  exactly what was declared. For the OIR the invariants are the multiset
  of section headers, the set of OI IDs with the section each occupies,
  the count of structural headers, and the closed-log entry count. For
  Facts: section numbers unique and sequential, plus the authoritative
  figures (odometer, GVM, kerb, payload) extracted before and after — an
  UNDECLARED change to a payload figure is exactly what should trip an
  alarm while those figures remain PROVISIONAL.
  CROSS-FILE CHECK, run at session close: assert this file's "next free"
  number equals the register's maximum OI plus one. MEASURE ITEM
  DEFINITIONS, NOT MENTIONS — match line-start "OI-nn" followed by a
  title, because closed-log prose routinely names numbers ("next free is
  now OI-46") and a naive scan reads those as items and reports a false
  failure. This probe did exactly that on its first run, 24 Jul 2026, and
  the file was correct. Same class as the id="changelog" probe error at
  OI-38: when a probe disagrees with the file, suspect the probe first. Two sessions both
  believing the same number is free is a collision no single-file gate can
  see.
  SCOPE THE PI SIDE TOO (added 28 Jul 2026, from a probe error made while
  building the status gate below). The authoritative "next free" is the one
  in the CURRENT session header block. This file ALSO carries historical
  "next free OI-nn" strings inside every archived session block, so a probe
  taking the LAST match in the file reads an old number and reports a false
  mismatch — it read OI-48 against a correct OI-54. Scope the extraction to
  the header block, not the whole file. Third probe error in one session,
  all three caught before being reported as file defects.
  SECOND CROSS-FILE CHECK — STATUS, NOT JUST NUMBERING (standing, adopted
  28 Jul 2026, from a live defect). The check above compares NUMBERING
  only. It cannot see a narrative in THIS file asserting an item is OPEN
  when the register records it CLOSED, and that gap shipped: the
  nineteenth-session block said OI-32/OI-42 "stay OPEN until the owner
  confirms the files are gone" when both had been closed the same day with
  HTTP 404 verification on every file. The claim survived a session and was
  then reported to the owner as live work outstanding. AT SESSION CLOSE:
  extract every OI-nn in this file's HEADER NARRATIVE carrying an ATTACHED
  status claim, and assert none appears in the register's closed log.
  WRITE THE PROBE TIGHT OR IT WILL CRY WOLF. A proximity window — "OI-nn
  within N characters of OPEN or pending" — over-reported by 100% on its
  first run, matching "OI-50 OPENED & CLOSED" and an unrelated "PENDING"
  about an index.html card two sentences away. The status claim must be
  ATTACHED to the reference — OI-nn[/OI-mm] (stays|remains|is|are)
  (OPEN|pending|unresolved) — not merely near it. Same lesson as the
  id="changelog" and next-free probes: when a probe disagrees with the
  file, suspect the probe first.
  AND NOTE WHAT CAUSED THE DIVERGENCE. The register is authoritative for
  what is OPEN; this file's narrative records WHAT HAPPENED. Where they
  disagree on STATUS, THE REGISTER WINS. The divergence exists because
  open-item DETAIL was duplicated into the narrative — which is exactly
  what the standing convention forbids: keep a POINTER here, not the
  detail. A duplicated fact is a seam that reports no error.
  LIMITATION, STATED SO THE GATE DOES NOT CREATE FALSE COMFORT: this
  catches SHAPE, NOT TRUTH. A well-formed entry containing an invented
  number passes every one of these assertions. The character-count error
  of 24 Jul was caught by working convention (d), verify-before-you-report,
  and not by any structural gate. They are different controls doing
  different jobs and neither substitutes for the other.
- Idempotence, not just anchors (standing, adopted 24 Jul 2026, from a live
  defect). `assert count == 1` proves the ANCHOR is unique; it proves
  nothing about whether the edit has already been applied. When a
  replacement CONTAINS its own anchor — the common case for "keep the old
  block and add a new one after it" — the guard passes on every re-run and
  silently duplicates the insertion. This happened on R32 on 24 Jul: a
  script re-run after two unrelated anchor failures applied the same block
  twice. Any script that might be re-run must ALSO assert that a
  distinctive string from the NEW content is ABSENT before writing, and a
  post-edit count of that string should be 1. Recovery is a SHA-pinned
  re-fetch and a single clean application.
- Encode before you truncate (standing, adopted 24 Jul 2026, from a live
  defect). Never write an edited file with open(path,'w') — that truncates
  the target the instant it is called, so any later failure (a bad encode, a
  lone surrogate in an emoji, an exception mid-write) destroys the file
  before a single validation gate has run. Encode the full string to bytes
  FIRST, confirm that succeeded, then write binary. The validation pipeline
  runs after the write and therefore cannot protect against this class at
  all; only the write pattern can. Recovery is a single SHA-pinned fetch,
  which is one more reason pinning is not optional.
- Emergency number format (standing, adopted 23 Jul 2026, first applied
  24 Jul). Every emergency or assistance number in the library is stored and
  printed in FULL INTERNATIONAL FORMAT (+country code). Where only a national
  short-dial number exists (SA 082 911, 084 124), it is labelled explicitly as
  short-dial / in-country-only, and it must NEVER appear in a satellite-comms
  activation instruction without that label — a short-dial code cannot be routed
  by Iridium or Inmarsat, so the instruction cannot execute in exactly the
  scenario where the satphone is the only channel. Numbers are NEVER converted
  to international format by inference: an unverified international equivalent
  is a fabricated number in a life-safety document (F65 class) and must be
  obtained from the provider. A number that is provider-published but has never
  been DIALLED on the handset that will dial it is recorded as such and carries
  a test-dial action (RA-10) — published is not the same as proven.
- Share-call codes never route by satellite (standing, 25 Jul 2026). A SA
  national share-call number (prefix 0860 / 0861) and the three-digit /
  short-dial codes (112 / 082 911 / 084 124) work only on an in-country
  mobile network. They CANNOT be routed by Iridium or Inmarsat, and
  prepending +27 to a share-call number does NOT create a dialable form
  (the rejected +27 860 555 505 was exactly this error). Where a provider's
  only published emergency line is share-call, that is recorded as a GAP
  with an action to obtain a geographic (+27 landline) equivalent — never
  papered over with a fabricated international form.
- A provider named in the library is not a provider the crew is a MEMBER of
  (standing, 25 Jul 2026). A medevac or assistance number must state the
  membership/cover position beside it. A number the crew cannot claim
  against is an own-cost line and is labelled as such (Netcare 911, demoted
  25 Jul) — otherwise it reads as cover it is not. R29 remains the
  provider-evaluation guide; naming a provider there is comparison, not a
  decision, until one is chosen (OI-43).
- Controlled Duplication Register (standing, adopted 23 Jul 2026, seeded
  24 Jul). A short list, held in the Open Items Register, of facts permitted to
  exist in more than one document, each with ONE named OWNER and a mandatory
  PROPAGATION LIST. Any change to a registered fact triggers a sweep of that
  entire list in the SAME session — not a follow-up item. Seed rows: emergency
  numbers (OWNER R29 §6 → R10, R1, R16, R38); SOS activation steps (OWNER R29 §7
  → R10, R16); GVM/kerb/payload (OWNER Facts → R11, R12, R29, R28); licence &
  disc dates (OWNER Facts → R29, R39); daily house load in Ah/day (OWNER R5
  breakdown table → R7, LiFePO4 Calculator — added 26 Jul 2026 BECAUSE IT HAD
  ALREADY FAILED: 77 Ah/day stood in three documents while two of those
  documents' own component breakdowns summed to 65.3. Any change sweeps the
  list in the SAME session, INCLUDING the derived values it drives — daily
  DoD %, wild-camp autonomy, 30%-alarm reserve hours, solar-deficit
  arithmetic); and electrical device names (OWNER Facts §8 / R7 / R26
  → the electrical-stack docs — added 8 Aug 2026 via OI-102, ALSO because it
  had already failed; the full propagation list lives in the OIR). This is the same move as the R30
  owner-flag column, and it converts a latent seam into a lookup.
  ABSORBED BY THE FACT STORE (SSOT, OI-107, 18 Sep 2026): under the two-domain
  source-of-truth doctrine the CDR is being COMPLETED and absorbed by the fact
  store in LC76_Facts.txt — each registered fact becomes a store entry with an
  owner and (if derived) a formula, and the release+gate pipeline enforces the
  propagation the CDR used to prompt by hand. MIGRATED SO FAR: the odometer and
  its km-since derivations (Phase 1). The rows above migrate cohort by cohort in
  later phases; a row is retired only once its fact lives in the store and its
  slots are tagged and gated.
  EMERGENCY-NUMBERS ROW, STATE AT 25 JUL 2026: the fact grew a PERSONAL-
  COVER set (Genesis/Neil, Bonitas-AZOZA/Mary-Ann, Santam) and Netcare 911
  was DEMOTED to a non-member own-cost line; both changes were swept across
  the whole propagation list in the same session, R29 §6 first. R38 carries
  no numbers and needs no edit. Two rejected numbers (+27 11 991 8000,
  +27 860 555 505) live in the R29 §6 provenance note so they are not
  re-introduced; satphone-dialable Santam/Genesis equivalents are OI-47.
- Two-tier content placement — pattern vs incident (standing, 21 Jul 2026).
  Durable PATTERNS live in the topic reference doc; location-dated,
  perishable INCIDENTS live in the relevant country/terrain profile (the R35
  scam-pattern vs profile-incident split; mirrors R14/R40 strategy-vs-spanner
  and R14/TPMS strategy-vs-tool). Applies to any perishable, dated,
  place-specific observation.
- Section renumbering is decided by MEASUREMENT, never by rule (standing,
  24 Jul 2026). Before inserting a section into an established document, sweep
  the corpus for external §-references and anchor links into that document. Zero
  references → insert and renumber (the OI-36 profile case). References exist →
  append and point (the R35 §12 and R29 §13 cases). Both outcomes arose from the
  same test two days apart. Neither "always append" nor "always renumber" is a
  safe default; the sweep is cheap and settles it.
- Project-knowledge currency (standing, adopted 27 Jul 2026, from OI-49).
  Project knowledge carries NO duplicate of a governance file that lives on
  GitHub. Facts, the Library Register and the Project Instructions were all
  found there five to six weeks stale, the PI fossil at 7% the size of the
  real file, and were removed. If such a copy is ever added back, it is
  refreshed in the SAME session as the GitHub edit or it is deleted — never
  left to age. THE REASONING IS THE GENERAL ONE THIS LIBRARY KEEPS
  RELEARNING: a stale duplicate is a seam that reports no error. Absence is
  safer than staleness, because absence forces the correct behaviour (fetch
  live, SHA-pinned) while a wrong copy invites the incorrect one. NOTE THE
  DISTINCTION that makes this safe: the Project Instructions FIELD is a
  different mechanism from a project-knowledge FILE — the field is injected
  automatically into every session and is what actually governs behaviour,
  so it must be kept current; the file was pure duplication and is gone.
- Archives are not reconstructed, and a redacted archive says so
  (standing; promoted 29 Jul 2026 from the twentieth/nineteenth-session
  narratives ahead of their archiving). The Open Items Register and
  LC76_Session_History.txt are ARCHIVES. Their narrative is never altered,
  removed or rewritten after the fact — a correction is an ANNOTATION
  against the original sentence, not a replacement of it, because a
  reconstructed record reads as contemporaneous when it is not (the F65
  class applied to governance). TWO COROLLARIES. (i) Withdrawing a VALUE is
  a different act from altering a RECORD, but a reader who finds a pointer
  where a number used to be must be told why: an archive that has been
  redacted carries a DATED REDACTION NOTICE naming the class of data
  withdrawn, stating that no narrative was altered, and noting that prior
  commits still hold the originals. Otherwise the redaction itself becomes
  the invisible seam. (ii) A missing session is recorded as a LABELLED,
  DATED GAP and sessions are NEVER RENUMBERED to close it — renumbering
  would silently re-point every existing cross-reference to the wrong
  ordinal.
- Precedence between the two narrative files (standing; promoted 29 Jul
  2026). LC76_Project_Instructions.txt is authoritative for all doctrine
  and working conventions. WHERE THIS FILE AND LC76_Session_History.txt
  DISAGREE, THIS FILE WINS. Nothing in the history file is a live rule.
  Note this sits ALONGSIDE, and does not contradict, the register rule
  above: the Open Items Register wins on an item's STATUS; this file wins
  over the history file on NARRATIVE.
- The cover / medical-scheme cluster is Class P — RETAIN (standing ruling,
  28 Jul 2026; promoted 29 Jul 2026). The insurer and both medical schemes
  appear in ~230 instances across the R29/R10/R1/R16 emergency stack and
  are IDENTIFYING, but they are deliberately NOT redacted: a paramedic or a
  bystander holding the laminated R1 card needs to know which scheme to
  claim against, and stripping the cluster would trade a nuisance call
  against a delayed evacuation. The residual is controlled BEHAVIOURALLY,
  in R35's standing habits, not by redaction. A future security pass must
  not re-litigate this as an oversight. NOTE THE METHOD, which generalises:
  this cluster was provisionally graded Class R (redact) in the opening
  review and regraded only after MEASURING what the strings do — a
  classification made before measurement is a hypothesis.
- A hard 404 on the repo is a working-method BLOCKER, not a transient
  (standing, 26/27 Jul 2026; promoted 29 Jul 2026). If github.com/
  Overlandslabs/Consumption returns a hard 404 on the repo page, BOTH
  branches of raw content and the Atom feed, while the org page loads
  normally, that is GitHub's mask for private/renamed/deleted — not the
  familiar unauthenticated-API 403. THE CORRECT BEHAVIOUR IS TO STOP, not
  to fall back on project-knowledge or recalled copies and carry on: a
  session that cannot fetch SHA-pinned cannot edit. Resume only when
  access returns, re-pinned.
- Symptom triage for "the site looks old" (standing; promoted 29 Jul 2026).
  Three layers fail differently and are routinely confused. Stale content
  in the owner's browser while the repo is current = the PWA
  SERVICE-WORKER CACHE serving offline-first. A 404 in a FRESH browser
  while the SHA-pinned raw source returns 200 = the GITHUB PAGES
  DEPLOYMENT layer. A 404 on raw content and the Atom feed = the REPO
  itself (see the blocker rule above). Diagnose which layer before
  proposing any fix; a CACHE bump does not repair a Pages deployment and a
  redeploy does not clear a service worker.
- Publish trips retrospectively (standing, adopted 28 Jul 2026, Gate C of
  the Karoo date-exposure pass). A trip itinerary published BEFORE or
  DURING the trip carries RELATIVE DAY NUMBERING (Day 1, Day 2 …) or a
  season ("winter 2026"); exact calendar dates are held in the private
  annex and go into the document only once the crew is home. THE REASONING:
  a dated day-by-day schedule, set against a named crew, a distinctive
  vehicle and a known region, publishes precisely which nights a South
  African property stands empty and for how long. Note the scope — this is
  NOT a two-date problem. The Karoo itinerary carried 22 distinct date
  tokens across 55 occurrences; generalising only departure and return
  would have left every intermediate leg dated. Anyone scoping this class
  of edit as a two-string change has mis-scoped it.
  CRITICALLY, THIS IS A TEMPORARY SUPPRESSION WITH A DEFINED RESTORATION
  POINT, not a permanent redaction of the RC1 class. Dates are dangerous
  BEFORE the fact and harmless AFTER it, and afterwards they carry real
  documentary value — they are the record of a real trip and the basis of
  the R23 §7 post-trip review. Annex sections holding trip dates therefore
  carry a RESTORE-AFTER note, which is the OPPOSITE instruction from the
  permanent §1/§2/§3 and MUST NOT be copied from them by pattern. The
  restoration moment is the post-trip review session itself.

- Personal-data discipline (adopted 20 Jul 2026; EXTENDED AND PARTLY
  SUPERSEDED 28 Jul 2026 by the RC1 / name reduction pass). SA ID numbers
  and other personal identifiers NEVER appear in committed files — the
  repo is public. Library documents carry blank fill-in lines; completed
  versions (e.g. the affidavit print master) are held offline only, never
  committed.
  SUPERSESSION, 28 Jul 2026. The former rule read: "Vehicle identifiers
  (VIN, engine, registration) are already public-facing and may appear."
  That is now SPLIT, because it was being read to cover the registration
  CERTIFICATE as well as the vehicle:
  · STILL PUBLIC — VIN, engine serial, chassis code, the CURRENT
    registration plate, and the tare figure. Each is stamped on the
    vehicle, visible on it, or a technical weight with live RA-1 value.
  · NOW OFFLINE (PRIVATE ANNEX §1) — the registration-certificate
    fields: register number, series name as registered, vehicle
    description, first-licensed date, PREVIOUS licence number, and
    certificate issue date/office. None is visible on the vehicle; all
    exist only on a document a stranger should not hold; together with
    the registered owner's full legal name they corroborate a false
    provenance story.
  · FULL LEGAL NAMES (PRIVATE ANNEX §2) do not appear in committed
    files. FIRST NAMES are retained deliberately throughout the library
    — a first name is not a join key, and removing ~280 instances would
    cost readability for no security gain.
  REASONING, recorded so it is not re-litigated: individually each of
  these facts is minor. Assembled with a distinctive vehicle, a region
  and a dated absence window they form a usable package for registration
  fraud and absence targeting. The test applied to each fact was: does a
  reader in the field need this specific VALUE to do something, and what
  could a stranger do with it that he could not do without it.
  RESIDUAL EXPOSURE, ACCEPTED KNOWINGLY: the repo history is public and
  was NOT rewritten (owner ruling, 28 Jul 2026). A filter-repo rewrite
  would rewrite every SHA and break the SHA-pinning convention the whole
  working method rests on, for material that is either self-expiring or
  visible on the vehicle. Redaction is forward-looking only.
- A redaction record describes the withdrawn value, never reproduces it
  (standing, adopted 28 Jul 2026, from a live defect in the pass that
  created it). When a session records WHY a value was withdrawn, the
  narrative must name the CLASS of data ("an abbreviated initial-plus-
  surname form of the owner name"), never quote the string itself. The
  28 Jul RC1 pass wrote its own session block and closed-log entry quoting
  the abbreviated name form it had just removed, reintroducing the surname
  into two governance files at the moment they recorded its removal. The
  hazard had been named explicitly earlier in the same session for the
  register number and avoided there; it recurred in a different field.
  COROLLARY FOR THE SWEEP: the zero-assert stale sweep must run AFTER the
  governance narrative is written, not before — a sweep run before the
  session block exists cannot see what the session block reintroduces.

- GPS coordinate policy: individual rock art panel locations (Tsodilo Hills),
  specific Himba village coordinates, black rhino sighting locations, and
  Maasai boma locations must never appear in public-facing documents. Country
  profiles use public town / border / park-gateway coordinates only. (The
  experimental route map uses public park/town/border coordinates only.)

SESSION EXECUTION & VALIDATION (every HTML session)
- Fetch SHA-pinned (see SOURCE OF TRUTH below), then edit surgically. Diff
  the LIVE Project Instructions header at HEAD against any conversation-
  supplied copy BEFORE drafting a spec — a pasted PI is a project-knowledge-
  class copy, not the source of truth (lesson, 21 Jul 2026).
- Edits: Python str.replace with an anchor guard (assert count == 1) on every
  old string. No blind or global replacements.
- Validation gates before staging: div balance open == close (run a depth walk
  — final depth 0, min depth 0, no negative events — not just a tag count;
  STRIP all <script> blocks and HTML comments from the document before
  walking — convention adopted 12 Jul 2026);
  node --check on every inline <script>; stale-term sweep to zero (documented
  exemptions stand) — and the sweep MUST run WIDER than the change spec: the
  instances you list are not the instances that exist, so sweep the whole
  corpus for the term, not only the sites named in the change spec (18th
  session — a planned 18-anchor '77' sweep found five more, including a table
  total that would otherwise have shipped under a corrected headline);
  anchor/id uniqueness. For tag-balance repairs, walk spans
  too. PRESENCE IS NOT VISIBILITY (fifty-ninth session, 24 Sep 2026;
  promoted the same day, PI restructure P7): a derived view that reconciles
  1:1 to its source proves PRESENCE only — after any regeneration also check
  that each item's band, tags and title read correctly (OI-108 was on the
  dashboard, reconciled, and still effectively invisible).
- Working papers (LC76_Library_Audit_WorkingPapers.xlsx): fetch live; edit
  append-only with openpyxl; NEVER run recalc.py; preserve the COUNTIF/COUNTIFS
  formula-cell baseline = 9 (Summary!B10–B14, B17–B20; owner-adjudicated
  12 Jul 2026, BASE-ADJ-1 in EX_Register). The baseline counts FORMULA CELLS,
  not substring occurrences (the retired substring metric of 13 double-counted
  two cells + a prose mention). Clone cell styles via copy.copy(src._style);
  verify testzip() == None; never load data_only=True.
- Known do-not-re-flag items: Service_Checklist has zero horizontal scrollers →
  vacuously compliant on the overscroll check; R5 carries two dead sticky
  selectors (.hdr and .masthead,.hero) from an earlier style swap — only the
  live .sticky-wrap is patched. CORRECTED 22 Jul 2026 — the note that
  Angola/Namibia profiles carry "legitimate negative depth-walk values from
  JS template strings" was a MISDIAGNOSIS and cost a real defect a month of
  invisibility. Both files carried genuine stray closing tags that survive
  script-stripping: Angola root-caused and fixed 12 Jul (now 126/126),
  Namibia found and fixed 22 Jul (was 156/157, now 167/167) — both from the
  same superseded multi-stripe masthead flag construct left behind when the
  SVG flag replaced it. STANDING RULE: a negative depth walk is treated as a
  real defect until root-caused in the static HTML with scripts and comments
  stripped. If a walk goes negative, find the tag. Facts §11 carries ONE intentional "later 2031" string
  — the 17 Jul supersession reference, exempt from the stale sweep. R28's
  "2,850" is the WHEELBASE in mm, not a stale GVM; the Fuel Log Analyser's is
  odometer data inside an embedded CSV; the Tyre Pressure Calculator offers
  both 265/70 and 265/75 by design (size selector; 265/75 marked "Current"
  since 22 Sep 2026 — the 265/70 option is comparison, not a stale term).
  All three were adjudicated as false positives in the Tier 5 sweep, 21 Jul
  2026 — do not re-raise them. The intentional hi-lift doctrine strings in R3
  and R9 (documented no-hi-lift doctrine statements) are stale-sweep exempt —
  a 'hi-lift' sweep will hit them and must not treat them as defects.
  LC76_How_To_Use_This_Library.html carries a Last-updated line by DELIBERATE
  DIVERGENCE (infrastructure class, otherwise masthead-exempt; Library
  Register) — do not 'correct' it. mary-ann-70/ on the repo is an intentional
  NON-LIBRARY artifact (Library Register, 'Known non-library artifacts') —
  out of scope for audits, sweeps and precache checks; do not re-flag it
  (both added 24 Sep 2026, PI restructure).
- Reconciliation discipline: sw.js / HTML fixes can land on GitHub outside a
  Claude session. Session start must diff live HEAD against this file's DEFECT
  FIX log and the CACHE version line, not just against the EX_Register. Commit-state is a claim like any other:
  when the owner reports files as committed, verify with a cache-busted raw
  fetch of the main-branch copy (byte count + expected markers) before editing
  against that HEAD — do not edit on a reported-but-unverified commit (14th
  session: reported-committed files had not landed).

MODEL ROUTING (updated 24 Sep 2026, owner ruling D5 — stated by CLASS so it
does not go stale at the next model release)
- Opus-class (currently Opus 5.5) for: new document builds, structural or
  cross-file changes, large-file surgical edits, architecture or cross-
  reference work, all EX sessions, all governance restructures, anything
  where an introduced error propagates quietly.
- Sonnet-class (currently Sonnet 5) for: established single-file edits with
  an unambiguous change spec, changelog entries, dual index + register
  updates.
- Haiku-class (currently Haiku 4.5) for: low-consequence lookups only —
  R-number checks, Facts.txt queries, fast Q&A — where correctness
  consequences are low and recoverable.

SOURCE OF TRUTH / PROJECT KNOWLEDGE POLICY
- Source of truth is SPLIT INTO TWO DOMAINS (SSOT programme, OI-107, adopted
  18 Sep 2026 — supersedes the blanket rule for the fact domain only). PROSE
  domain: GitHub / the HTML document remains the single source of truth for a
  document's authored prose. FACT domain: LC76_Facts.txt is the single source
  of truth for FACTS — the fact-values shown in HTML (and in Facts’ own
  readable lines) are DOWNSTREAM RELEASED COPIES, never hand-edited. A fact is
  edited in the store and released to the documents by lc76_fact_release.py;
  the gate (lc76_fact_gate.py) FAILS on any tagged slot that disagrees with the
  store, and the fact-store validator (LC76_Fact_Validator.py) rejects a
  malformed store. WORKFLOW: edit the fact in LC76_Facts.txt -> run
  lc76_fact_release.py -> gate passes (or lists every drift) -> commit; a
  monthly odometer reading is one store line + release, never a library-wide
  hand-sweep. Governing architecture: LC76_Single_Source_of_Truth_Design.txt.
  THE FACT STORE IS A MAINTENANCE-MONITORING INSTRUMENT (G1, 19 Sep 2026), not
  only a drift-preventer: a service-interval fact whose km-since meets or exceeds
  its interval is a SAFETY ALERT, and the odometer reading is the trigger that
  surfaces overruns — a primary purpose of the programme (see the OVERRUN
  convention in WORKING CONVENTIONS). Directly serves the "do not break down
  because something was missed" goal.
  Project
  knowledge holds only the lightweight authoritative texts. Never edit a
  document against a project-knowledge copy — always fetch live, SHA-PINNED:
  take the HEAD SHA from the repo Atom feed
  (github.com/Overlandslabs/Consumption/commits/main.atom — the first
  /commit/<40-hex>), then fetch
  raw.githubusercontent.com/Overlandslabs/Consumption/<SHA>/<filename>.

SURGICAL EDIT CHANGE-SPEC FORMAT
- Surgical edits must follow the standard change-spec format before
  execution: (1) target file and R-number, (2) exact old string to be
  replaced, (3) exact new string, (4) propagation map. Claude proposes the
  full spec; Neil confirms before any edit is made. Pre-approved handover
  packs are specs in themselves — execute autonomously within their scope.

▲▲▲ END — PROJECT INSTRUCTIONS ▲▲▲
