LC76 OVERLAND REFERENCE LIBRARY — PROJECT INSTRUCTIONS
═════════════════════════════════════════════════════════════════

Last updated: 30 July 2026, thirty-second session (AUDIT OF TRUTH SESSION 6 —
THE OPEN ITEMS DASHBOARD WIRED. Regenerated from the remediated register as a
derived view (50 open items after OI-59 was CLOSED by the wiring), linked from
index.html §8 Governance, and precached in sw.js — CACHE v25 → v26. OI-59
CLOSED. next free OI-79. Session 7 — the systemic-generation sweep — is the
last session).

SESSION 6 IN ONE PARAGRAPH. Re-pinned to HEAD a0b115ba (owner had committed the
Session-5 files byte-identical; the validator PASSES on the committed register).
The prototype dashboard was 40 hand-authored article blocks reflecting the
PRE-remediation register — so wiring was not link-and-precache; the page had to
be REGENERATED from the current register first (the derived-view pattern the
OI-59 entry itself mandates). A parser read the 51 remediated entries into
records and emitted the article blocks, banding each by the new §10 STATUS
vocabulary rather than guessing — WAITING-DATE items go to 'date-locked', the
rest to 'live', safety to the top — and surfacing the new confirmation-date
field on every card. WIRING COMPLETES OI-59, so Session 6 CLOSED it (strict-tier
evidence: the index link and the sw.js precache entry at the commit, plus
re-runnable validation); the register therefore drops to 50 open, and the
dashboard was regenerated a second time from the 50-item register so it does not
list its own just-completed build task. WIRED: an index.html §8 card, a sw.js
precache entry, CACHE v25 → v26 (the dashboard IS a precached library file — the
deliberate exception recorded in the Library Register). The page carries a
'generated from the register at date/SHA' line so a cache-served stale copy is
self-evident. VALIDATION: dashboard script-stripped depth walk 0/0 on
div/span/article/header/nav/button, node --check clean, TOC index contract 5/5,
ids unique, no browser storage; index.html observer contract 8/8 and its Last
Updated bumped to 30 Jul; sw.js node --check clean; the OIR validator PASSES on
the 50-item register. Two probe artefacts (a hardcoded total of 51 after the
OI-59 close; a data-target regex catching a JS string) were caught and fixed —
the files were correct. POST-BUILD FIX (owner report, same session): each
card's status line was rendering only the FIRST physical line of the status
narrative — breaking mid-sentence and duplicating the opening of the
expandable detail — so the on-card status element was REMOVED; the full status
stays in the detail and the section band conveys the category. Content-only,
CACHE STAYS v26. TWO FURTHER CARD REFINEMENTS (owner, same session): the
confirmation NEXT value was promoted to its own amber meta-label (matching
WHO/WHEN/SOURCE/CONFIRMED), and eight WHAT lines were neutralised of first-
person / 'both' phrasing (OI-2/4/28/69/73/76/77/78) since WHO already names the
crew — the WHAT line is the SOURCE of the card heading, so the fix is at source
in the register and the dashboard regenerates from it. Register + dashboard
re-staged; validator PASSES; CACHE STAYS v26. FILES THIS ROUND: LC76_Open_Items_Dashboard.html,
index.html, sw.js, LC76_Open_Items_Register.txt (OI-59 closed),
LC76_Audit_of_Truth_Charter.txt, LC76_Library_Register.txt, plus this file.
CACHE BUMPED v25 → v26 — a library file was added to the precache.

PRIOR (30 July 2026, thirty-first session) (AUDIT OF TRUTH SESSION 5,
PHASE 5A EXECUTED — REGISTER REMEDIATION against the Session-4 owner rulings.
ELEVEN new items opened (OI-68…OI-78), 40 open items become 51; six bundled
entries split; OI-62/63 re-rated SIGNIFICANT; status vocabulary enforced; a
confirmation-date field added to every entry; twelve closures reclassified
DONE-PENDING-EVIDENCE; Facts §3 recast to pointers; ADV-010 re-mapped. NO item
CLOSED, CACHE STAYS v25; next free OI-79. Phase 5B EXECUTED — the schema
validator LC76_OIR_Validator.py built and run, the register PASSES. SESSION 5
COMPLETE).

SESSION 5 PHASE 5A IN ONE PARAGRAPH. Re-pinned to HEAD 6c64d70c (the owner had
committed the Session 2–4 governance files byte-identical to the working
copies; the OIR itself was untouched, so the fieldwork base held). The register
was remediated against rulings D1–D6, edited as a whole file by a parser-based
REBUILD of the open section with byte-level reconciliation — every unchanged
entry asserted verbatim, every changed entry declared. WHAT CHANGED: (1) SPLITS
(D3) — the bundled entries were reduced to one job each and their siblings
carved out as new items: OI-11 → OI-68 (kit acquisition; R23/§06 struck as
closed elsewhere); OI-24 → OI-69 (Mary-Ann's MANDATORY card renewal); OI-62 →
OI-70 (thermostat & water pump); OI-61 → OI-71 (front knuckle); OI-13 → OI-72
(RA-8 solo assessment); OI-4 → OI-73 + OI-74 (medical screening; medical kit).
OI-66 was KEPT WHOLE (one ORDERED job — breather before reseal — so A6 passes)
and OI-12 kept whole with the carnet interlock flagged as the binding
sub-constraint. (2) RE-RATE (D2) — OI-62 and OI-63 rated SIGNIFICANT on
integrity/schedulability, not quantum. (3) ADV-010 (CC-7) — the boost-pipe
clamp check, mis-referenced to OI-7, given a subject-correct home as OI-75 and
the ref corrected in LC_Service_history_FINAL.xlsx (Advisories). (4) G3 folded
in (owner instruction) — the expiry-mapping generator run early: three genuine
untracked gaps opened, OI-76 passport validity, OI-77 insurance duration-cap,
OI-78 card expiry; first-aid/tyre-DOT/medication shelf-life noted on existing
items rather than opened. (5) STATUS VOCABULARY (§10, CC-9) enforced — BLOCKED
OI-59; DONE-PENDING-EVIDENCE OI-8/OI-36; WAITING-DATE OI-13/14/21/22/25/28/72;
STANDING OI-20/26. (6) CONFIRMATION-DATE FIELD (CC-8) added to all 51 entries
and to the schema (line 5). (7) CLOSED-LOG (D4 strict tier) — twelve
non-strict-clean closures reclassified DONE-PENDING-EVIDENCE by one dated
annotation block (OI-67/58/55/52/40/23/15/9/34/35/38 + OI-49); the fifteen
strict-clean stay CLOSED. (8) FACTS §3 (CC-6) recast from a competing status
list into pointers into the OIR — the event figures stay (Facts is master of
figures), the status defers to the owning OI, and the parallel '~N km ago'
verdicts that disagreed with the register are withdrawn. RECONCILIATION: OIR
51 unique open ids, sections and closed log intact, confirmation field on all
51; Facts figures (odometer, GVM, kerb, tare) byte-unchanged, only §3 edited;
service xlsx testzip None. NO item was closed and no library HTML touched —
CACHE STAYS v25. Phase 5B stands up the schema validator (charter R2) and runs
it against this remediated register. FILES THIS ROUND: LC76_Open_Items_
Register.txt, LC76_Facts.txt, LC_Service_history_FINAL.xlsx, LC76_Library_
Audit_WorkingPapers.xlsx, LC76_Audit_of_Truth_Charter.txt, plus this file.

SAME-SESSION ADDENDUM — PHASE 5B (schema validator, charter R2). The validator
LC76_OIR_Validator.py was built and run against the remediated register. It
parses every open entry and FAILS LOUDLY (non-zero exit) on any of: a missing
WHAT line, a missing OWNER·GATE·SOURCE meta line, a STATUS token outside the
§10 controlled set, a missing CONFIRMED/NEXT-DUE field, a duplicate id, or a
PI-vs-OIR next-free mismatch; it WARNS (non-blocking) on a source that names a
moment not a place (A7) and on a CLOSED closure lacking a checkable artifact
(A10 strict). ON FIRST RUN IT EARNED ITS PLACE: one hard FAIL — OI-10 carried a
two-part meta line (owner·gate, no source), a pre-existing defect the rebuild
had preserved verbatim — now fixed; and OI-69's source improved to name its E3
evidence. The register now PASSES with 7 documented A7 WARNs (the legacy
consultant-review-8-Jul source class, CC-3 — surfaced every run rather than
remediated this session). A schema is only real if something rejects
violations: it now does. Session 5 is COMPLETE; Session 6 wires the dashboard
(CACHE v25 → v26). FILES: LC76_OIR_Validator.py (new), LC76_Open_Items_
Register.txt (OI-10/OI-69 fixes), LC76_Library_Register.txt, plus this file.

PRIOR (30 July 2026, thirtieth session) (AUDIT OF TRUTH SESSION 4
EXECUTED — THE CONSOLIDATED FINDINGS DOCUMENT BUILT: LC76_Audit_of_Truth_
Findings.txt, a new governance file presenting all forty items (10
SIGNIFICANT, 27 MODERATE, 3 HOUSEKEEPING, PROVISIONAL pending M5) and the
27-closure A10 result, ending in a DECISIONS-REQUIRED schedule D1–D7 for the
owner. NO item remediated, NO OIR edit, NO OI consumed, CACHE STAYS v25; next
free OI-68).

SESSION 4 IN ONE PARAGRAPH. Session 4 is the charter's owner-ruling gate —
findings first, complete, in one document, with NO edits before the owner
rules (charter M4). The consolidated findings were built as a governance .txt
in the charter's family (LC76_Audit_of_Truth_Findings.txt) rather than an
HTML report, because the Session-6 dashboard IS the eventual HTML view of the
register and building one now would pre-empt it; the .txt is also the natural
input to Session-5 remediation. THE DOCUMENT consolidates Sessions 0–3: the
result in one table (40 items — 10/27/3), the ten SIGNIFICANT findings grouped
by why they are significant (safety/mobility OI-61/OI-46/OI-4; feasibility/
obligation OI-12/OI-1/OI-24/OI-43/OI-47; register-integrity OI-11/OI-60), the
two maintenance reconciliations carried separately because their rating turns
on M5 (OI-62/OI-63), the closed-log finding (F-D's '7 of 27' unreproducible —
25/27 checkable under A10 as written, 15/27 clean under the strict §8 reading,
neither giving 7), the cross-cutting findings CC-1…CC-13 consolidated, the
independence statement, and a DECISIONS-REQUIRED schedule D1–D7. THE HEADLINE
REMAINS the closed-log result: the instrument that measures evidence carried
an unevidenced count (F-D), the sharpest single vindication of the audit. D4
is the highest-leverage ruling — which tier (loose 'names something checkable'
vs strict 'named commit + re-runnable validation') becomes the closure
standard the Session-5 validator enforces; the strict reading reclassifies ~11
past closures as DONE-PENDING-EVIDENCE. RATINGS STAY PROVISIONAL until the
owner re-evaluates M5 (D1), which the charter reserves for this session.
NOTHING WAS REMEDIATED and the OIR was not touched — Session 5 remediates only
against the rulings recorded here, then stands up the schema validator
(charter R2). The findings file is registered in LC76_Library_Register.txt as
a governance file (not precached, no CACHE bump). FILES THIS ROUND:
LC76_Audit_of_Truth_Findings.txt (new), LC76_Library_Register.txt, plus this
file.

SAME-SESSION ADDENDUM — OWNER RULINGS ON SESSION 4 (D1–D6, 30 Jul 2026). The
findings were presented and the owner ruled; the rulings change two governance
instruments and are recorded here before Session 5 touches the register.
D1 — M5 RETIRED. Cost is not a materiality test: a job vital to safety, legality
or mobility is done regardless of price and is never deferred or downgraded for
cost; where cost bears it is an INPUT to a DECISION item, never a warrant or a
rating driver. This aligns §4.3 with its own opening line ("materiality is
consequence, not subject matter") — a rand threshold was the one magnitude test
in a consequence list. The R50,000 working figure is withdrawn and must not be
re-attached. D2 — QUANTUM IS NOT A RATING INPUT. OI-62 and OI-63 are re-rated
MODERATE → SIGNIFICANT on INTEGRITY and SCHEDULABILITY (the register cannot say
when these safety-relevant jobs are due and three instruments disagree), NOT on
quantum; both jobs are done regardless. The tally becomes 12 SIGNIFICANT / 25
MODERATE / 3 HOUSEKEEPING. D3 agreed (register-integrity remediation approved).
D4 accepted with the STRICT closure tier — a closure needs a named commit AND
re-runnable validation, "validation clean" prose is E5, ~11 past closures become
DONE-PENDING-EVIDENCE, and the Session-5 validator enforces the bar. D5 agreed
(F-C OI-39 and F-D corrections accepted). D6 agreed (OI-61/OI-46/OI-4 confirmed
on their action lists). CHARTER AMENDED TO ISSUE 3: §4.3 M5 retired, §12 M3
gains the consequence-only rating clause, A10 and §14 F-D carry the strict
standard and the withdrawal of "7 of 27", §14 F-C carries the OI-39 correction.
The findings document records the rulings (RULINGS block, §8) and the re-rate;
the Library Register reflects 12/25/3 and charter issue 3. SYSTEMIC GENERATORS
(owner query): the eight Appendix-A generators have NOT been run — Session 7,
deliberately last; several half-sprung incidentally (G3→Mary-Ann's card,
G4→OI-12, G5→OI-59, G6→OI-1); G3 (expiry mapping) may be pulled forward, the
full sweep holds. NO REGISTER EDIT, no OI consumed, CACHE STAYS v25. Session 5
is authorised and remediates against these rulings, then stands up the schema
validator (charter R2). FILES: LC76_Audit_of_Truth_Charter.txt (issue 3),
LC76_Audit_of_Truth_Findings.txt, LC76_Library_Register.txt, plus this file.

PRIOR (30 July 2026, twenty-ninth session) (AUDIT OF TRUTH SESSION 3
EXECUTED — FIELDWORK ON THE REMAINING TWENTY OPEN ITEMS (21–40) PLUS
CLOSED-LOG SUFFICIENCY (A10) AT PINNED HEAD b13c5233: 4 SIGNIFICANT, 14
MODERATE, 2 HOUSEKEEPING; all 27 numbered closures tested; two charter §14
findings corrected on evidence; findings filed in the working papers, NO
item remediated, NO OI numbers consumed, CACHE STAYS v25; next free OI-68).

SESSION 3 IN ONE PARAGRAPH. HEAD had moved to b13c5233 since Session 2 — an
owner commit that hardened .gitignore (the insurance-enquiry .docx and the
private annex now excluded; handover loose-end 1 discharged) and renamed
gitignore.txt; NO governance or library content file changed, so the
Session-2 working papers and PI remain uncommitted and the local S1+S2 base
is correct to build on. Re-pinned to b13c5233, the twenty items 21–40 were
tested against A1–A10 at 100% coverage and the twenty-seven numbered closures
against A10. ITEMS 21–40: 4 SIGNIFICANT, 14 MODERATE, 2 HOUSEKEEPING, all
PROVISIONAL pending M5. The four SIGNIFICANT: OI-60 (the library-wide print
defect — SIGNIFICANT not for the backlog but because DG §16 currently MANDATES
a form that cannot work and files have been mis-certified §16-compliant
repeatedly, an active hazard); OI-12 (the Carnet 12-month interlock is a
feasibility constraint that, unresolved, invalidates the two-year no-return
structure); OI-11 (the register's worst atomicity breach — seven bundled
deliverables, two of which are already closed elsewhere as OI-35 and OI-36, so
the entry overstates what is open); OI-46 (crank-survival critical path — with
no spare alternator the jump pack is the only crank route after alternator
failure, and whether it can be field-recharged is unanswered). THE HEADLINE
CLOSED-LOG FINDING: charter §14 F-D asserts '7 of 27 closures cite no
evidence', a figure that is UNLISTED AND NOT REPRODUCIBLE. Under A10 as written
(fails only on pure narrative) 25 of 27 name a checkable artifact — a commit,
an HTTP-404 check, a provider reference, a testzip, a char/byte reconciliation
or a dated owner ruling — and only two are weak (OI-49, whose closing state is
a project-knowledge removal outside the repo; OI-40, whose provider source is
named by class not by locator). Under the strict §8 reading (a build closure
needs a named commit AND re-runnable validation, and 'validation clean' prose
is E5) only 15 of 27 are clean and about eleven rest on validation narrative
with no commit SHA. F-D's '7' matches neither bound; it is itself an E5 count
inside the charter — the exact defect the programme exists to remove. TWO
CHARTER SELF-CORRECTIONS, surfaced per §13 independence: F-C lists OI-39 as
'three jobs' when it is ONE decision offering three OPTIONS (A6 passes); and
F-D as above. COMPLETENESS (CC-10): the register overstates what is open —
OI-11 lists work closed elsewhere, OI-36's residual overlaps OI-14/EX-10, and
OI-29's proposed anti-staleness controls partly overlap the Audit of Truth
itself; 40 'open' items are not 40 distinct live obligations. STATUS VOCAB
(CC-9): several items are mis-labelled against the §10 controlled set —
OI-59/OI-36 are DONE-PENDING, OI-13/OI-14 are WAITING-DATE, OI-20/OI-26 are
STANDING. CONFIRMATION DATES (CC-8): 0 of 40 open items carry a status-
confirmation or next-due date, F-B now measured corpus-wide, the standing
items excepted. No finding rests on assertion — each cites an OIR line range
or the re-runnable closed-log census. EVIDENCE: AoT_S3_Findings (20-row
matrix), AoT_S3_ClosedLog (27-closure sufficiency matrix, both tiers),
AoT_S3_XCut (CC-8…CC-13 + the F-C/F-D corrections) appended to the working
papers; COUNTIF baseline 9 before and after, testzip None. The Session-2
probe error at close (a line-start regex missed OI-67 because closed-log
entries are date-prefixed) was disclosed and the matcher fixed. THE OIR WAS
NOT TOUCHED — zero items opened, closed or changed. FIELDWORK IS NOW COMPLETE
across all forty open items and the closed log; Session 4 is the findings
document and owner rulings, with NO edits before it. FILES THIS ROUND:
LC76_Library_Audit_WorkingPapers.xlsx plus this file.

PRIOR (30 July 2026, twenty-eighth session) (AUDIT OF TRUTH SESSION 2
EXECUTED — FIELDWORK ON THE FIRST TWENTY OPEN ITEMS AT PINNED HEAD cf4c4146:
all ten assertions A1–A10 tested per item, 100% coverage; 6 SIGNIFICANT, 13
MODERATE, 1 HOUSEKEEPING, all ratings PROVISIONAL pending M5; findings filed
in the working papers, NO item remediated, NO OI numbers consumed, CACHE
STAYS v25; next free OI-68).

SESSION 2 IN ONE PARAGRAPH. Fieldwork ran the ten assertions A1–A10 against
the first twenty open items at pinned HEAD cf4c4146 — 100% coverage, not a
sample (charter M1) — reusing the Session-1 population rather than
re-establishing it, and reading charter §7–§9 and §13 before opening a file.
RESULT: 6 SIGNIFICANT, 13 MODERATE, 1 HOUSEKEEPING, every rating PROVISIONAL
pending the M5 re-evaluation (R50,000, owner's working figure). THE TWO
FLAGGED A3 RECONCILIATIONS RESOLVED INTO SPECIFIC FINDINGS by testing all
three inputs, not the result (charter F-I): OI-62's coolant flush
'~86,006 km overdue' is arithmetically ELAPSED (310,412 − 224,406 exactly),
carries NO interval, and reads '~85,000 ago' in Facts §3 — the same event,
two numbers, two labels; OI-63's bearing repack '~63,684 km overdue'
recomputes only to a PHANTOM as-at of 308,684, subtracts no interval despite
the word 'overdue', and ignores the Feb-2025 @ 271,200 replacement (on which
the overrun is ~19,800, not 63,684 — an ~3× overstatement). Neither
underlying job is asserted unnecessary; the finding is that the QUANTUM is
untraceable and nobody can currently tell. A6 MEASURED THE 'FIVE ARE MORE
THAN ONE JOB': OI-61 (two independent — rear brake vs front knuckle), OI-62
(three), OI-4 (three), OI-24 (four, including Mary-Ann's MANDATORY card
renewal, which must be carved out), plus OI-66 (two, dependent/ordered). A7:
eight of twenty source-fields name a moment not a place, the
consultant-review-8-Jul class weakest; A8: zero of twenty carry a next-due
date (charter 0/40 confirmed on this batch). A GENUINE MISS SURFACED that
Session 1's trace did not — ADV-010 (boost pipe clamp) is OI-reffed to OI-7
(wheel-nut torque), a subject mismatch; S1 verified that refs were OPEN, not
that they were SUBJECT-CORRECT (CC-7). The mastership seam was OBSERVED but
NOT acted on, per the handover: Facts §3 is a parallel STATUS list living
inside the FIGURES master, carrying different numbers from the OIR (CC-6).
INDEPENDENCE (charter S3): all twenty entries were authored by Claude and
read harder; no finding rests on assertion — each cites a file-plus-locator
or the re-runnable A3 script, and the A3 numbers are E2, reproduced this
session against the ledger at the pinned HEAD. EVIDENCE: AoT_S2_Findings
(20-row assertion matrix), AoT_S2_A3_Recon (both reconciliations with
method), AoT_S2_XCut (CC-1…CC-7) appended to the working papers; COUNTIF
baseline 9 verified before and after, testzip None. Verifications banked in
passing: OI-21 belt (full Cambelt kit 2025-04-23 @ 273,023) and OI-6
radiator (2024-11-15 @ 264,347) reconcile to the ledger, and the previously
unsourced '308,500 km' water-pump reading is real (radiator check
2026-04-17 @ 308,500). THE OIR WAS NOT TOUCHED — zero items opened, closed or
changed; findings live in the working papers until the owner rules at
Session 4. FILES THIS ROUND: LC76_Library_Audit_WorkingPapers.xlsx plus this
file.

PRIOR (29 July 2026, twenty-seventh session) (AUDIT OF TRUTH SESSION 1
EXECUTED — POPULATION ESTABLISHED BY SWEEP AT PINNED HEAD c0ba1d19: 21
instruments adjudicated, three new live feeders found beyond the charter's
nine, five origination streams mapped to capture points, two-way trace
matrix built and filed in the working papers; NO item-level findings, NO OI
numbers consumed pending owner acceptance; CACHE STAYS v25; next free
OI-68).

SESSION 1 IN ONE PARAGRAPH. The population sweep ran the charter §6 test —
does anything here assert that something is outstanding, unverified,
monitored, pending or to be done — across every textual file in the corpus
tarball, keyword-first then adjudicated on context, because raw signal is
not status (Botswana's "outstanding game concentrations" is adjectival
English; a profile's GATE is a border post). THE SWEEP YIELDED THREE LIVE
FEEDERS THE CHARTER MISSED: Facts §3 (a parallel CRITICAL-OUTSTANDING /
ACTIVE-MONITORS list living inside the figures master — eleven bullets, a
standing tension with the mastership map), R37 (ten OUTSTANDING training
rows — the legitimate S4 capture home), and R31 (a DONE/PLANNED status
table with two live PLANNED rows). It also yielded ONE UNREGISTERED FILE:
an insurance-enquiry .docx in the public repo carrying initial-plus-surname
forms of both crew names — the class withdrawn to the annex on 28 Jul —
with ZERO mentions in the Library Register, PI, OIR, index or session
history; the master of what exists does not know it exists. OWNER RULING
REQUIRED on its disposition; per the acceptance filter it is proposed, not
registered. THE TRACE MATRIX: direction 1 clean — all 5 TA rows, all 14
open EX rows, all 12 advisories (100% OI-reffed, the healthiest feeder),
all 4 checklist items and both R31 rows trace to open register entries;
4 Concordance rows are WP-internal with no register anchor (the charter's
interim finding, now enumerated) and 1 electrical-stack row reads DIVERGENT
against the PI's RS-COMPLETE claim — candidate stale, held for fieldwork.
Direction 2: 29 of 40 entries carry feeder linkage; ELEVEN ARE REGISTER-
BORN with no capture instrument — the measured footprint of the S3
assistant-proposal gap the charter predicted. STREAMS: S1→Advisories LIVE
and working; S2→Service Checklist LIVE (the empty Schedule stub was found
DELETED between sessions — charter F-K actioned by the owner); S3→NO
CAPTURE POINT, confirmed; S4→R37 functioning, R3 describes rather than
registers; S5→instruments exist, sweep held for Session 7 by charter.
FROZEN-STATUS HAZARD: the 4 Jul EX Programme Handover carries twelve
PENDING statuses superseded by execution and also sits in project
knowledge — OI-49 staleness class, owner disposition proposed. FIVE PROBE
ERRORS THIS SESSION, ALL CAUGHT BEFORE REPORT: an unquoted-variable file
loop; a JSON probe on a dashboard whose data lives in data-text attributes;
an OIR slice truncated by 'CLOSED LOG' occurring in the file's own
preamble at line 7; an entry regex demanding a separator the shape pass
does not use; and an R37 row probe assuming tables where the file uses
badge spans. Suspect the probe first is now nine for nine across three
sessions. EVIDENCE: AoT_S1_Population (21-row census with adjudications)
and AoT_S1_Trace (full two-way matrix) appended to the working papers,
COUNTIF baseline 9 verified before and after, testzip None. FILES THIS
ROUND: LC76_Library_Audit_WorkingPapers.xlsx plus this file. The OIR was
NOT touched: zero items opened, closed or changed.

SAME-SESSION ADDENDUM — OWNER RULINGS ON THE FOUR SESSION-1 MATTERS
(29 Jul 2026). (1) INSURANCE-ENQUIRY .docx — REMOVE AND HOLD OFFLINE.
Owner-ruled removed from the public repo and held offline with the affidavit
print master. .gitignore updated so it cannot be re-committed. Forward-looking
only: git history retains the blob, the SAME accepted residual as the 28 Jul
RC1/name pass, and a history rewrite stays rejected because it breaks every
pinned SHA. The names it carried are the already-accepted initial-plus-surname
residual class; VIN and plate are public per the 28 Jul supersession. Not an
open item — a removed-and-done action; recorded here, evidenced by .gitignore.
(2) EX PROGRAMME HANDOVER frozen-status — OWNER RULED 29 Jul: remove the stale
copy from project knowledge, keep the dated GitHub archive (OI-49 class). The
removal from project knowledge is a manual step on the owner's side (the
knowledge base is not assistant-reachable). Durable caveat: the same 4 Jul file
can be re-introduced as a per-session upload, so it must stop being treated as
a live input, not merely deleted once. (3) STALE EX_Register rows
(RA-4 vs the 24 Jul ruling; RA-1's withdrawn deadline) — DEFERRED to Session 5
remediation by owner agreement; already captured as adjudications in
AoT_S1_Population; working-papers append-only discipline is the reason not to
correct evidence mid-audit. (4) MATERIALITY M5 SET — R50,000, PROVISIONAL
(owner's own word: a working figure to be evaluated properly later). Recorded
in the charter §4.3 M5 and its owner-time line; any Session 4 rating against it
carries the provisional caveat until re-evaluated. FILES ADDED THIS ADDENDUM:
LC76_Audit_of_Truth_Charter.txt (M5) and .gitignore, plus this file.

PRIOR (29 July 2026, twenty-sixth session) (AUDIT OF TRUTH SESSION 0
EXECUTED — THE MASTERSHIP MAP IS NOW VISIBLE TO THE OWNER: one block inserted
into LC76_How_To_Use_This_Library.html §04 The Registers; content-only, CACHE
STAYS v25; no OI numbers consumed; next free OI-68).

SESSION 0 IN ONE PARAGRAPH. Scope held deliberately to one section in one
file, per the handover and charter M4: a "Mastership Map" sub-block inserted
at the head of the guide's §04, before the per-register detail — a
plain-English framing paragraph, a seven-row instrument/master-of table, and
a closing paragraph telling the reader what to DO with the map (look up the
fact class, open its master, report the other file as the fault). Written to
the same standard as sections 03/06/08 — no shorthand, usable by Mary-Ann.
No audit fieldwork was started while the file was open. THE STANDING DATE
BUMPS RESOLVED TO NO-OPS, disclosed rather than churned: both the guide's
masthead and index.html's header-meta already carried 29 July 2026, the
session date, so re-writing identical values was skipped. Validation clean:
script-stripped depth walks 0/0/0 on twelve tag types, ids unique, TOC index
contract 9/9 in order under i===idx with every id resolving, node --check
clean on both inline scripts. FLAGGED, NOT FIXED (scope discipline): the
guide's §04 "A readable dashboard is planned" info block is now half-stale —
the dashboard EXISTS but is deliberately unwired pending the audit; rewording
that block belongs to Session 6, the session that wires it. FILES THIS
ROUND: LC76_How_To_Use_This_Library.html (+2,542 chars) plus this file. The
OIR was NOT re-dated: zero items opened, closed or changed — a header re-date
with no delta is the churn class this library avoids.

PRIOR (29 July 2026, twenty-fifth session) (THE REGISTER SHAPE PASS RAN
AND THEN THE SESSION TURNED INTO SOMETHING LARGER: AN ASSURANCE CHARTER OVER
THE OPEN ITEMS REGISTER — "AUDIT OF TRUTH" — APPROVED AS THE BASIS FOR A
SEVEN-SESSION PROGRAMME; THE DASHBOARD WAS BUILT AND THEN DELIBERATELY LEFT
UNWIRED; A MASTERSHIP MAP RULED; CACHE STAYS v25; next free OI-68).

WHAT HAPPENED, AND THE SEQUENCING ERROR THAT CAUSED IT. The owner asked for a
"register shape pass then dashboard". SHAPE WAS READ AS FORMATTING — line
positions, status on its own line — because that was the reading that let the
build start. The register did not need tidier layout. It needed its ENTRIES
checked: is this one job or three, is the source real, does the status mean
anything. The dashboard was built on unchecked entries, and every defect
underneath surfaced as a complaint about the page. GENERAL LESSON: WHEN AN
INSTRUCTION HAS A CHEAP READING AND AN EXPENSIVE ONE, THE CHEAP READING IS
SUSPECT — especially when it is the one that lets work begin immediately.

THE OWNER'S FIVE TRANSACTION ASSERTIONS FOUND A HOLE IN THE METHODOLOGY IN ONE
MESSAGE. Issue 1 of the charter carried seven assertions: COMPLETENESS was
covered, OCCURRENCE obliquely, CLASSIFICATION only as a remediation construct.
ACCURACY AND CUT-OFF WERE ABSENT ALTOGETHER — in a register whose entries turn
on mileages, intervals and dates. The audit would have certified entries whose
figures nobody had tested, and looked rigorous doing it. Ten assertions now.

AND THE ACCURACY ASSERTION IMMEDIATELY EARNED ITS PLACE. Tested against the
service ledger: OI-62 states the coolant flush is ~86,006 km overdue, while the
ledger carries coolant work 2,752 km ago; OI-63 states the bearing repack is
~63,684 km overdue while the replacement it cites was 39,800 km ago. NEITHER IS
ASSERTED WRONG — a top-up is not a flush, an interval may run from an earlier
event. The finding is that NOBODY CAN CURRENTLY TELL, and both figures justify
workshop spend.

THE ITEM LIFECYCLE IS THE OWNER'S CONTRIBUTION AND IT RESTRUCTURED THE CHARTER.
Issue 1 audited the balance — the 40 items as they stand — and never asked how
an item is BORN. Completeness cannot be established by tracing between files: if
something never enters the system, two instruments agree perfectly and both are
wrong. Five origination streams are now defined, with their capture points; the
fifth — SYSTEMIC GENERATION — is the one that produces items nobody thought to
raise, and eight generators are chartered at charter Appendix A.

MASTERSHIP RULED (see WORKING CONVENTIONS). Three instruments were each behaving
as master, which is why the working papers still carry RA-4 as owner-gated five
days after the ruling that closed it.

THE REGISTER STAYS A .txt AND THAT WAS THE RIGHT CALL. A spreadsheet master was
proposed and rejected on the owner's reasoning: a .xlsx is a binary blob, cannot
be diffed, and this whole working method rests on SHA-pinning and reading what
changed. The field-discipline objection died the same day — a parser read all 40
entries into structured data with full fidelity. A TEXT FILE WITH AN ENFORCED
SCHEMA IS A DATABASE, and it is also readable. THE CONDITION IS NOT OPTIONAL: a
schema is only real if something rejects violations. A shape convention was
written in the morning and five entries still had to be repaired in the
afternoon.

THE DASHBOARD EXISTS AND IS DELIBERATELY UNWIRED (owner ruling). It is on GitHub,
reachable by direct URL, NOT linked from index.html, NOT precached, NOT in sw.js
— because it displays a register whose entries have not been audited, and wiring
it would present unverified data as the library's answer to "what is open".
Wiring is a Session 6 action and carries the CACHE bump v25 → v26.

FOUR ERRORS THIS SESSION, ALL DISCLOSED. (1) The sequencing error above.
(2) A closed-log count of 60 written into the register before checking — the
real figure is 61, one entry starting with a bracket that the first probe
skipped. (3) Detail text rendered with a hard line break at every register line,
producing a narrow ragged column; the owner reported it as a layout fault and it
was a data-handling fault. (4) THE INSTRUCTIVE ONE: the charter asserted that
the manual-prescribed origination stream had no capture point, on the evidence
of an empty Schedule sheet in the service workbook. IT WAS WRONG — LC76_Service_
Checklist.html carries 66 items, 64 with intervals, every one provenance-tagged.
The error was made by asserting a gap from ONE instrument without establishing
the population: the exact failure the charter exists to address, committed inside
the charter itself. The empty sheet is now a housekeeping finding (F-K).

FILES THIS ROUND: 3 pushed — LC76_Open_Items_Register.txt (shape pass, meta-line
repairs, 40 plain-language WHAT lines), LC76_Open_Items_Dashboard.html (prototype,
unwired), LC76_Audit_of_Truth_Charter.txt (new governance file) — plus this file
and LC76_Library_Register.txt. Dashboard validation clean: depth walks 0/0/0 on
nine tag types, ids unique, TOC contract 5/5 in order, node --check clean, 40
items rendered, no browser storage. NO LIBRARY FILE WAS ADDED TO THE PRECACHE —
CACHE STAYS v25.

PRIOR (29 July 2026, twenty-fourth session) (THE LIBRARY'S FIRST USER
GUIDE BUILT — LC76_How_To_Use_This_Library.html, INFRASTRUCTURE CLASS, NINE
SECTIONS; A LIBRARY-WIDE PRINT DEFECT FOUND BY DIAGNOSTIC — THE PRINT STANDARD
HAS NEVER TAKEN EFFECT IN ANY OF 71 FILES; CACHE v24 → v25; OI-58 OPENED &
CLOSED, OI-59, OI-60, OI-61–66 OPENED, OI-67 OPENED & CLOSED; next free OI-68).

THE OWNER CAUGHT A REAL GAP AND IT IS THE MOST IMPORTANT FINDING OF THE DAY.
The OI-59 reconciliation gate checked the working papers and the three audit
reports and pronounced the register substantially complete. IT NEVER OPENED R8.
R8 §2 carries a MONITOR block flagged by the workshop at the Apr 2026 service
(308,248 km) and a curated "Critical Outstanding" section — and NONE of it had
ever reached the Open Items Register. Two of those items are safety-relevant: a
rear hub seal weeping onto the shoes of the primary parking brake, and front
knuckle grease escaping from birfields that fail quickly once dry. Also
untracked: no thermostat on record in 316 entries, no water pump replacement on
record, a coolant flush ~86,006 km overdue, a wheel bearing repack ~63,684 km
overdue, and diff breather extensions nobody has looked for.
GENERAL LESSON: A COMPLETENESS GATE IS ONLY AS WIDE AS ITS POPULATION. The gate
was scoped to the instruments that LOOK like registers — spreadsheets and audit
reports — and missed a reference DOCUMENT that had quietly become one. R8 is a
dashboard; dashboards accumulate status. Any future reconciliation must sweep
for status-bearing content wherever it lives, not only where a register is
expected. This is the same failure shape as the philosophy-batch flag written
from recollection: the population was assumed rather than established.

WORKSHOP ADVISORIES NOW HAVE A HOME. They had none — the service ledger records
work PERFORMED, and its Notes column has never carried "the workshop said watch
X". A new ADVISORIES sheet is added to LC_Service_history_FINAL.xlsx: ID, date,
mileage, source, item, detail, severity, action, status, OI ref. Ten rows seeded
from R8, one already closed. THE ROWS WERE DELIBERATELY NOT ADDED TO THE
SERVICES SHEET — under the COST-0 convention a zero-cost row means "child line
item of the invoice above", so advisories there would corrupt the cost ledger
and both derived HTML ledgers.
STANDING CONVENTION ADOPTED: every service event asks what the workshop flagged,
and anything flagged is written to the Advisories sheet and, if it needs action,
carried as an OI. R8 remains the OWNING document for the detail; the register
holds a pointer.

AND THE SAME OWNER CATCH EXTENDED TO THE R8 SUPPLEMENT, WHICH IS WORSE. The
Service Checklist carries `var OUTSTANDING` — six items presented as live
workshop actions — HARDCODED WITH NO SOURCE FIELD, while the ALL_ITEMS array in
the SAME FILE carries src:'FSM'/'DOC'/'SH'/'BOTH' on every entry. The convention
existed and this array did not use it. Untraceable, therefore unmaintained,
therefore drifted: head studs still flagged for assessment after Udel ruled them
NOT REQUIRED in Jun 2026, and a boost gauge still flagged "verify fitted or
install" after the owner confirmed it fitted on 8 Jul 2026. This document has a
print-and-take-to-the-workshop function, so a stale CRITICAL flag spends money
re-doing settled work. OI-67.
THE THREE LEGITIMATE ENTRIES WERE TRACKED NOWHERE — rocker cover gasket, PCV
routing, Racor pre-filter. The first two are now OI-66, deliberately as ONE item
because the ORDER matters: verify the crankcase breather routing BEFORE
resealing the head. If the breather is routed post-intercooler, boost
pressurises the crankcase and forces oil past every top-end seal — reseal first
and the new gasket fails the same way.
GENERAL LESSON, PROMOTED TO A WORKING CONVENTION BELOW: A LIST WITHOUT
PROVENANCE CANNOT BE MAINTAINED. Every status-bearing array in this library
carries a source field or it becomes a liability with a print button.
ADVISORIES SHEET IS LIVE-ONLY (owner ruling, 29 Jul 2026): a closed advisory is
REMOVED, not retained — ADV-005 (fan belt quality) deleted on that basis, the
belts having been replaced since. IDs are not reused, so a gap is deliberate.
The durable lesson from a removed row lives in its owning document, not here.

WHY THIS SESSION. The owner opened it as a business question — how the library
might be monetised — and it turned into a usability finding, which is the more
valuable of the two. Working through what a productised version would contain,
he could not answer from his own library what the abbreviations meant, how to
use the Open Items Register, or whether the Controlled Duplication Register
existed. It does. It sits between Section E and the closed log. THE OWNER OF A
FOUR-YEAR SYSTEM COULD NOT NAVIGATE IT, AND THAT IS A DEFECT IN THE SYSTEM, NOT
IN HIM.

ROOT CAUSE, AND IT GENERALISES. The governance layer was built WRITE-OPTIMISED
FOR THE ASSISTANT, NOT READ-OPTIMISED FOR THE OWNER. Every instrument is a plain
.txt outside the PWA — unlinked from index.html, not precached, not searchable,
not printable. An assistant reads 108 KB instantly and joins three files in
working memory; the owner cannot, and should not have to. He has been the
proprietor of this system but never its operator. Confirmed against the Library
Register that no user guide existed: R30 answers "which document covers X",
index.html answers "I have a problem, where do I start", and nothing answered
"what is this machinery and how do I drive it".

A ONE-FACT-THREE-OWNERS VIOLATION, INSIDE THE LAYER THAT ENFORCES THE RULE. The
Readiness Actions live in three places: their descriptions in this file, their
status in the register, their tracking row in the working papers. Answering
"what is RA-5 and is it done?" requires two files and a mental join. Logged as
input to the dashboard work rather than fixed here.

THE PRINT FINDING IS THE LARGER ONE, AND IT CAME FROM A COMPLAINT ABOUT FONT
SIZE. Every font size in this library is declared in rem, which resolves against
the ROOT element. Every @media print block sets body. BODY IS NOT ROOT, so for
rem-sized text the print rule does nothing whatever, and never has. Depth-aware
diagnostic across all 71 HTML files at pinned HEAD: ZERO set the root size in
print; 1,547 rem declarations sit in base CSS. Printed pages have been coming
out at screen size since the print standard was written. Detail and remedy:
OI-60. GENERAL LESSON: A STANDARD THAT WAS NEVER MEASURED IS A HYPOTHESIS. DG
§16 has been cited as satisfied in session after session on the strength of the
rule being PRESENT in the file, which is not the same as it being IN EFFECT.

THE REMEDY IS A KNOB, NOT A TABLE OF SIZES. One root declaration inside @media
print, plus a shared block of rem heading overrides that COMPRESS the hierarchy
— on screen a heading earns its size, on paper it need only be distinguishable.
Because the overrides are rem rather than pt they do not fight the root, so a
single value rescales the whole document with relationships intact. Prototyped
on the guide: 11.6px → 8pt body, 13.0 → 9pt, 14.5 → 10pt, 17.4 → 12pt.

AND THE FIRST SIZE RULING WAS WRONG BECAUSE THE DIAGNOSIS WAS MIS-STATED. Claude
reported the body text as the cause of long printouts; the owner reasonably
ruled 12pt, printed it, and found it worse. Body prose was already printing at
11pt — the length was coming from 20.4pt section titles, a 33.6pt masthead and
1.65 line-height. THE MEASUREMENT WAS AVAILABLE BEFORE THE RULING WAS SOUGHT AND
WAS NOT TAKEN. Same class as the 29 Jul masthead-date error: an option offered is
already a claim, and an owner ruling on a defective option set produces a
defective ruling and costs a round trip. 8pt adopted on re-ruling.

SECTIONS 03, 06 AND 08 OF THE GUIDE ARE WRITTEN FOR MARY-ANN. This is an RA-8
input, not a courtesy. Solo capability against a sixty-document library with five
register categories and twelve abbreviation families requires an entry point
assuming no prior knowledge; the library had been written throughout on the
assumption that its reader was the owner or the assistant.

TWO OWNER GATES RULED. Classification: infrastructure class, no R-number,
masthead-EXEMPT under DG S10 — but carrying a Last-updated line by DELIBERATE
DIVERGENCE, because unlike the Design Guidelines this document goes stale
whenever the library's shape changes. Recorded here and in the Library Register
so a later session does not "correct" it. Placement: first card in index.html §2
Find Anything, ahead of R30 — crisis-first is a stress-state principle and a
manual does not outrank Emergency.

FOUR PROBE ERRORS, ALL CAUGHT. The instructive one searched for this file's
PHRASING of the duplication register rather than the register's actual table
FORMAT, returned zero hits, and nearly reported that the register did not exist.
Also: a "max-width:1" guard firing on the correct "max-width:100%"; a status
extractor reading four entries as blank because they use varied vocabulary
("DORMANT by design", "SUBSTANTIALLY CLOSED") rather than a keyword; and an
over-broad print classifier whose "OK" category dissolved on spot-checking
against computed sizes. SUSPECT THE PROBE FIRST, four for four.

FILES THIS ROUND: 6 — LC76_How_To_Use_This_Library.html (new), index.html,
sw.js, LC76_Library_Register.txt, LC76_Open_Items_Register.txt, plus this file
and LC76_Session_History.txt. Validation clean — script-stripped depth walks
0/0/0 on seventeen tag types, node --check clean, ids unique, TOC index contract
9/9 in order under i===idx, design conformance swept. CACHE BUMPED v24 → v25 —
a file was ADDED, which is the trigger.

PRIOR (29 July 2026, twenty-third session) (S10 PHILOSOPHY BATCH
COMPLETED — THE FOUR PHILOSOPHY DOCUMENTS MIGRATED TO THE SIX-SLOT MASTHEAD;
DESIGN GUIDELINES v1.9 WITH A PHILOSOPHY FAMILY ALLOWANCE AND THE COLLECTION
VOCABULARY CORRECTED THREE → FOUR; THE CHANGELOG RULE GAINS A NON-PROFILE
R-DOC EXEMPTION WITH A NAMED EVIDENCE INSTRUMENT; ONE OWNER RULING REVERSED
ON EVIDENCE; THE HEADER ARCHIVED BACK TO CURRENT + TWO WITH SIX DOCTRINES
PROMOTED FIRST; OI-55 & OI-56 OPENED & CLOSED, OI-57 OPENED; next free OI-58).

WHY THIS SESSION. Three items were carried out of the twenty-second session
as flags, not fixes, and the owner ruled on all three. Two were executed here;
the third — archiving the PI header back to the current + two convention — is
a separate governance-only session so this diff stays reviewable (the Gate D
reasoning from the nineteenth session).

THE FLAG WAS WRONG IN BOTH DIRECTIONS, AND MEASUREMENT SETTLED IT. It named
five files lacking a masthead date. The Content Audit among them is EXEMPT —
S10 lists audit HTML under governance/infrastructure — and a second exempt
audit file it had not named also lacks the line. The real population is FOUR
philosophy documents. A flag written from recollection rather than a sweep
mis-stated a population in both directions at once.

ROOT CAUSE: AN UNFINISHED MIGRATION, NOT A DEFECT. S10's Migration status
block groups "philosophy + itinerary" as ONE family batch. The itinerary half
ran 28 Jul (Karoo); the philosophy half never did. The files were UNMIGRATED,
a state the standard explicitly anticipates and tolerates — which reframes the
work from "add four missing lines" to "finish the batch", and changed its
scope from one slot to three.

SCOPE WAS THREE SLOTS. Beyond the absent date, all four carried a non-standard
eyebrow and had substituted editorial prose for the invariant identity line.
A date-only pass would have shipped two live divergences under a conformance
claim.

A RULING WAS REVERSED ON EVIDENCE, AND THE ERROR WAS MINE. The owner first
ruled that each file carry its true last-CONTENT-edit date rather than the
session date — on my proposal. Reading S10 at HEAD then showed the standard
ALREADY prescribes the session date for migrated files: I had offered an open
sub-gate on a question the standard had settled, having proposed before
verifying, which inverts working convention (c). Independently the ruling was
unexecutable: commit messages in this repo are bare ("commit", "comit") and
the most recent commit on every one of these files is the 29 Jul CSS-only TOC
pass, so a "true content date" could only have come from diff-adjudication
across five-plus revisions — a number asserted rather than read, F65 class.
GENERAL LESSON: AN OPTION OFFERED IS ALREADY A CLAIM. Convention (c) says
verify a finding before writing it down; it applies with equal force to the
OPTIONS in a proposal, because an owner ruling on a defective option set
produces a defective ruling and costs a round trip to unwind.

THE EDITORIAL LINE WAS KEPT, AND THE PLACEMENT DEVIATES FROM THE PRECEDENT
IT CITES. The four glosses ("written to be read at the fire") are good
writing and S10's own rationale — the masthead identifies the DOCUMENT, it
does not re-specify the vehicle — cuts against stamping a turbo spec onto a
manifesto. They are retained as a Philosophy family extra element on the
profile native-name precedent. But profiles place that element directly under
the TITLE, and porting that here was measured and rejected: the profile
.std-sub is a 0.92rem sans italic while the philosophy .std-sub is a 0.54rem
mono amber line — typographically the SAME instrument as the profile's
.std-local — so the profile placement stacks two near-identical lines. The
gloss therefore stays below the subtitle, and S10 records the reason so the
divergence is not later "corrected" into the defect.

FOUR THINGS FLAGGED, NOT FIXED. (1) index.html header-meta stood at 28 July
while the twenty-second session edited 39 library files — a live miss against
the standing bump rule, repaired in passing here. (2) The OIR closed log
carries a formatting corruption at the 22 Jul OI-37 entry: a horizontal rule
run together with the entry text on one line. Pre-existing, exactly the class
the roll-forward convention exists to catch, left for an owner ruling rather
than fixed silently. [FIXED later the same day on owner instruction — see
the closing addendum.] (3) LC76_History_of_Overlanding.html is the only one of
the four carrying a doc-footer, against the 8 Jul footer standardisation which
gives philosophy documents none. [FIXED later the same day on owner
instruction — see the closing addendum.] (4) That same file alone has no .std-*
print-colour overrides; its print edit was skipped by design to match its own
pattern rather than imposing the family's.

FILES THIS ROUND: 8 — the four philosophy HTML, LC76_Design_Guidelines.html
(v1.8 → v1.9), index.html, LC76_Library_Register.txt, LC76_Open_Items_
Register.txt, plus this file. Validation clean throughout — script-stripped
depth walks 0/0/0 on div/span/header/nav/section/table/tbody/tr/dl/pre, ids
unique, node --check clean, per-file asserts that the identity line and the
date each resolve exactly once, and a corpus sweep confirming zero .std-header
files now lack the date line. Content-only — CACHE STAYS v24.

SAME-SESSION ADDENDUM — HEADER ARCHIVED TO CURRENT + TWO (OI-56, opened and
closed same day; OI-57 opened). The third flag from the twenty-second session
was ruled and executed as a governance-only pass, deliberately separate from
the philosophy work above so each diff stays reviewable.

SCOPE GREW BY ONE, DISCLOSED. The ruling named three blocks when the current
session was the twenty-second; the block written earlier today made it FOUR —
seventeenth through twentieth — to reach current + two. That follows the
ruling's stated goal rather than extending it.

THE SWEEP RAN BEFORE THE MOVE AND IT PAID FOR ITSELF TWICE. Six narrative-only
live rules were promoted into WORKING CONVENTIONS first: archives are not
reconstructed (with the dated-redaction-notice and labelled-gap corollaries);
precedence between this file and the history file; the Class P cover/medical
RETAIN ruling; the hard-404 working-method blocker; and three-layer triage for
"the site looks old". Two probe readings claimed two of these were already in
the body — the hits were an unrelated renumbering convention, a short-dial
labelling rule and an HTTP-404 verification note. Both were genuinely absent.
Suspect the probe.

AND IT FOUND AN UNLOGGED DEFECT. The nineteenth session diagnosed the
Pages/PWA layer question and RESERVED an open-item number for it; that number
was consumed by the RC1 pass the next day and no replacement was ever
assigned. The finding therefore existed only inside a session narrative that
was about to be archived. It is now OI-57, OPEN. GENERAL LESSON: A RESERVED
NUMBER IS NOT A LOGGED ITEM — the same failure as the OI-46 collision seen
from the other side. A finding worth an item number is logged in the register
in the session that finds it, or it does not exist.

RECONCILIATION: move set 27,229 chars across four blocks, asserted verbatim in
the destination and absent from the source; kept blocks asserted present here
and absent there; character accounting closed (this file −23,162 = −27,229
moved +4,067 declared; history +28,160 = 27,229 moved +931 provenance);
START/END delimiters one each; PRIOR-block count 6 → 2; history file 28 → 32
blocks. Files: this one, LC76_Session_History.txt, LC76_Open_Items_Register.txt.
Governance-only — no library file touched, CACHE STAYS v24.

CLOSING ADDENDUM — THE TWO REMAINING FLAGS FIXED ON OWNER INSTRUCTION. Both
were defects against an existing standard rather than decisions, so neither
consumed an open-item number; the evidence lives here and in the registers,
per the changelog exemption adopted earlier today.

(a) OIR CLOSED-LOG CORRUPTION REPAIRED. A stray 31-character horizontal rule
had been run together with the opening line of the 22 Jul OI-37 entry. The
rule was spurious, not a displaced divider — the closed log separates entries
by blank line and carries no rules of its own — so it was removed and no entry
text was touched. THE REPAIR EXPOSED A MEASUREMENT DEFECT WORTH RECORDING: the
closed-log entry count rose from 24 to 25, because while the line was corrupt
the OI-37 entry did not match the entry probe at all. A malformed record is
not merely untidy; it is INVISIBLE to the very gates meant to audit it, and
every entry count this file has reported since 22 Jul was low by one.

(b) HISTORY OF OVERLANDING DOC-FOOTER REMOVED. It was the only one of the four
philosophy documents carrying one, against the 8 Jul footer standardisation
which gives philosophy documents none. The markup and BOTH dead .doc-footer
CSS rules (base and print) were removed, following the 8 Jul precedent of
cleaning the rules rather than orphaning them. The masthead date already stood
at the session date and was not re-bumped. Content-only — CACHE STAYS v24.

A THIRD MALFORMED PROBE THIS SESSION, DISCLOSED. The guard asserting the stray
rule was gone counted a 31-character run of the box character — which also
occurs inside this file's legitimate 70-character header rules, so it fired on
a correct file. Rewritten to test the corrupt PATTERN (a rule abutting an entry
date) rather than the run. Three probe errors today, all caught before being
reported as file defects; the standing instruction to suspect the probe first
is earning its place.

THE TWO FLAGS THAT REMAIN ARE DELIBERATE, NOT OUTSTANDING: index.html was
repaired in passing earlier today, and the absence of .std-* print-colour
overrides in History of Overlanding is that file's own pattern, left alone
rather than having the family's imposed on it.

PRIOR (29 July 2026, twenty-second session) (LIBRARY-WIDE TOC WRAP
DEFECT CLOSED — THE SINGLE-ROW SCROLLER IS NOW A BASE RULE IN 33 FILES AND
MANDATORY BY DESIGN GUIDELINES v1.8; 39 FILES EDITED ACROSS FOUR PASSES;
A DEPTH-AWARE RE-CLASSIFICATION DISSOLVED AN ENTIRE CATEGORY AND PREVENTED
13 UNNECESSARY EDITS; OI-54 OPENED & CLOSED; next free OI-55).

WHY THIS SESSION. The owner opened it with a UI complaint, not a governance
task: the Karoo itinerary's TOC wrapped to three rows on a laptop while
behaving correctly on mobile. Chasing it produced a LIBRARY-WIDE defect and a
Design Guidelines change. The first diagnosis — "my regression, I added two
buttons" — was WRONG and was corrected: the two extra buttons pushed one file
over a threshold, but the wrap was structural and pre-existing in 29 files.

ROOT CAUSE, ONE LINE. The single-row scroller was scoped to a max-width media
query, so desktop fell through to a base rule declaring flex-wrap:wrap. The
TOC sits inside the mandatory sticky unit, so every wrapped row permanently
consumed viewport on every screen of the document.

THE FIX WAS ALREADY IN THE LIBRARY. Four files (R4, R8, R19, Seal Gasket
Atlas) carried the scroller in the BASE rule. R4, with 75 TOC buttons and a
~15,400px row, works precisely because of it. The remedy was therefore
ADOPTION of a proven in-house pattern, and the one thing Claude invented — a
slim @media (pointer:fine) scrollbar for mouse users — was offered, REJECTED
by the owner in favour of consistency, and reverted out of the Karoo file
before the programme started. Do not reintroduce it.

FOUR PASSES, 39 FILES. S1 Profile TEMPLATE + Design Guidelines — template
FIRST, so no future profile inherits the defect (the OI-36 lesson), and the
standard written into DG S6 with the document bumped v1.7 → v1.8. S2 ten
country/terrain profiles, with changelog entries in BOTH formats per the
OI-39 divergence. S3 six remaining overflowing files. S4 ten latent ones.
Closing corpus assert passes on all 61 files: none wraps, none lacks a scroll
mechanism. 33 carry the standard explicitly; 28 are correct by CSS default
and were LEFT UNTOUCHED — editing working files for cosmetic uniformity is
churn, and every edit is a chance to break something.

FOUR PROBE ERRORS, ALL CAUGHT, AND THE FOURTH IS THE INSTRUCTIVE ONE. (1) The
first survey matched only <nav class="toc"> and reported ONE affected file;
most of the library uses .toc-bar. (2) The idempotence guard fired on Angola
for overscroll-behavior-x:contain — which also sits on .table-scroll under
the standing inner-scroller pattern. (3) The changelog anchor assumed a
wrapper style that varies between profiles. (4) THE CATEGORY CLASSIFIER TOOK
THE FIRST .toc-bar MATCH IN THE FLATTENED CSS, but in many files that match
is inside a media query — Brake System Guide's first match is a mobile
padding rule, Vehicle Recovery's is the print rule. Rebuilt with DEPTH-AWARE
parsing (a rule is BASE only when braces balance to zero before it), which
DISSOLVED an entire category: the 13 files classed "no scroll mechanism" were
already correct, and S4 fell from 23 files to 10. Thirteen healthy files
would have been edited for nothing. GENERAL RULE: a CSS probe must respect
NESTING, not text order — flattening the stylesheet destroys the very scoping
that decides which rule applies.

THE ANCHOR THAT WOULD HAVE DONE REAL DAMAGE. flex-wrap:wrap appears 2–8 times
per file, almost always on OTHER components — .xref-bar, .comp-header,
.ohm-tri, .cover-specs, .footer, .sec-header. Spares & Tools alone has eight.
A replace on that string would have silently unwrapped half a dozen unrelated
layouts per file. The safe anchor was ';flex-wrap:wrap;border-bottom'
(count==1 everywhere), and validation asserts the OTHER wrap rules survive.

FLAGGED, NOT FIXED. Five files carry no masthead "Last updated" line — the
Manifesto, the Commentary, History of Overlanding and Expedition as a System
(all Philosophy, which IS in the DG S10 closed vocabulary) plus the Content
Audit (governance, legitimately exempt). Same class as the Karoo masthead
ruled on 28 Jul; not fixed silently. Separately, non-profile R-docs have no
changelog section, so the "every masthead bump carries a changelog entry"
rule cannot be honoured for them — it needs an exemption or changelogs.

FILES THIS ROUND: 39 HTML + this file, the OIR and the Library Register.
Validation on every edited file — depth walks 0/0/0 (div, span, section,
nav, table, tbody, tr, dl, pre), ids unique, node --check clean, TOC index
contract 1:1 in order on all ten profiles, and a per-file assert that the
base rule carries all five declarations with no flex-wrap:wrap surviving.
Content and CSS only — CACHE STAYS v24.

DATE BOUNDARY NOTE: this session began 28 Jul and ran past midnight. The
Karoo restructure and the date-exposure passes are stamped 28 July, the day
that work was done; the TOC programme is stamped 29 July.

SESSION HISTORY — ARCHIVED SESSIONS (full record: LC76_Session_History.txt)
Blocks below the twenty-second session were moved to the history file — those
below the seventeenth on 27 Jul 2026, the seventeenth to twentieth earlier on
29 Jul 2026, the twenty-first later the same day. One line each, newest-first;
open the history file for detail.

  · 28 Jul 2026 · twenty-first— Karoo restructure; RA-1 de-dating completed
  · 28 Jul 2026 · twentieth  — RC1 values & full legal names withdrawn to annex
  · 27 Jul 2026 · nineteenth — PI restructure; Session History split out
  · 26 Jul 2026 · eighteenth — LiFePO4 multi-day block; 77 → 65.3 Ah/day
  · 25 Jul 2026 · seventeenth— Emergency-number personal-cover set; Netcare demoted
  · 24 Jul 2026 · sixteenth  — RA-4 ruled: no spare alternator carried
  · 24 Jul 2026 · fifteenth  — Emergency doctrine & insurance restructure; OIR re-file
  · 24 Jul 2026 · fourteenth — 23-Jul handover discharged; R29/R10 emergency numbers
  · 23 Jul 2026 · THIRTEENTH — NO NARRATIVE RECORD (labelled gap; see history file)
  · 22 Jul 2026 · twelfth    — OI-36/OI-37 built: profile §06 + freshness markers
  · 22 Jul 2026 · eleventh   — Decision session: RA-1 undated + four ratifications
  · 21 Jul 2026 · tenth      — Content Audit Session E; programme closed
  · 21 Jul 2026 · ninth      — Content Audit Session D (electrical & service)
  · 21 Jul 2026 · eighth     — R23 §7 post-trip protocol + Content Audit Session C
  · 21 Jul 2026 · seventh    — Content Audit Session B (expedition-gating)
  · 21 Jul 2026 · sixth      — Content Audit Session A (sweep + Tier 1)
  · 21 Jul 2026 · fifth      — R42 Fire Safety Guide expanded
  · 21 Jul 2026 · fourth     — index.html situation-based restructure
  · 21 Jul 2026 · third      — Scam coverage expansion (R35 §8)
  · 21 Jul 2026 · second     — R35/R42 fire split executed
  · 21 Jul 2026 · first      — Scam register added (R35 §12)
  · 20 Jul 2026 · second     — Owner's authority affidavit built (R39 supplement)
  · 20 Jul 2026 · first      — No-trace camp kit standing doctrine
  · 17 Jul 2026 ·            — Licence dates corrected (Mary-Ann mandatory renewal)
  · 13 Jul 2026 · third      — Experimental route working method adopted
  · 13 Jul 2026 · second     — EX-9 seasonal/licence pre-work; Option A adopted
  · 12 Jul 2026 · second     — Systems-thinking: R41 built + R38 §6
  · 12 Jul 2026 · first      — Reconciliation & small-fixes; OIR created
  · 11 Jul 2026 ·            — Mobile pattern v1.7 propagation, Session 2
  · 11 Jul 2026 ·            — Mobile pattern v1.7 propagation, Session 1
  · 11 Jul 2026 ·            — R29 mobile sway fix + Design Guidelines v1.7
  · 11 Jul 2026 ·            — R29 programme complete (M1–M5)
  · 11 Jul 2026 ·            — R29 evaluation + M1/M3 + readability pass
  · 11 Jul 2026 ·            — R29 expert evaluation & remediation

Maintained by: Neil / Mary-Ann
═══════════════════════════════════════════════════════════════════════


▼▼▼ START — PROJECT INSTRUCTIONS ▼▼▼

This project builds and maintains the LC76 Overland Reference Library for Neil
& Mary-Ann's 2009 Toyota Land Cruiser 76 Series SW (HZJ76R-RKMRS), based in
South Africa. The vehicle is used for serious overlanding across Southern and
East Africa, with a planned two-year, 60,000+ km expedition in 2028–2030 (distance
figure confirmed authoritative 9 Jul 2026 — supersedes the ~40,000 km working
figure). En-route service policy (owner, 9 Jul 2026): follow the service
schedule at Toyota dealers where the route allows; oil changes are never
deferred — every en-route oil change is also an RA-2 oil-analysis sampling point.

EXPEDITION ROUTE (working position, 13 Jul 2026): OPTION A adopted PENDING
EX-9 ratification — a two-year continuous arc, Namibia-first opening leg,
eastern corridor (Mozambique coast last) as the southbound return; no SA
return. Ride the rising dry season south-to-north, coastal return ahead of
the November rains. Seasonal anchors: Zambezi/Zambia Aug–Oct; N. Tanzania/
Kenya Jun–Oct or Ndutu calving Jan–Feb; Moz coast May–Oct (cyclone-safe).
The route is developed as an EXPERIMENTAL DRAFT outside the library until
EX-9 ratifies it (see WORKING CONVENTIONS + OI-26). Top open constraint:
the Carnet 12-month interlock on a no-return two-year trip (M4, EX-9).
NEW EX-9 INPUT (17 Jul 2026): licence-card dates pull on departure timing
in opposite directions — earlier departure worsens Mary-Ann's mid-trip
card expiry (mid-2029), later departure tightens Neil's post-return
margin (card expires early 2031).

Library hosted at: overlandslabs.github.io/Consumption
Repo: github.com/Overlandslabs/Consumption

INDEX LAYOUT (restructured 21 Jul 2026 — situation-based): index.html is
organised by reader situation, urgency-descending, EIGHT sections:
1 Emergency & Safety · 2 Find Anything (R30) · 3 Field Tools (R1 +
calculators) · 4 Vehicle (four h3 sub-groups: Engine & Mechanical /
Drivetrain & Tyres / Battery & Electrical / Spares & Service Record) ·
5 The Expedition · 6 Country Profiles & Trips · 7 Philosophy & Ethos ·
8 Governance. Principles: crisis content first (stress-state design);
R30 owns find-by-topic, the index owns browse-by-situation; governance
de-emphasised last. New cards are placed by SITUATION, not by build
order — Emergency section order is by worst-day priority (medical →
vehicle-dead → stuck → fire → security → wildlife → health). SECTION
COLOUR SYSTEM (21 Jul 2026): colour follows section — red full card
headers RESERVED for Emergency & Safety (all 7 cards); every other
section carries its --sec-* token as a 4px card top-border + card-code
eyebrow + h2 underline only (tokens in the DESIGN SYSTEM section). New
cards inherit colour from their section grid automatically. The 13 Jul
"R30 immediately after Philosophy" placement is superseded: R30 stays
SECOND on the page and in the TOC, now after Emergency & Safety.

VEHICLE
- 1HZ 4.2L inline-6 diesel, SAC low-boost turbo (reconned Aug 2023 @ 224,406
  km; boost confirmed 0.6 bar Jun 2026), intercooler (verified good, Udel
  Jun 2026). 311,000 km current odometer (read Jul 2026 at LiFePO4 install).
- EGT (pyrometer) and boost instrumentation FITTED (confirmed by owner,
  8 Jul 2026).
- Timing belt last replaced Apr 2025 @ 273,023 km — full kit: belt, tensioner,
  idler, cover (per R23/R8). 1HZ is an INTERFERENCE ENGINE — belt failure bends
  valves. Any document stating otherwise is in error. 100,000 km interval falls
  due ~373,000 km — inside the expedition window. RA-3 pre-departure replacement
  (belt + tensioner, ~T-3 months) CONFIRMED 9 Jul 2026 (Gate 1 = A); removed
  belt becomes the carried emergency spare; valve-clearance re-check combined
  at the same rocker-cover-off event.
- Fuel-injection overhaul Jun 2026 @ 310,412 km (Udel): Bosch VE pump rebuilt
  & bench-calibrated, 6 injectors reconditioned (new nozzles), new filter
  housing, all 12 valve clearances set (first recorded adjustment).
- R151F 5-speed manual, part-time 4WD, F&R diff locks
- Terrain Tamer heavy-duty clutch (Feb 2026)
- Free-flow exhaust: 76 mm bore, 409-grade stainless (Powerflow, Bellville,
  May 2026 @ 309,968 km)
- OME medium suspension, 2" lift — OME 859 front coils, N181 NitroCharger+
  front shocks, CS058R multi-leaf rear springs, 63111 NitroCharger+ rear shocks
- GVM 3,000 kg (Toyota-confirmed Jul 2026, authoritative — supersedes the
  2,850 kg figure previously recorded here; Facts §2 is the source). Kerb
  2,190 kg factory (Toyota-confirmed); as-built kerb ~2,317 kg ESTIMATE.
  Kerb/payload as-built PROVISIONAL & unverified — THREE estimate
  pairs circulate (~2,160 / ~690 kg · ~2,299 / ~551 kg · ~2,287 / ~563 kg in
  R11's component table); weighbridge pending (TA-002/RA-1 — UNDATED by
  owner decision, 22 Jul 2026; to be done in full Karoo/expedition trim when
  convenient. NEVER re-attach a deadline. Kerb and payload remain
  PROVISIONAL until the weighbridge slip exists, and any session finding
  either figure presented as settled must re-flag it rather than propagate
  it). Do not treat payload as fixed until the loaded vehicle is weighed.
- 145 L main tank + 2 × 20 L Desert Fox collapsible auxiliary cells
- Current tyres: 265/70R16 Maxxis AT811 · Spare: 265/70R16 · Rims: 16" STEEL
  · Tubeless-only doctrine — NO TUBES carried (owner decision, 10 Jul 2026;
  redundancy = carried carcass + plug kit, per R40 §5).
- Tyre policy (reframed 8 Jul 2026, RA-9): the expedition departs on a fresh,
  PROVEN 265/75R16 set with ≥5,000 proving km (implies fitting ~2027; the
  AT811s finish their life as daily-runaround rubber). Choice criteria: casing
  strength + regional availability. Second-spare / casing-repair strategy for
  East Africa is an open owner decision (one spare is thin north of the
  Zambezi). Field wheel/tyre procedures (wheel change, bead breaking, manual
  carcass swap, re-seat) are owned by R40. R40 kit gaps chartered to the R3
  ledger (EX-8): long tyre levers ×3, rim protectors ×3, spare valve stems ×4
  + TPMS grommet/seal kit, bead lube; OPEN owner decision: clamp-type bead
  breaker (recommended buy). Carcass carried on the EXPEDITION ONLY (owner,
  10 Jul 2026) — not on domestic trips; R40 drill is pre-departure on the
  265/75 set. Wheel-nut torque recorded as 131 Nm
  (standard 70-Series steel-rim spec) — VERIFY against handbook at next
  service and correct R40 §6 if it differs.
- Satellite comms (status per owner, 11 Jul 2026): Garmin inReach Mini 2
  OWNED (tracking, GPS coordinates, SOS). Satellite voice phone NOT YET
  OWNED — planned pre-departure purchase for emergency voice calls;
  IsatPhone 2 recommended (R16). SOS registration is via the Garmin IERCC
  Registration Portal (GEOS acquired by Garmin — geosalliance.com is dead);
  handset Assistance Button config is a mandatory second step. R29 §7/§11
  is authoritative for the activation doctrine.
- Licences & documentation (CORRECTED 17 Jul 2026 — exact dates off the
  physical cards/disc; supersedes the 13 Jul resolution): Driving-licence
  CARDS — Mary-Ann's expires mid-2029, INSIDE the expedition window
  (~month 9–11 of a mid-2028 departure) → MANDATORY pre-departure early
  renewal (a SA card cannot be renewed abroad or by proxy — in-person
  biometrics; expiry kills her IDPs, is an insurance repudiation ground,
  and deletes RA-8 solo-driver capability); fallback = fly-home renewal
  ~Apr/May 2029 clustered with the OI-25 disc waypoint. Neil's expires
  early 2031 — ~5 months AFTER a mid-2030 return → renewal margin-driven,
  NOT mandatory; renew in the same sweep if the DLTC accepts. Doctrine
  basis: WC government — renewal allowed AT ANY TIME before expiry, new
  card valid 5 years from renewal date; CONFIRMED IN WRITING by the WCG
  Contact Centre 17 Jul 2026, ref 260717-000921 (OI-27 closed same day;
  carry the reference to the DLTC when booking — OI-24 un-gated). Start
  the card sweep ~T-6 months (production backlogs routine).
  An IDP never outlives the card it is issued against — issue both IDP
  types against the fresh card(s). VEHICLE LICENCE DISC: current disc
  expires mid-2027 — one ordinary renewal Jul 2027, then early renewal
  in the FINAL WEEK before the 2028 departure (SA early renewal resets
  the cycle pro-rata; full 12 months on the windscreen); one
  mid-expedition renewal at ~month 12 (~2029) handled by RSA PROXY (not
  either crew) under Mary-Ann's letter of authority + certified copies,
  couriered to a route address (Windhoek / Arusha / Nairobi). Actions:
  OI-24 (pre-departure sweep, both cards + disc) / OI-25 (mid-trip disc
  waypoint); OI-27 (DLTC window verification) CLOSED 17 Jul. Doctrine:
  R29 pre-departure checklist + R39 §4; dates: Facts §11.
  OWNER'S-AUTHORITY AFFIDAVIT (R39 §3): print-ready SAPS template built
  20 Jul 2026 as an R39 supplement — complete in ink before commissioning;
  affidavits are commonly treated as current ~3 months only, and
  re-certification is SAPS-only (impossible abroad) → OI-30, EX-9 input,
  decide alongside OI-24/OI-25 (commission in the final pre-departure
  week with the OI-24 sweep).
- Recovery: electric winch fitted + full kit (bow/soft shackles, tree
  protector, kinetic rope, snatch strap, damper, 2× MaxTrax, hydraulic +
  bottle jack, shovel, machete, compressor, deflators, gauge, tyre repair
  kit, ground sheet, PPE). No hi-lift (deliberate — winch + MaxTrax).
  Inventory & crew skills: Facts §11B, R9, R37.
- Electrical (AS FITTED — all-Victron LiFePO4; installed by Manjaro Industries,
  quote LC76SW004). Dual-chemistry: LiFePO4 house + lead-acid crank.
  · House battery: Enertec Power+ 12.8V 210Ah LiFePO4 (15BLI-12V210-BLP) —
    internal BMS + Bluetooth, IP65, 20 kg, 105A max continuous discharge.
  · Crank battery: Willard 650 lead-acid — UNCHANGED.
  · DC-DC: Victron Orion Smart 12/12-50A Non-isolated — cap output to ~40A
    in VictronConnect to protect the 1HZ alternator.
  · Inverter/charger: Victron MultiPlus 12/500/20-16 230V (500VA / ~430W
    continuous, 20A charger, 16A transfer) — replaces Sinetech 1200W.
  · Solar: Victron SmartSolar MPPT 100/30; Hardkorr 200W portable panel
    carried over (confirmed Jul 2026; was not itemised in the quote).
  · Monitor: Victron SmartShunt 500A — replaces BM2000.
  · Load protection: Victron Smart BatteryProtect 65A.
  · Shore power: 16A EZ-Lock SS inlet + 15 m 16A lead — mains charge via
    the MultiPlus.
  · Distribution: Fuse Block Safety Hub 150 + 8-circuit blade + M-series
    battery switch — replaces Lynx Distributor.
  Charge profile: per Enertec Power+ manual; Victron LiFePO4 preset as
  fallback (absorption 14.2–14.4V, float 13.5V, temp-comp OFF, equalise OFF;
  low-temp charge cutoff handled by the battery BMS). The 105A discharge
  ceiling is the total continuous 12V load budget.
- No dedicated crank-battery mains maintainer is fitted (DEFA removed at
  install; MultiPlus and solar serve the house bank only) — crank charging
  is alternator-while-driving. See R32 §6–§7 for the standing routine.
  Rotating-electrics redundancy (spare alternator or brush/regulator kit,
  starter kit, belts) chartered as RA-4 — feeds the R3 ledger (EX-8).
- Electrical install complete: R26 finalised (INTERIM → FINAL); R6 retired
  (folded into R7); R33 retired (superseded by R26); R1/R5/R7/R30 updated to
  the fitted spec. R32 rebuilt to dual-chemistry as-fitted (v2.0, Jul 2026).
- Vehicle registered to Mary-Ann (also title holder); SA-plated RHD.
  RC1 registration-certificate values are HELD OFFLINE — PRIVATE ANNEX
  §1 (removed from the public library 28 Jul 2026; DO NOT RESTORE).
  Tare 2,118 kg is RETAINED in Facts §1 as a technical weight figure
  and RA-1 reconciliation input. Model year 2009 per marketing
  descriptor, plate confirmation pending (OI-31). Formal documents
  (affidavit etc.) describe the vehicle EXACTLY as the RC1 does — no
  model year, no chassis code; take the values from the annex or the
  certificate, not from this library.

LIBRARY REGISTER (authoritative source: LC76_Library_Register.txt)
Always cross-check the register before assuming the library state — index.html
and the project file set have historically lagged behind the live GitHub repo.

Philosophy & ethos:
  LC76_Overlanders_Manifesto.html             Manifesto — fourteen commitments
  LC76_Overlanders_Manifesto_Commentary.html  Commentary — sixteen companion essays + references
  LC76_History_of_Overlanding.html            History — origins & lineage of overlanding
  LC76_Expedition_as_a_System.html            Systems — six essays on seeing the
                                              whole (Meadows/Ackoff/Capra et al.);
                                              philosophical root of R41 + R38 §6.
                                              Built 12 Jul 2026.

Cross-reference index (quick-search entry point — START HERE):
  R30  LC76_Cross_Reference_Index.html       Searchable topic index across the
                                             whole library — the fastest way to
                                             find which R-doc covers a subject.
                                             Kept upfront for quick lookups
                                             (owner, 13 Jul 2026; since the
                                             21 Jul index restructure it sits
                                             SECOND on index.html, directly
                                             after Emergency & Safety); R-number
                                             remains R30 in the sequence below.
                                             OWNER-FLAG COLUMN (making topic
                                             ownership a lookup rather than an
                                             act of memory) approved in
                                             principle 22 Jul 2026 — DEFERRED
                                             until after the Karoo loop.

Field tools:
  LC76_Fuel_Range_Calculator.html
  LC76_Trip_Fuel_Planner.html
  LC76_Fuel_Log_Analyser.html
  LC76_Water_Budget_Calculator.html
  LC76_LiFePO4_Calculator.html   — single-day power budget AND the
                                   Multi-day Stay block (26 Jul 2026):
                                   how many days a camp holds on solar,
                                   and when a recharge drive is needed.
  LC76_Tyre_Pressure_Calculator.html

Reference documents (R1–R42; R6 & R33 retired — numbers not reused;
next free number is R43):
  R1   LC76_Reference_Card.html              Quick reference card. OPENS WITH
                                             EMERGENCY: medevac numbers +
                                             first-action drill added 24 Jul 2026
                                             (OI-41) ahead of Vehicle Identity —
                                             the EX-11 print-and-laminate
                                             door-pocket card. Numbers are a
                                             controlled copy; R29 §6 owns them.
                                             (Field Torque & Pressures card added
                                             21 Jul 2026 — 131 Nm + R14/R40
                                             pointers)
  R2   LC76_Departure_Checklist.html         Pre-departure checklist (21 Jul 2026:
                                             fire-kit critical row, affidavit/
                                             RC1 detail, counts corrected 71/42)
  R3   LC76_Spares_Tools.html                Bush spares & tools (tyre-kit line
                                             aligned to R40/OI-17, 21 Jul 2026)
  R4   LC76_Engine_Training.html             1HZ engine training
  R5   LC76_Battery_Training_Guide.html      Battery & electrical training.
                                             OWNING DOCUMENT for the daily
                                             house-load figure (itemised
                                             breakdown table) — 65.3 Ah/day,
                                             corrected from 77 on 26 Jul 2026;
                                             R7 and the LiFePO4 Calculator
                                             delegate to it (Controlled
                                             Duplication Register)
  R7   LC76_LiFePO4_System_Report.html       LiFePO4 house system — as fitted
                                             (incl. battery longevity & care;
                                             absorbed R6; commissioning
                                             charge-profile table added
                                             21 Jul 2026; daily load 77 →
                                             65.3 Ah/day with all derived
                                             values recomputed 26 Jul 2026 —
                                             autonomy 2.6 days, DoD 31%,
                                             degradation column re-based;
                                             fade curve retained with a
                                             conservative-basis note)
  R8   LC76_Service_History.html             Service & cost dashboard (REBUILT
                                             10 Jul 2026, EX-12 S2) — KPI strip,
                                             charts V1–V4 + KPI strip (=V5),
                                             major-works register, status &
                                             forward plan; chronological ledgers
                                             in two supplements (boundary
                                             200,000 km)
       LC76_Service_Checklist.html           R8 supplement — interactive interval service checklist
       LC76_Service_Ledger_Archive.html      R8 supplement — chronological ledger
                                             <200,000 km (79 rows, 2010–2021,
                                             R132,851.31; closed era). Built
                                             10 Jul 2026, EX-12 S3.
       LC76_Service_Ledger_Current.html      R8 supplement — chronological ledger
                                             ≥200,000 km, live era (236 rows,
                                             2021–present, R677,987.01 excl.
                                             LiFePO4 TBC; future entries land
                                             here). Built 10 Jul 2026, EX-12 S3.
  R9   LC76_Vehicle_Recovery_Guide.html      Vehicle recovery (winch electrical
                                             doctrine added 21 Jul 2026 —
                                             crank-direct, engine at fast idle)
  R10  LC76_Emergency_Procedures_Guide.html  Emergency procedures. NOW OPENS
                                             WITH an "Emergency Numbers — Read
                                             First" block (25 Jul 2026): SA
                                             cellphone list + satphone list +
                                             the share-call-won't-route warning;
                                             Netcare removed from every dial
                                             surface (non-member). OWNS THE DRILL;
                                             R29 owns the cover stack. (§3 fire
                                             drill rewritten 21 Jul 2026 to R42-
                                             consistent doctrine — M-series,
                                             no crank isolator, LiFePO4 water.
                                             §9 gained 24 Jul 2026: ownership
                                             note, THE HANDOVER five-item call
                                             script, call-first-argue-cover-later
                                             doctrine, and the satellite-dialable
                                             medevac numbers as a controlled copy
                                             of the R29 §6 master set)
  R11  LC76_Packing_Optimisation_Guide.html  Packing optimisation
  R12  LC76_Water_Management_Guide.html      Water management
  R13  LC76_Drivetrain_Training_Guide.html   Drivetrain training
  R14  LC76_Tyre_Management_Guide.html       Tyre management (TPMS TP-V31
                                             trend-and-alarm doctrine card
                                             added 21 Jul 2026)
  R15  LC76_Border_Crossings_Guide.html      Border crossings (Border Predation
                                             card added 21 Jul 2026 — fake
                                             agents, touts, collectors, fixer
                                             selection, passport-in-hand rule)
  R16  LC76_Comms_Navigation_Guide.html      Comms & navigation (ER24/Netcare
                                             international numbers corrected
                                             24 Jul 2026, OI-44 — the
                                             unprovenanced +27 10 205 3068 is
                                             gone; RA-10 gained item 10, the
                                             medevac number test-dial from the
                                             handset)
  R17  LC76_Health_Hygiene_Guide.html        Health & hygiene (Lake Malawi
                                             bilharzia doctrine card added
                                             21 Jul 2026 — praziquantel timing)
  R18  LC76_1HZ_Troubleshooting_Guide.html   1HZ troubleshooting (21 Jul 2026:
                                             timing-belt failure card [interference
                                             — STOP CRANKING], §3B EGT pyrometer
                                             doctrine, Victron-era charging note)
  R19  LC76_AT_Tyre_Review_R19.html          AT tyre review
  R20  LC76_Seal_Gasket_Atlas.html           Seal & gasket atlas
  R21  LC76_Turbo_Operations_Guide.html      Turbo operations
  R22  LC76_Cooling_System_Guide.html        Cooling system (RA-5 condition-
                                             assessment note added 21 Jul 2026)
  R23  LC76_Pre_Trip_Prep_Protocol.html      Pre-trip prep protocol (§7 POST-TRIP
                                             REVIEW PROTOCOL built 21 Jul 2026,
                                             OI-35 — first execution: Karoo
                                             return)
  R24  LC76_Fuel_System_Guide.html           Fuel system & diesel quality
  R25  LC76_Suspension_Steering_Guide.html   Suspension & steering
  R26  LC76_Electrical_Wiring_Guide.html     Electrical & wiring guide — all-Victron
                                             LiFePO4 (CURRENT; supersedes the interim
                                             lead-acid R26 at install)
  R27  LC76_Bush_Mechanics_Guide.html        Bush mechanics field guide
  R28  LC76_Driving_Conditions_Guide.html    Driving conditions guide
  R29  LC76_Insurance_Medevac.html           Insurance & medevac. OWNING DOCUMENT
                                             for the emergency-number master set
                                             (§6) — full international format,
                                             SA short-dial labelled satellite-
                                             undialable; R10/R1/R16/R38 delegate.
                                             §6 four-layer medical model, decoupled
                                             from the motor insurer's bundled
                                             benefit (retain, never rely). §13
                                             Vehicle Asset Cover added 24 Jul 2026
                                             — agreed-value build-up, duration
                                             gate, Carnet conflict on a total loss
                                             abroad, exclusions that bite;
                                             acknowledged skeleton pending policy
                                             wordings (R38 §4 precedent). (licence-card
                                             validity chain CORRECTED 17 Jul —
                                             Mary-Ann mid-2029 mandatory
                                             renewal; Neil early 2031
                                             margin-driven)
  R30  → relocated to "Cross-reference index" above (quick-search entry
         point, kept upfront) — R-number retained as R30.
  R31  LC76_Upgrade_Assessment.html          Upgrade assessment (LiFePO4 / solar /
                                             winch DONE rows + 265/75 PLANNED row
                                             added 21 Jul 2026; radiator row wired
                                             to RA-5)
  R32  LC76_Crank_Battery_Care.html          Crank/starter battery care
  R34  LC76_Brake_System_Guide.html          Brake system guide — consolidation
  R35  LC76_Security_Scams_Guide.html         Security & scams guide — folds
                                             audit F1 (security); fire (audit
                                             F4) split out to R42 on 21 Jul
                                             2026. §1–7 security (awareness,
                                             docs & money, checkpoints, spot
                                             fines/bribes, urban crime, theft
                                             prevention, incident response);
                                             §8 graded scam & confidence-trick
                                             register (evidence grades V/M/S +
                                             EX-10 refresh rule; EXPANDED
                                             21 Jul 2026 — 14 rows + six-
                                             standing-habits doctrine + §4
                                             named variants). Renamed from
                                             LC76_Security_Fire_Guide.html —
                                             true rename, old file git rm'd.
  R36  LC76_Wildlife_Camp_Safety_Guide.html  Dangerous wildlife & camp safety
                                             (baboon/vervet + African bees
                                             added 21 Jul 2026)
  R37  LC76_Crew_Readiness_Training_Register.html  Crew readiness & training register
                                             (EX-1; checkpoint/scam roleplay
                                             drill row added 21 Jul 2026 —
                                             Karoo loop, R23-assessed; fire-
                                             response drill row added same
                                             day — live discharge + blanket)
  R38  LC76_Expedition_Operations_Plan.html  Expedition operations plan (EX-2;
                                             §6 Assumption & Trigger Register
                                             A1–A12 added 12 Jul 2026; §4
                                             licence-break still SKELETON —
                                             M4/EX-9 fills it)
  R39  LC76_Expedition_Documentation_Money_Guide.html  Expedition documentation & money guide (EX-3;
                                             disc doctrine + card-chain note
                                             CORRECTED 17 Jul 2026 — exact
                                             dates, Mary-Ann mandatory; §6
                                             mobile-money fourth-instrument
                                             block added 21 Jul 2026)
       LC76_Owner_Authority_Affidavit.html   R39 SUPPLEMENT — print-ready SAPS
                                             owner's-authority affidavit (the
                                             registered owner authorising the
                                             co-driver, cross-border). Vehicle
                                             identity: VIN/engine/registration
                                             pre-filled; RC1 certificate fields,
                                             names, ID numbers, address, dates &
                                             countries BLANK by design —
                                             completed in ink BEFORE commissioning.
                                             Formal legal layout — design-system
                                             EXEMPT (calculator exemption class).
                                             Built 20 Jul 2026. Completed print
                                             master (IDs filled) held OFFLINE only.
  R40  LC76_Wheel_Tyre_Field_Procedures.html Wheel & tyre field procedures —
                                             wheel change, bead breaking (no
                                             hi-lift), manual carcass swap,
                                             ratchet-strap bead seat. Built
                                             10 Jul 2026. R14 owns strategy;
                                             R40 owns the spanner work.
  R41  LC76_Expedition_Systems_Map.html      Expedition systems map — the
                                             connections, not the parts. Six
                                             systems + coupling matrix, failure
                                             cascades C1–C10 (chain · clocks ·
                                             chain-breaker), seams register,
                                             sensing & feedback loop. Companion
                                             to R38 §6 assumption register.
                                             Owns no doctrine. Built 12 Jul 2026.
  R42  LC76_Fire_Safety_Guide.html            Fire safety guide — split from
                                             R35 on 21 Jul 2026 (audit F4);
                                             EXPANDED same day (fire-service
                                             review): blanket + aerosol-stick
                                             + sizing doctrine, LiFePO4 fire
                                             (water correct), prevention
                                             walk-around, LPG/canister rows,
                                             tent fire; §2 crank isolation
                                             corrected — no isolator fitted.
                                             §1 extinguishers, §2 vehicle-fire
                                             response (don't open the bonnet;
                                             isolate the batteries), §3 fuel &
                                             gas handling, §4 bush & camp fire
                                             discipline (raised pan + ground
                                             sheet on no-dig terrain). Owns
                                             prevention & equipment only — the
                                             in-the-moment drill is R10 §3,
                                             stowage R11, legal carry R15.
                                             Built 21 Jul 2026.

  RETIRED (numbers not reused):
  R6   LC76_Battery_Longevity_Report.html    Retired — content folded into R7.
  R33  LC76_Aux_Battery_Care.html            Retired — superseded by R26.

Country & terrain profiles (001–010):
  001  Angola_Overland_Profile_2026-04.html
  002  Namibia_Overland_Profile_2026-04.html
  003  Botswana_Overland_Profile_2026-04.html
  004  Zambia_Overland_Profile_2026-04.html
  005  LC76_Kalahari_Profile_2026-04.html
  006  Tanzania_Overland_Profile_2026-05.html
  007  Mozambique_Overland_Profile_2026-07.html
  008  Malawi_Overland_Profile_2026-07.html
  009  Zimbabwe_Overland_Profile_2026-07.html
  010  Kenya_Overland_Profile_2026-07.html

  SECTION NUMBERING — HARMONISED 22 Jul 2026 (OI-36). All ten country
  profiles now carry §06 Parts & Serviceability and run the SAME numbering.
  001–004 and 006 had the section INSERTED at §06 and their former §06–§18
  renumbered to §07–§19, matching 007–010, which were built with it. Each
  is a 19-section profile (plus alerts and changelog). 005 Kalahari is a
  TERRAIN profile with a different section spine and is deliberately OUT of
  scope. LC76_Profile_TEMPLATE.html was corrected the same day so the gap
  cannot recur on the next profile built. Perishable content in the new
  sections carries the OI-37 freshness marker with V/M/S grades; the full
  build of that content lands at EX-10.

Trip itineraries:
  LC76_Karoo_Winter_Loop_2026.html  (Cape Town round-trip, winter 2026,
                                     29 nights, 10 stops, anticlockwise.
                                     Runs on RELATIVE DAY NUMBERS — exact
                                     dates in PRIVATE ANNEX §4, restored at
                                     the R23 §7 post-trip review)

EXPERIMENTAL / PRE-EX-9 PLANNING ARTIFACTS (NOT library files — outside the
repo, not precached, no CACHE impact; see WORKING CONVENTIONS + OI-26):
  LC76_Experimental_Route_OptionA_WORKING.docx  Owner's working brief (source
                                             of truth; edit in place, "CHANGES
                                             THIS ROUND" list at top on re-upload)
  LC76_Experimental_Route_OptionA_DRAFT.md      Markdown twin of the brief
  LC76_Experimental_Route_OptionA_MAP.html      Standalone full-screen Leaflet
                                             route map (a VIEW, re-rendered from
                                             the brief on demand — not persistent)

Supporting: index.html, LC76_Design_Guidelines.html, LC76_Profile_TEMPLATE.html,
PWA manifests and service workers.
  LC76_How_To_Use_This_Library.html  THE LIBRARY'S USER GUIDE (built 29 Jul
                                     2026, OI-58). Nine sections: file classes,
                                     glossary of every abbreviation, the four
                                     registers explained, the change loop,
                                     monthly/pre-trip/post-trip routine, session
                                     protocol, precedence order, fault
                                     reporting. INFRASTRUCTURE CLASS — no
                                     R-number, masthead-EXEMPT under DG S10, but
                                     carries a Last-updated line by DELIBERATE
                                     DIVERGENCE (it goes stale whenever the
                                     library's shape changes; the Design
                                     Guidelines do not). DO NOT "CORRECT" THIS.
                                     Sections 03/06/08 are written for Mary-Ann
                                     as an RA-8 input. First card in index.html
                                     §2, ahead of R30. PRECACHED.

Audit, governance & data (internal — not field-reference, not precached):
  LC76_Library_Audit_2026-06.html        Coverage audit (Jun 2026), findings F1–F17
  LC76_Library_Audit_2026-07.html        Post-install audit (Jul 2026), findings
                                         F18–F37; remediation RS-1–RS-11 COMPLETE
  LC76_Library_Audit_2026-07_Content.html  Content currency & completeness audit
                                         (Jul 2026), findings F38–F74 — currency /
                                         completeness / accuracy / consistency /
                                         field fitness, risk-tiered. 31 of 37
                                         remediated in-session. Governance file —
                                         index-linked, NOT precached, no CACHE bump.
  LC76_Open_Items_Register.txt           SINGLE authoritative open-items tracker
                                         (OI-1…OI-46 + CLOSED LOG + Controlled
                                         Duplication Register). Governance
                                         file — GitHub + project knowledge; NOT
                                         precached, NEVER triggers a CACHE bump.
  LC76_Library_Audit_WorkingPapers.xlsx  Audit working papers — TA / LX / EX registers
                                         (EX_Register carries the RA Readiness
                                         Actions block; EX-12 row closed
                                         10 Jul 2026; COUNTIF baseline = 9
                                         formula cells, owner-adjudicated 12 Jul)
  LC_Service_history_FINAL.xlsx          Service ledger of record (source behind
                                         R8 and the two ledger supplements).
                                         TWO SHEETS from 29 Jul 2026: Services
                                         (the cost ledger, 315 records) and
                                         ADVISORIES (workshop-flagged items —
                                         ID, date, mileage, source, item,
                                         detail, severity, action, status, OI
                                         ref; seeded 10 rows from R8 §2 and
                                         Critical Outstanding). Advisories are
                                         NEVER written to the Services sheet:
                                         a zero-cost row there means a child
                                         line item of the invoice above.
                                         R8 and the two ledger supplements) —
                                         315 records. Single Services sheet.
                                         LEDGER HYGIENE APPLIED 10 Jul 2026
                                         (EX-12 S1): 34× cost=1 placeholders
                                         normalised to 0; COST-0 CONVENTION and
                                         rule-based scheduled/unscheduled
                                         classification documented in col I.
                                         Recorded total R810,838.32 excl. the
                                         LiFePO4 install entry (date/cost TBC).
                                         Row 234 (1 Jan 2026 "Year end") is a
                                         blank-cost milestone marker. New service
                                         entries are made in the xlsx FIRST; the
                                         HTML ledgers are derived views.

EXPEDITION READINESS PROGRAMME (EX) — charter approved 4 Jul 2026;
extended 8 Jul 2026 (consultant strategic review) and 10 Jul 2026 (EX-12)
  EX-1 through EX-7: DONE (see handover pack for detail).
  EX-8:  R3 Expedition Consumables Ledger (projection basis: 60,000+ km,
         corrected 9 Jul 2026) + R23 Post-Trip Review Protocol [BUILT
         21 Jul 2026 as the OI-35 pull-forward — R23 §7] + retrofit
         Parts & Serviceability §06 back to profiles 001–006 [PULLED
         FORWARD AND BUILT 22 Jul 2026 as OI-36, outside EX-8, as a
         combined pass with OI-37 — DONE for 001/002/003/004/006, with
         005 scoped out and the template corrected; only the EX-10 full
         build of perishable content remains. See the header. RATIONALE: the R3 Consumables Ledger
         keys its carry-versus-source-en-route logic to profile
         serviceability data, so the six missing sections block EX-8's
         principal deliverable for the first year of the route — the
         retrofit must therefore precede the R3 build, not follow it].
         Now also carries the R40 kit gaps (tyre levers, rim protectors,
         valve stems + TPMS seals, bead lube; bead-breaker decision
         item). EXTENDED 20 Jul 2026 with the no-trace camp kit:
         consumables — pack-out sanitation bags, gelling agent/powder,
         urine bags, loo rolls, hand sanitizer (usage rate per crew-day);
         hardware — flat-pack fire pan, braai/cooking kit, fibreglass
         ground sheet, ash tin + scoop, sealed sanitation container.
         Doctrine R17 §5 / R42 §4; kit lines R11. EXTENDED 21 Jul 2026
         with the fire-suppression kit: fibreglass fire blanket
         ≥1.2×1.2 m (1.8 m preferred), 2× aerosol suppressor sticks
         (Fire Stryker class), extinguisher fleet review vs R42 §1
         sizing (primary ≥2.5 kg), tent knife ×2. Doctrine R42 §1–§4;
         kit lines R11.
  EX-9:  Route & licence-break workshop (interactive). Option A adopted
         13 Jul pending ratification; direction inverts Namibia-first opening /
         eastern corridor as southbound return. Carnet 12-month interlock (a
         no-return two-year trip cannot ride one Carnet) is the TOP open
         constraint — feasibility, not scheduling. Seasonal/route input already
         drafted as the experimental route (OI-26). NEW INPUT (17 Jul): card
         dates pull on departure timing in opposite directions (Mary-Ann
         mid-2029 mid-trip vs Neil early 2031 return margin). On
         ratification: minute decisions → M4 executes (R38 §4 rewrite from
         SKELETON + R39 §1 split-trip reconciliation) → then propagate to
         profile route framing.
  EX-10: [26 Jul 2026 — the multi-day rolling-balance candidate raised this
         session is CLOSED, built into the LiFePO4 Calculator the same day
         rather than deferred: the owner's stay pattern (3–5 nights at one
         camp, discretionary recharge drive) is a live planning question
         now, not a T-6 one.]
       Pre-departure profile currency refresh — chartered full refresh of
         profiles 001–010 plus R15/R39 fast-moving data (visas, fees, fuel,
         security, road state) at ~T-6 months before expedition departure
         (trigger ~2028). Profiles built 2026 will be materially stale by a
         2028 crossing. Also re-verifies/prunes S-grade scam-register rows
         older than ~18 months (R35 §8 embedded refresh rule). NOW ALSO
         CARRIES (22 Jul 2026): the FULL build of the §06 Parts &
         Serviceability sections retrofitted thin under OI-36 — the durable
         content is placed in 2026, the perishable specifics are completed
         and re-verified here; and the ongoing maintenance of the OI-37
         data-freshness markers, whose whole purpose is to make this refresh
         estimable rather than open-ended. The Session E report's
         per-profile perishable-data census is the workload estimate.
  EX-11: Crisis-critical printed field pack — print-optimised pack (R1, R10,
         R18, R37 emergency/comms extract, active profile) per DG Section 16;
         owner prints & laminates R1/R10 for the door pocket. Mitigates
         device-dependency of the PWA library. (R40 §2–§5 is a candidate
         addition — a carcass swap is exactly the job done when devices are
         flat or broken.) Recommended next build session (OI-10).
  EX-12: R8 Service History restructure & cost analysis — PROGRAMME CLOSED
         10 Jul 2026 (three sessions). S1: ledger hygiene on the xlsx (cost=1
         normalised, COST-0 + classification conventions in col I). S2: R8
         rebuilt as the Service & Cost Dashboard — KPI strip, charts V1–V5
         inline SVG from embedded JSON, major-works register, forward plan;
         V6 supplier concentration DROPPED (owner). S3: both ledger supplements
         built (Archive <200,000 km · 79 rows · R132,851.31; Current
         ≥200,000 km · 236 rows · R677,987.01 excl. LiFePO4 TBC; boundary
         200,000 km; future entries land in Ledger_Current only, via the xlsx
         first). Classification rule (Gate 1): category beginning "Manufacturers
         service" or "In between service" = SCHEDULED; all else = UNSCHEDULED/
         RENEWAL. Standing residue: Gate B (LiFePO4 invoice — OI-8).

AUDIT OF TRUTH — ASSURANCE PROGRAMME (chartered 29 Jul 2026; charter file
LC76_Audit_of_Truth_Charter.txt, issue 2, APPROVED as the basis for the work;
no fieldwork executed)
  WHY: three audits have examined the LIBRARY; none has examined the
  instrument that tracks it. The register has been the auditor and has never
  been audited. The gap was already recorded in this file's roll-forward
  convention — "this catches SHAPE, NOT TRUTH" — and every control built
  since verifies that entries are correctly FORMED, not that they are true.
  RUNNING ORDER:
    Session 0  Mastership map into this file + the user guide (visible).
               EXECUTED 29 Jul 2026 (twenty-sixth session) — map block
               inserted into the guide §04; PI and Library Register
               already carried it from Session 25.
    Session 1  POPULATION and ORIGINATION — established by sweep, not
               assumed; five streams mapped; two-way trace matrix.
               EXECUTED 29 Jul 2026 (twenty-seventh session) — 21
               instruments adjudicated; three new live feeders (Facts §3,
               R37, R31); 11 register-born entries enumerated (S3 gap);
               evidence in working-paper sheets AoT_S1_*.
    Session 2  Fieldwork, assertions A1–A10, items 1–20.
               EXECUTED 30 Jul 2026 (twenty-eighth session) — 20 items
               tested exhaustively (M1, not sampling); 6 SIGNIFICANT,
               13 MODERATE, 1 HOUSEKEEPING (provisional pending M5). The
               two flagged A3 reconciliations resolved as findings; a new
               A2 mapping defect (ADV-010→OI-7) logged. Evidence in
               AoT_S2_Findings / _A3_Recon / _XCut. No OI consumed.
    Session 3  Fieldwork, items 21–40 + closed-log sufficiency.
               EXECUTED 30 Jul 2026 (twenty-ninth session) — 20 items tested
               exhaustively + all 27 numbered closures (A10); 4 SIGNIFICANT,
               14 MODERATE, 2 HOUSEKEEPING. F-D '7 of 27' found not
               reproducible; F-C OI-39 over-count corrected. Evidence in
               AoT_S3_Findings / _ClosedLog / _XCut. No OI consumed.
    Session 4  FINDINGS DOCUMENT — owner rules. NO EDITS BEFORE THIS.
               EXECUTED 30 Jul 2026 (thirtieth session) — consolidated
               findings document LC76_Audit_of_Truth_Findings.txt built (40
               items 10/27/3; 27-closure A10; F-D unreproducible; D1–D7
               decisions schedule; 40 items 10/27/3 as built). Owner rulings
               RECORDED same day (addendum, D1–D6): OI-62/63 re-rated → 12/25/3,
               M5 retired, charter → issue 3. NO OIR edit, no OI consumed.
               Session 5 AUTHORISED — remediation follows.
    Session 5  Remediation + stand up the schema validator.
               PHASE 5A EXECUTED 30 Jul 2026 (thirty-first session) — register
               remediated against D1–D6: 11 new items (OI-68…78, 40→51), 6
               bundles split, OI-62/63 re-rated, status vocab + confirmation-
               date field applied, 12 closures reclassified DONE-PENDING, Facts
               §3 recast, ADV-010 re-mapped. No item closed. PHASE 5B DONE —
               validator built + run, register PASSES (caught & fixed the OI-10
               meta-line defect). SESSION 5 COMPLETE; Session 6 (dashboard) next.
    Session 6  Dashboard wired (CACHE bump v25 → v26 lands here).
               EXECUTED 30 Jul 2026 (thirty-second session) — dashboard
               regenerated from the remediated register (50 open items, OI-59
               closed by the wiring), index-linked + precached, CACHE v25→v26.
    Session 7  Systemic generation sweep (charter Appendix A) — LAST,
               because running it earlier pours new findings into a
               leaking instrument.
  OWNER TIME: Session 4, plus the materiality cost threshold (charter M5).
  INDEPENDENCE: Claude wrote most of the entries it proposes to audit. The
  self-review threat is stated in charter §13 with five safeguards; the
  governing one is that no finding rests on Claude's assertion — every
  finding cites a file and locator the owner can open, or a script he can
  re-run.
  ELEVEN INTERIM FINDINGS already measured (charter §14), of which
  SIGNIFICANT: provenance failure in 26 of 40 items; no status has a
  confirmation date; five entries are more than one job; the RA-4 cross-
  instrument divergence; unresolved working-paper rows with no register
  item; and two numeric claims that do not reconcile to the service ledger.

READINESS ACTIONS (RA-1–RA-10) — owner-gated physical actions, chartered
8 Jul 2026 from the external consultant strategic review; tracked in the
working-papers EX_Register and the Open Items Register:
  RA-1  Loaded weighbridge, full expedition trim — UNDATED, OWNER-TIMED
        (owner decision 22 Jul 2026; the former pre-departure deadline is
        WITHDRAWN and must never be re-attached).
        Same physical action as TA-002 — closing RA-1 closes TA-002. Gates
        tyre pressures, spring adequacy, insurance validity (overloading is
        a standard repudiation ground — now written into R29 §2/§11), axle-
        load legality. Closure = single library-wide figure propagation
        (R11, R12, Facts §2) from the weighbridge slip. Until then all kerb
        and payload figures remain PROVISIONAL and must be re-flagged, never
        propagated as settled. KNOWN LIMITATION accepted with the undating:
        the Karoo loop no longer doubles as a shakedown at measured weight,
        so its R23 §7 consumption actuals carry an unmeasured mass basis —
        record that caveat with the data rather than treating the figures as
        clean EX-8 calibration input. [OI-1, CRITICAL PATH for library
        propagation, not for the Karoo trip]
  RA-2  Diagnostic baseline: compression + leak-down (all six), coolant
        analysis, and START a spectrographic oil analysis programme (sample
        every service) to trend wear toward ~380–400k km. [OI-5]
  RA-3  Timing belt + tensioner pre-departure replacement — CONFIRMED 9 Jul
        2026 (Gate 1 = A). Belt Apr 2025 @ 273,023 km, full kit; interval falls
        ~373,000 km mid-expedition. Replace ~T-3 months; carry removed belt as
        emergency spare; combine valve-clearance re-check. [OI-21]
  RA-4  Rotating-electrics redundancy — RULED 24 Jul 2026: NO complete
        spare alternator is carried (70-Series alternator is commonly
        available on the route; the unit is heavy and expensive; payload
        PROVISIONAL until RA-1). Carried: alternator brush/regulator kit
        (R3 item 4.7), starter contact/solenoid kit (R3 4.6), spare V-belt
        set (R3 1.3). Replacement sourced en route. Consequence doctrine
        R32; cascade R41 C1; ledger lines EX-8. [OI-45 CLOSED; OI-46 open
        — confirm the jump pack can be recharged in the field]
  RA-5  Cooling hardware condition assessment: radiator core, viscous fan
        hub, hoses — replace on condition before departure. [OI-6]
  RA-6  Medical readiness: wilderness first aid course incl. trauma +
        snakebite for BOTH crew; pre-departure medical AND dental screening;
        prescribing-doctor expedition medical kit with border paperwork.
        Completion recorded in R37. [OI-4]
  RA-7  Recovery practice day pre-Karoo: winch under real load, snatch-block
        rigging, MaxTrax; both crew rig and both drive. Recorded in R37. [OI-2]
  RA-8  Mary-Ann solo-capability scenario designed into the Karoo loop
        (solo recovery, solo dirt driving, solo comms-tree execution).
        Feeds R37/R38; assess on return via R23 post-trip review. [OI-13]
        NOTE 17 Jul: RA-8 capability legally depends on Mary-Ann's licence
        card remaining valid — see OI-24/OI-27 (card expires mid-2029).
  RA-9  Expedition tyre policy (owner decision): depart on proven 265/75R16
        set (>=5,000 km, fit ~2027); casing/availability-led choice; decide
        second-spare / casing-repair strategy for East Africa. The R40
        carcass-swap drill is run pre-departure on the new set once fitted
        (both crew, R37 sign-off). [OI-22, and OI-19 second-spare decision]
  RA-10 Comms fail-safe verification: independent inReach layer (InReach
        Mini 2) confirmed registered & operational; SCOPE EXTENDED 11 Jul 2026
        to the planned satellite voice phone (R16 §6 items 6–9 + R38 §1
        sign-off rows 4–5, contingent until purchase). Physical verification
        (inReach live test pre-Karoo; satphone regime from purchase) is the
        open owner action. [OI-3, and OI-18 satphone purchase]

ROUTE EVALUATION CHANGE MAP M1–M4 (chartered / executed 13 Jul 2026;
card-date elements CORRECTED 17 Jul 2026)
  M1  EXECUTED 13 Jul — Open Items Register: OI-12 (EX-9) updated with Option A
      adopted-pending-ratification + direction inversion; OI-24 (pre-departure
      admin sweep) and OI-25 (mid-trip disc waypoint) added.
      [17 Jul: OI-24 REWRITTEN — both cards, exact dates, decision ladder;
      OI-27 DLTC-window verification opened AND closed same day (WCG
      written confirmation ref 260717-000921) — OI-24 un-gated.]
  M2  EXECUTED 13 Jul — R29: pre-departure "verify both cards" row converted to
      the RESOLVED finding + action. [17 Jul: row REWRITTEN as CORRECTED —
      Mary-Ann mid-2029 mandatory / Neil early 2031 margin-driven; WC
      any-time-renewal basis (confirmed in writing, ref 260717-000921)
      + T-6 start. Masthead 17 Jul.]
  M3  EXECUTED 13 Jul — R39: disc-renewal doctrine note (vehicle-licence area)
      + §4 card-validity-chain note. [17 Jul: card-chain note REWRITTEN as
      CORRECTED (exact dates, EX-9 departure-timing input); disc note gains
      current disc expiry mid-2027. Masthead 17 Jul.]
  M4  PROPOSED, NOT EXECUTED — belongs to EX-9. R38 §4 rewrite (SKELETON →
      Option-A "continuous, no-return, disc-by-proxy" framing; elevate the
      Carnet 12-month interlock as THE open constraint; re-point the major-
      service window) + R39 §1 reconciliation ("may be split around the licence
      renewal" → the split is gone; the Carnet constraint now bites on the
      24-month continuous duration). Route-direction propagation across
      profiles/R15 also HELD for EX-9. index.html header-meta bumped 13 Jul
      (and again 17 Jul).

PROFILE §06 RETROFIT + FRESHNESS MARKER (OI-36 + OI-37, chartered 22 Jul 2026)
  Executes as ONE combined per-profile pass — one visit per file, not two.
  GATE 0 (Session 1, before any spec): confirm at pinned HEAD what §06 is in
    001–006 and in 007–010, and what the live LC76_Profile_TEMPLATE.html
    carries. The project-knowledge template runs 18 sections with §06 = Roads
    while 007–010 apparently use §06 = Parts & Serviceability; either the
    template was revised or the cohorts diverged. If the template lacks the
    section, correct the TEMPLATE too — otherwise the gap recurs on the next
    profile built. Outcome settles APPEND (adopted default) vs renumber.
  DEPTH: thin-but-structurally-complete. Durable content now — 1HZ parts
    reality, tyre sizes carried and planned, Victron dealer/support presence,
    fuel grade & 50ppm availability. Perishable specifics carry the freshness
    marker and are completed at EX-10.
  FRESHNESS MARKER: "Data verified: <date> · prices & fees indicative only",
    inheriting the R35 §8 V/M/S evidence-grade convention. Row-level grades
    only where a wrong number costs money or safety (fees, fuel prices,
    security posture). DEFAULT ADOPTED: the existing content-currency badge
    (document-level provenance) and the freshness marker (data-class-level)
    are DIFFERENT instruments — keep both, leave the badge untouched per the
    standing convention, place the marker at the head of each perishable
    section. Owner may collapse to one mechanism at Session 1.
  SEQUENCE (by route exposure, not profile number): Session 1 — 002 Namibia,
    003 Botswana, 004 Zambia. Session 2 — 006 Tanzania, 001 Angola.
  EXECUTED 22 Jul 2026, both sessions, at pinned HEAD 9c6799c1 (full-corpus
    tarball fetch). GATE 0 outcome: cohorts had diverged, the template was
    stale, and the append premise was false — see the header. All five
    profiles INSERTED at §06 and renumbered 06–18 → 07–19; the template
    corrected the same day. 005 Kalahari SCOPED OUT: it is a terrain
    profile with a different section spine (its §06 is "Zone 3 — Khutse"),
    a Parts & Serviceability section does not map onto it, and its ground
    lies in Botswana, Namibia and the Northern Cape — covered by 002/003.
  VALIDATION on all six edited files: script-stripped div/span depth walks
    0/0/0, spans balanced, ids unique, node --check clean, sec-num sequence
    01–19 + LOG, and the TOC/sectionIds index contract 20/20 with pairwise
    order verified.
  PROPAGATION per session: R30 topic rows · Library Register if descriptions
    move · Open Items Register · working-papers row · per-profile changelog
    entries · mastheads · index.html header-meta. Content-only — NO CACHE
    bump (no file added, removed or renamed).

R29 EVALUATION CHANGE MAP M1–M5 (11 Jul 2026 — CLOSED same day; all executed).
MOBILE PATTERN v1.7 PROPAGATION (11 Jul 2026 — S1–S3 all DONE; WORKSTREAM
CLOSED; 62 in-library files conform). Both retained in the header narrative
and the handover pack.

FOLLOW-UPS CLOSED 12 Jul 2026: Validator refinement (script-stripped depth
walk adopted); R30 R8-entry staleness + missing R26 entry; Angola stray
</span> (OI-15). R40 reciprocal xrefs + R30 R40 row — all CLEARED. R5/R7
design-system rebuild — found already executed upstream, CLOSED.

OPEN R40 / MISC follow-ups: R40 §6 wheel-nut torque VERIFY vs handbook at next
service (OI-7); clamp-type bead breaker purchase decision (OI-17); kit gaps
feed the EX-8 R3 ledger session.

FOOTER STANDARDISATION (CLOSED — 8 Jul 2026): 36 files touched; 23 footers
removed, 8 profile footers reduced to sources-only, 4 dead .doc-footer CSS
cleaned; calculators/audit/index footers retained. Branding discrepancy
resolved. Content-only, no sw.js bump.

DEFECT FIXES (7 Jul 2026): offline map tiles (sw.js, CACHE v15→v16); tile-source
reconciliation (009 → OSM basemap, CACHE v17→v18). Detail in handover pack.

DESIGN SYSTEM (mandatory — full spec in LC76_Design_Guidelines.html, now v1.9)
- Fonts: Bebas Neue (display), Oswald / 'Oswald','Arial Narrow',sans-serif
  (body), IBM Plex Mono (labels/mono). The whole library is on this single
  on-system set — no off-system faces anywhere.
- Reference doc tokens: --ink:#0d0e12, --paper:#dad2c0, --paper2:#edeae0,
  --amber:#b86800, --amber2:#e07800, --rule:#d4cfc4, --steeldk:#2a2f3a,
  --steelmd:#6a7080, --red-warn:#B91C1C, --green-ok:#059669
- Calculator tokens (warm palette): --clay, --ochre, --sand, --cream, --ink,
  --bark, --smoke
- Dark masthead + light content area pattern
- Masthead standard (DG v1.6, section S10) — six slots, fixed order:
  back-link (⌂ Index) · eyebrow (Collection · ID) · title (Title Case) ·
  one-line subtitle (≤~12 words) · invariant identity line · Last-updated date.
  Identity line, identical on every file: "2009 LC76 SW · 1HZ 4.2L Diesel ·
  low-boost turbo (0.6 bar) · intercooler". NEVER in a masthead: odometer, GVM,
  per-document component specs, the EX-programme tag, or a vehicle code in the
  eyebrow. Closed collection vocabulary (six): Reference · Country Profile ·
  Terrain Profile · Field Tool · Philosophy · Itinerary. Profiles carry ONE
  extra element — native official name as a de-emphasised line under the title;
  their entry/visa/plating strip moves to the body. Exempt: calculators (no
  back-link/identity/date, eyebrow "Field Tool") and governance/infrastructure
  (index.html, Design Guidelines, Profile Template, audit HTML). Date form:
  "Last updated: D Month YYYY" (lowercase, full month, no zero-pad).
  R8-supplement eyebrow form: "Reference · R8 Supplement".
  PHILOSOPHY — ONE EXTRA ELEMENT (DG v1.9, adopted 29 Jul 2026). The four
  philosophy documents carry an editorial gloss as a de-emphasised italic
  .std-local line placed BELOW THE SUBTITLE and above the identity line —
  deliberately NOT the profile placement (profiles put .std-local directly
  under the title), because the philosophy .std-sub is a small mono amber
  line, typographically the same instrument as the profile .std-local, and
  the profile placement stacks two near-identical lines. Slots 5 and 6 are
  unaffected. The philosophy batch of the S10 migration completed 29 Jul
  2026; the itinerary half ran 28 Jul.
- Sticky nav: masthead and TOC MUST be wrapped together in a single sticky
  container (position:sticky; top:0; z-index:100). Never separate.
- TOC IS ONE ROW AT EVERY WIDTH (mandatory, DG v1.8, adopted 29 Jul 2026).
  The single-row horizontal scroller lives in the BASE rule, never in a
  max-width media query: flex-wrap:nowrap; overflow-x:auto;
  overscroll-behavior-x:contain; -webkit-overflow-scrolling:touch;
  scrollbar-width:none — plus .toc-bar::-webkit-scrollbar{display:none} at
  base. The child selector needs white-space:nowrap ONLY; no flex
  declarations. THE SCROLLBAR STAYS HIDDEN on desktop too (owner ruling,
  29 Jul) — do NOT add a @media (pointer:fine) scrollbar to individual
  files. REASONING: the TOC is inside the sticky unit, so a wrapped row
  costs viewport on every screen of the document, permanently. A TOC that
  wraps is LATENT until someone adds a section — the Karoo file was fine
  at 17 buttons and became a three-row header at 19. If a row gets
  tediously long, SHORTEN THE LABELS (the section h2 carries the full
  title), do not widen the bar.
- Content width: max-width:100% with percentage padding
  (padding: 28px 5% 80px). NEVER a fixed pixel max-width cap. Headers,
  footers, sticky nav always full browser width.
- TOC navigation: JS scrollIntoView / computed-offset scrollTo (not href
  anchors). Scroll offset must account for full sticky container height.
  IntersectionObserver for active-state highlighting.
- TOC INDEX CONTRACT (mandatory, adopted 22 Jul 2026 from OI-38;
  CORRECTED the same day after the first version of this rule caused three
  regressions). THE CONTRACT HAS THREE PARTS, NOT TWO: the sectionIds
  array, the TOC buttons, AND THE TOGGLE EXPRESSION that compares them.
  Read the toggle in the file before judging the pairing — do not assume it.
  Under toggle i===idx the array must mirror the TOC buttons exactly (same
  entries, same order, same length, including any alerts and changelog
  entries). Under toggle (i-1)===idx the array must be the TOC list MINUS
  its leading button, and an array that looks "short" is correct. Both
  conventions existed in this library and a rule written from one of them
  broke three files built on the other. STANDARDISED 22 Jul 2026: all ten
  profiles and the template now use i===idx with a 1:1 array, and any new
  profile inherits that from the template. If a future file is found on the
  offset convention, normalise it rather than adjusting the array. A mismatch does not throw; it silently highlights
  the wrong button, so it will never be caught by node --check or a depth
  walk. Two mirror-image failures were found in the profile family: an
  'alerts' id in the array with no button (003), and an "! Alerts" button
  absent from the array (006 and the TEMPLATE). ADD TO THE STANDARD
  VALIDATION SET: assert len(sectionIds) == len(tocLinks), assert every
  sectionIds entry resolves to a real element id, and verify the pairing in
  order. Whenever a section is added or removed, both lists change together.
  HOW TO EXTRACT THE TOC SIDE (learned the hard way, 22 Jul 2026 — two
  extractor versions gave false readings before the third was trusted):
  compare sectionIds against the getElementById TARGETS taken from every
  element carrying class="toc-link". Do NOT compare against button LABELS;
  do NOT assume the element is an <a>, because 009 Zimbabwe uses
  <span class="toc-link" onclick=...> and read as zero buttons; and do NOT
  anchor the pattern on attribute ORDER, because a class-first regex misses
  every file where class is the last attribute and will report most of the
  family as broken. The safe form is: find all tags matching
  <[a-zA-Z]+[^>]*class="toc-link"[^>]*>, then pull getElementById('X') from
  each tag. Both false readings were caught before being reported — working
  convention (d) doing exactly what it exists for.
- Footer convention (post-standardisation, 8 Jul 2026):
  · Reference docs, profiles (body), philosophy, itinerary: NO footer.
  · Profiles: single-line sources-attribution footer only.
  · Calculators: functional footer RETAINED (live JS readout values).
  · Audit reports: provenance footer RETAINED (governance).
  · index.html: minimal brand line RETAINED.
  · Do NOT add footers to new documents unless they carry genuinely unique
    functional content.
- v1.7 mobile PWA pattern (MANDATORY): html
  {overflow-x:hidden; overflow-x:clip; overscroll-behavior-x:none} AND body
  {overflow-x:clip; overscroll-behavior-x:none} — body must use CLIP, never
  HIDDEN. CONFORMS: entire library (62 in-library files, 11 Jul 2026;
  WORKSTREAM CLOSED). Also: a ::before slab on the sticky-nav unit
  (bottom:100%; height:100vh) with an opaque background; safe-area-inset-top
  padding; overscroll-behavior-x:contain on inner horizontal scrollers; a
  print guard resetting the sticky unit to position:static and suppressing the
  ::before slab.
- Print CSS: Design Guidelines Section 16 print standard — 7mm page margins,
  7.5pt body, no forced page breaks by default, break-inside:avoid on small
  elements only, break-inside:auto + thead repeat on large tables (15+ rows).
  ** THE 7.5pt BODY RULE HAS NEVER TAKEN EFFECT ANYWHERE — see OI-60. Sizes in
  this library are declared in rem, which resolves against the ROOT element;
  the print blocks set BODY. Diagnostic 29 Jul 2026: zero of 71 files set the
  root size in print, 1,547 rem declarations sit in base CSS, and printed pages
  have been coming out at screen size throughout. The remedy is a single root
  declaration inside @media print plus rem heading overrides that COMPRESS the
  hierarchy — prototyped on LC76_How_To_Use_This_Library.html (knob 11.6px =
  8pt body, owner-ruled off a real printout after 12pt was trialled and
  rejected). DG §16 must be corrected BEFORE any propagation; until it is,
  §16 specifies a form that cannot work. DO NOT record a file as §16-compliant
  on the strength of the rule being present. **
  Firefox: redefine CSS custom properties as hex with !important inside
  @media print — var(--token) does not resolve reliably in Firefox print.
- Calculator shared files (embedded in other pages) must have the index nav
  button removed so they remain standalone-shareable.
- 60-30-10 colour rule and WCAG AA compliance apply throughout.
- index.html section layout: situation-based, eight sections (see INDEX
  LAYOUT block above). h3.sec-sub sub-group headers (mono, uppercase,
  amber left rule) are index-only; they carry NO ids so the h2[id]
  IntersectionObserver contract is untouched.
- index.html section colour tokens (21 Jul 2026, index-only):
  --sec-emergency #B91C1C · --sec-find #e07800 · --sec-tools #3E6B4F ·
  --sec-vehicle #2a2f3a · --sec-expedition #6B6B3A · --sec-profiles
  #8A4B2A · --sec-philosophy #6E5B46 · --sec-governance #6a7080.
  Red = Emergency ONLY (full headers, white titles); all other sections
  border/eyebrow/underline accents only. The old per-card tool/green
  header treatment and the --green-field token are RETIRED.

OWNER VALUES
- Safety, simplicity, reliability. "Eco not ego."
- Direct, action-oriented writing — no unnecessary jargon.
- Field documents must print well and be readable in harsh conditions.
- No over-engineered solutions — good quality, cost-effective, appropriate.
- Ground tents (OzTent RV4), no sleeping in vehicle, no trailer.
- No-trace camp doctrine (standing, 20 Jul 2026): pack-out sanitation and
  raised fire pan + ground sheet on no-dig/sensitive terrain; ash packed
  out on sensitive ground. Doctrine R17 §5 / R42 §4; kit R11; consumables
  via EX-8.
- Vehicle also used as daily run-around at home.

WORKING CONVENTIONS
- Ownership-first propagation (adopted 22 Jul 2026, from the content-audit
  seam pattern — the sharpest of the three structural findings). Before
  editing any topic, look it up in R30 and identify the OWNING document.
  Edit the owner FIRST, then every document that delegates to it, in the
  SAME session — never the reverse. R42-then-R10 is the failure mode that
  produced F38 (R42 corrected while R10 §3, which R42 delegates to, still
  carried superseded fire doctrine); R10-then-R42 would not have produced
  it. SESSION-CLOSE COMPLETENESS ASSERTION: for each topic touched, list
  the documents that delegate to it and assert each was visited, or
  explicitly deferred to a numbered open item — same control class as the
  COUNTIF baseline on the working papers. An R30 OWNER-flag column, which
  would convert this discipline from an act of vigilance into a lookup, is
  approved in principle and DEFERRED until after the Karoo loop.
- Report only what was actually executed (standing, 21 Jul 2026, ninth
  session). Claude states what it has ACTUALLY executed in the current
  session, verified against tool output, and never reports a session or step
  as complete from expectation (the ninth session opened with a prior turn
  wrongly asserting completed work that had not been done).
- Status-bearing content carries provenance (standing, adopted 29 Jul 2026,
  from OI-67). Any array, table or list in this library that asserts a live
  STATUS — outstanding actions, monitors, flags, "no record" claims — must
  carry a source field naming where each entry came from (manual, service
  history, owning R-doc, workshop advisory). A list without provenance cannot
  be audited, therefore is not maintained, therefore drifts — the Service
  Checklist's OUTSTANDING array carried two items that had been settled weeks
  earlier, on a document with a print-and-take-to-the-workshop function. The
  convention already existed one array away in the same file. COROLLARY FOR
  RECONCILIATION: a completeness gate must sweep for status-bearing content
  WHEREVER IT LIVES, not only in instruments that look like registers. The
  29 Jul gate checked spreadsheets and audit reports, declared the register
  substantially complete, and missed both R8 and its supplement.
- Numeric traceability, self-audit, and verify-before-you-report (adopted
  22 Jul 2026 from the content-audit programme close). (a) Any specific
  numeric specification introduced into a library document must be
  traceable to Facts, the owning R-doc, or a manual — a plausible number
  reads as authority and is the most dangerous thing an assistant can add
  to a field reference (F65, a self-introduced defect). (b) A self-audit
  of prior-session Claude edits is a standing step in every audit session
  — it is the only control that caught F65. (c) A candidate finding is
  VERIFIED before it is written down; a false finding costs the owner's
  trust in the whole register (F74's dropped candidate). (d) Completeness
  checklists are written BEFORE the file is opened, never after.
- Experimental / pre-EX-9 route planning (adopted 13 Jul 2026): the expedition
  route is developed as an EXPERIMENTAL DRAFT that lives OUTSIDE the library
  until EX-9 ratifies it — kept in ordinary chats, NOT the project, so a fluid
  draft is never mistaken for committed fact. Source of truth = the working
  brief (LC76_Experimental_Route_OptionA_WORKING.docx / .md); the owner edits
  in place, and a "CHANGES THIS ROUND" list at the top of the doc is the
  reconciliation signal on re-upload (read it first, reconcile the body against
  it). The route MAP (…_MAP.html or the inline map tool) is a VIEW re-rendered
  from the brief on demand, NOT a persistent document — feedback goes to the
  brief, the map is redrawn (the inline map tool's "Day 1–6" labels are its
  fixed vocabulary; the HTML file says "Phase" and is the reference version).
  Iteration loop: edit brief in chat → when a version stabilises it becomes the
  EX-9 seasonal/route input → EX-9 ratifies → ONLY THEN propagate into the
  library (R38 §4, R39 §1, profile route framing). Nothing route-related commits
  to GitHub / the library pre-EX-9. These experimental artifacts are NOT library
  files, NOT precached, NEVER trigger a CACHE bump. Tracked as OI-26. Word docs
  are heavier to round-trip than markdown/txt — for fast iteration markdown or
  plain txt is the smoother medium; use Word only when the owner prefers the
  editing feel.
- Changelog discipline (owner ruling, 22 Jul 2026). (a) EVERY masthead
  "Last updated" bump carries a matching changelog entry — the masthead is
  the claim, the changelog is the evidence, and a claim without evidence is
  the defect class the content audit spent five sessions removing. This
  applies to silent fixes too: "TOC highlighting corrected, no content
  changed" is a valid entry. EXEMPTION, ADOPTED 29 JUL 2026 (owner ruling):
  rule (a) does NOT apply to non-profile R-docs, which carry no changelog
  section. The EVIDENCE INSTRUMENT FOR THAT CLASS IS NAMED INSTEAD —
  LC76_Session_History.txt and LC76_Library_Register.txt, which record what
  changed, when and why, per session. The principle is unchanged: a masthead
  claim still requires evidence; the evidence lives centrally rather than in
  the file. THE ALTERNATIVE WAS MEASURED AND REJECTED: adding changelog
  sections to ~40 R-docs would touch the three-part TOC index contract in
  every one of them — the structure that produced three regressions on
  22 Jul — and the back-catalogue could not be populated without
  reconstructing history, which doctrine forbids; each file would have
  gained a changelog whose only entry recorded its own creation. Profiles
  keep rule (a) because they are perishable, country-specific and read
  standalone by someone deciding whether a fee is current. THIS IS AN
  EXEMPTION, NOT AN OVERSIGHT — recorded so a later session does not "fix"
  it. (b) Every profile carries a changelog.
  (c) Changelog dates are ISO (2026-07-22) — sortable and unambiguous.
  (d) The content-currency badge ("Compiled 7 Jul 2026") is NOT a changelog
  date and is never normalised or bumped with these edits — it is
  document-level provenance and has its own convention.
  STRUCTURAL DIVERGENCE, UNRESOLVED (OI-39): 001–008 carry the changelog as
  an UNNUMBERED section (id="changelog") with .changelog-entry grid rows;
  009 and 010 carry it as the FINAL NUMBERED SECTION (§19 / §20) as a
  revision table. Both are valid and both now satisfy (a)–(c). Harmonising
  would renumber two files a second time, so it is parked for EX-10.
  PROBE WARNING: testing for id="changelog" reports 009 and 010 as having
  NO changelog. They have one. Test for a changelog SECTION by title, not
  by that id.
- Masthead migration & the 80k rule: a masthead-only migration is a 3–4
  string anchored str.replace + whole-file validation walk — safe at any file
  size, EXEMPT from the "80k+ = own-session" rule (owner, 7 Jul 2026). The
  own-session rule still applies to heavy/rebuild edits of large files.
- File naming: LC76_Title_Case.html. No legacy patterns (hzj76r-, 1hz-,
  service-history-).
- Bar is the primary pressure unit on all displays; PSI is derived/secondary.
- Weights in kg, distances in km, pressures in bar (PSI shown alongside).
- R-numbering is authoritative — cross-check LC76_Library_Register.txt before
  building index updates; never assume the project file set reflects the live
  GitHub state.
- Index card placement (adopted 21 Jul 2026): new library files are placed
  on index.html by SITUATION per the INDEX LAYOUT block — not appended to
  a build-order grid. Always inspect the actual markup of the adjacent card
  before inserting.
- Service ledger cost conventions (10 Jul 2026, EX-12 S1): cost 0 = child line
  item of the parent invoice row immediately above (parent carries the full
  amount) — child rows EXCLUDED from per-item averaging, never double-counted.
  No cost=1 placeholders. Classification is rule-based (see EX-12), not
  per-record tagged. Blank-cost milestone rows render as em-dash, contribute
  nothing to totals.
- Service entry workflow (EX-12 S3): new entries made in
  LC_Service_history_FINAL.xlsx FIRST (always ≥200,000 km era), then
  LC76_Service_Ledger_Current.html refreshed by a data-only edit of its
  embedded JSON const + §3 era totals; the R8 dashboard aggregates refreshed
  the same session if numbers move materially. The Archive ledger is a closed
  era — never touched.
- For large files (R4, R8, and any 80k+ HTML): start a fresh chat, upload the
  live HTML from GitHub, provide a change spec, execute surgical edits. Never
  rebuild from project-knowledge fragments.
- For structural or cross-file changes: propose first, get approval, then
  build. Flag the full required change map when a literal edit would create
  internal contradictions elsewhere in the document.
- Before batch-editing a UI element across files, ask Neil to identify a
  confirmed working file as the reference template. Never conclude an element
  is absent without careful full-file inspection.
- Update index.html AND LC76_Library_Register.txt when adding new documents.
- Open Items Register (standing, adopted 12 Jul 2026): LC76_Open_Items_
  Register.txt is the SINGLE authoritative list of everything open. Every
  session close: update it (close items with date, add new findings, re-date
  the header) and present it alongside the updated Project Instructions. Closed
  items move to the CLOSED LOG (never deleted). The header narrative records
  what happened; the Open Items Register records what is still open — do not
  duplicate open-item detail here beyond a pointer. Governance file: GitHub +
  project knowledge; NOT a library file, NOT precached, NEVER a CACHE bump.
- Project Instructions upkeep (standing): whenever a session changes anything
  recorded in this file, Claude updates it to match and PRESENTS the full
  updated text as a deliverable for the owner to paste into the project field
  and commit to GitHub. Do this automatically, every time, and bump the
  "Last updated" header to the session date.
- Service-worker cache versioning (Claude-owned): whenever a library file is
  ADDED, REMOVED, or RENAMED, Claude bumps the CACHE constant in sw.js. Plain
  edits self-refresh via background revalidation and do NOT require a bump.
  CLARIFIED 21 Jul 2026 (audit Gate B, evidence-based): GOVERNANCE files —
  audit HTML reports, the Open Items Register, working papers — are
  index-linked but NEVER precached in sw.js and NEVER trigger a CACHE bump,
  even when newly added. Verified against sw.js: zero references to either
  prior audit HTML or the OIR. The bump rule applies to LIBRARY files only.
  Current CACHE constant: lc76-library-v26 (bumped 30 Jul 2026 — the Open Items
  Dashboard was wired and precached, Session 6). PRIOR NOTE 29 Jul 2026: LC76_Open_
  Items_Dashboard.html is on GitHub but is NOT a precached library file —
  it is deliberately unwired (owner ruling) and DID NOT trigger a bump.
  The bump to v26 belongs to the session that wires it. (v25 bumped
  29 Jul 2026 — the user guide LC76_How_To_Use_This_Library.html ADDED. Prior: v24, 21 Jul 2026 — R42 Fire
  Safety Guide added AND R35 renamed to LC76_Security_Scams_Guide.html;
  the later 21 Jul scam-expansion and index-restructure sessions, and the
  22 Jul decision session, were content-only and did not bump it).
- Automatic date bumps: on every edited document that carries a masthead
  "Last updated" line, bump it to the session date. Bump index.html's
  header-meta "Last Updated" whenever any library file changes.
- Monthly odometer update: if a session opens in a NEW calendar month and no
  odometer reading has been logged, ask Neil for the current reading before
  proceeding; then update LC76_Facts.txt §2 and propagate current-odometer
  references library-wide (NEVER touch historical event mileages). Current
  reading: 311,000 km (read Jul 2026).
- All country/terrain profiles are on GitHub (incl. Angola). The register's
  "NOT ON GITHUB" section is empty — nothing is intentionally excluded.
- Chrome extension get_page_content is unreliable — prefer web_fetch with a
  direct URL, or ask Neil to paste the URL.
- Supplier naming discipline: forward-looking check/action items must use
  generic terms ("the workshop," "a diesel/turbo workshop"). Historical
  service records may retain supplier names. Dirk Du Plessis / Dup Diesel
  is no longer the preferred turbo/fuel-system workshop.
- MASTERSHIP MAP — which instrument is master of what (standing, owner
  ruling 29 Jul 2026). Three instruments were each behaving as master,
  which is how the working papers came to carry RA-4 as "OPEN — owner-
  gated" five days after the ruling that closed it. There is now ONE
  master per fact class:
    LC76_Open_Items_Register.txt   MASTER OF STATUS — what is open, who
                                   owns it, what closes it, on what
                                   evidence. The database everything
                                   else feeds.
    LC_Service_history_FINAL.xlsx  MASTER OF WORK DONE.
      · ADVISORIES sheet           CAPTURE POINT for workshop-flagged
                                   findings; feeds the register, never
                                   competes with it.
    LC76_Facts.txt                 MASTER OF VEHICLE FIGURES.
    Working papers .xlsx           AUDIT EVIDENCE — method, locator,
                                   external source. Feeds the register.
                                   NOT a parallel open-items list.
    LC76_Service_Checklist.html    MASTER OF THE PRESCRIBED SCHEDULE.
    LC76_Library_Register.txt      MASTER OF WHAT EXISTS.
    R-documents                    MASTER OF DOCTRINE in their topic.
  ONE MASTER, SEVERAL FEEDERS, NONE ARGUING. THIS MAP MUST BE VISIBLE,
  NOT MERELY RECORDED — it is carried here, in the Library Register, and
  it is a Session 0 action to surface it in LC76_How_To_Use_This_Library.
  html. A governance rule the owner cannot see is not governing anything.

- The register is a database with a schema (standing, adopted 29 Jul
  2026). Every open entry carries, in fixed order: the technical title;
  a «WHAT:» line in plain English with no codes; OWNER · GATE · SOURCE;
  and a STATUS line standing alone. The WHAT line is not decoration —
  register titles are written in project shorthand (RA-, EX-, TA-, R-
  numbers, § refs) which is efficient for whoever wrote them and opaque
  to everyone else, INCLUDING THE SECOND CREW MEMBER ON WHOM RA-8 SOLO
  CAPABILITY DEPENDS. A SCHEMA IS ONLY REAL IF SOMETHING REJECTS
  VIOLATIONS: a session-close validator that parses every entry and
  fails loudly on non-conformance is a Session 5 deliverable, and until
  it exists the schema is a wish, not a control.

- A source is a place, not a moment (standing, adopted 29 Jul 2026).
  Every open item names a file plus a locator someone can open —
  document and section, sheet and row, a commit, a supplier reference,
  a physical artifact. "Consultant review 8 Jul" is not a source; the
  conversation no longer exists. MEASURED AT ADOPTION: 26 of 40 entries
  failed this, 13 passed, 1 had no source at all. Those items were not
  badly recorded — they were BORN WITHOUT EVIDENCE, in conversation,
  because the assistant-proposal origination stream has no capture point.

- Evidence grades apply at ORIGINATION as well as closure (standing,
  29 Jul 2026). E1 physical artifact · E2 system-verifiable measurement ·
  E3 third-party written confirmation · E4 recorded owner ruling ·
  E5 assistant narrative or recollection. E5 IS NOT EVIDENCE, AND THAT
  INCLUDES CLAUDE'S OWN REPORT OF ITS OWN WORK: "validation clean, depth
  walks 0/0/0" is E2 only if the script and its output are recorded and
  re-runnable, otherwise it is an assertion. Full definitions and the
  per-item-type closure tests: LC76_Audit_of_Truth_Charter.txt §8–§9.

- Governance files delivered whole, never as patches (standing, 26 Jul
  2026). Governance .txt files (PI, OIR, Facts, Register) are delivered as
  COMPLETE FILES, never as patch/diff documents for the owner to hand-apply —
  a committed patch file is an orphan of the OI-32/OI-42 class and updates
  neither the GitHub file nor the PI field. For a large file the protection
  against silent drift is anchored programmatic str.replace with count==1
  asserts plus a before/after structural reconciliation, not hand-application.
- Roll-forward reconciliation on governance files (standing, adopted
  24 Jul 2026). The validation pipeline was built from HTML defects and is
  made of HTML instruments; the governance .txt files — the Open Items
  Register, Facts, the Library Register, this file — are spliced by the
  same str.replace technique and had NO structural gate at all. A
  duplicated "CLOSED LOG" header survived two commits because depth walks
  and node --check cannot see it. The fix is deliberately NOT a checklist:
  a checklist only catches the failure modes someone anticipated, and
  nobody anticipated that one. Instead, reconcile the file the way a
  ledger is reconciled — opening balance, declared movements, closing
  balance, and anything unexplained is a finding.
  METHOD: before editing, snapshot the file's structural invariants; state
  the intended delta; recompute after; assert the difference equals
  exactly what was declared. For the OIR the invariants are the multiset
  of section headers, the set of OI IDs with the section each occupies,
  the count of structural headers, and the closed-log entry count. For
  Facts: section numbers unique and sequential, plus the authoritative
  figures (odometer, GVM, kerb, payload) extracted before and after — an
  UNDECLARED change to a payload figure is exactly what should trip an
  alarm while those figures remain PROVISIONAL.
  CROSS-FILE CHECK, run at session close: assert this file's "next free"
  number equals the register's maximum OI plus one. MEASURE ITEM
  DEFINITIONS, NOT MENTIONS — match line-start "OI-nn" followed by a
  title, because closed-log prose routinely names numbers ("next free is
  now OI-46") and a naive scan reads those as items and reports a false
  failure. This probe did exactly that on its first run, 24 Jul 2026, and
  the file was correct. Same class as the id="changelog" probe error at
  OI-38: when a probe disagrees with the file, suspect the probe first. Two sessions both
  believing the same number is free is a collision no single-file gate can
  see.
  SCOPE THE PI SIDE TOO (added 28 Jul 2026, from a probe error made while
  building the status gate below). The authoritative "next free" is the one
  in the CURRENT session header block. This file ALSO carries historical
  "next free OI-nn" strings inside every archived session block, so a probe
  taking the LAST match in the file reads an old number and reports a false
  mismatch — it read OI-48 against a correct OI-54. Scope the extraction to
  the header block, not the whole file. Third probe error in one session,
  all three caught before being reported as file defects.
  SECOND CROSS-FILE CHECK — STATUS, NOT JUST NUMBERING (standing, adopted
  28 Jul 2026, from a live defect). The check above compares NUMBERING
  only. It cannot see a narrative in THIS file asserting an item is OPEN
  when the register records it CLOSED, and that gap shipped: the
  nineteenth-session block said OI-32/OI-42 "stay OPEN until the owner
  confirms the files are gone" when both had been closed the same day with
  HTTP 404 verification on every file. The claim survived a session and was
  then reported to the owner as live work outstanding. AT SESSION CLOSE:
  extract every OI-nn in this file's HEADER NARRATIVE carrying an ATTACHED
  status claim, and assert none appears in the register's closed log.
  WRITE THE PROBE TIGHT OR IT WILL CRY WOLF. A proximity window — "OI-nn
  within N characters of OPEN or pending" — over-reported by 100% on its
  first run, matching "OI-50 OPENED & CLOSED" and an unrelated "PENDING"
  about an index.html card two sentences away. The status claim must be
  ATTACHED to the reference — OI-nn[/OI-mm] (stays|remains|is|are)
  (OPEN|pending|unresolved) — not merely near it. Same lesson as the
  id="changelog" and next-free probes: when a probe disagrees with the
  file, suspect the probe first.
  AND NOTE WHAT CAUSED THE DIVERGENCE. The register is authoritative for
  what is OPEN; this file's narrative records WHAT HAPPENED. Where they
  disagree on STATUS, THE REGISTER WINS. The divergence exists because
  open-item DETAIL was duplicated into the narrative — which is exactly
  what the standing convention forbids: keep a POINTER here, not the
  detail. A duplicated fact is a seam that reports no error.
  LIMITATION, STATED SO THE GATE DOES NOT CREATE FALSE COMFORT: this
  catches SHAPE, NOT TRUTH. A well-formed entry containing an invented
  number passes every one of these assertions. The character-count error
  of 24 Jul was caught by working convention (d), verify-before-you-report,
  and not by any structural gate. They are different controls doing
  different jobs and neither substitutes for the other.
- Idempotence, not just anchors (standing, adopted 24 Jul 2026, from a live
  defect). `assert count == 1` proves the ANCHOR is unique; it proves
  nothing about whether the edit has already been applied. When a
  replacement CONTAINS its own anchor — the common case for "keep the old
  block and add a new one after it" — the guard passes on every re-run and
  silently duplicates the insertion. This happened on R32 on 24 Jul: a
  script re-run after two unrelated anchor failures applied the same block
  twice. Any script that might be re-run must ALSO assert that a
  distinctive string from the NEW content is ABSENT before writing, and a
  post-edit count of that string should be 1. Recovery is a SHA-pinned
  re-fetch and a single clean application.
- Encode before you truncate (standing, adopted 24 Jul 2026, from a live
  defect). Never write an edited file with open(path,'w') — that truncates
  the target the instant it is called, so any later failure (a bad encode, a
  lone surrogate in an emoji, an exception mid-write) destroys the file
  before a single validation gate has run. Encode the full string to bytes
  FIRST, confirm that succeeded, then write binary. The validation pipeline
  runs after the write and therefore cannot protect against this class at
  all; only the write pattern can. Recovery is a single SHA-pinned fetch,
  which is one more reason pinning is not optional.
- Emergency number format (standing, adopted 23 Jul 2026, first applied
  24 Jul). Every emergency or assistance number in the library is stored and
  printed in FULL INTERNATIONAL FORMAT (+country code). Where only a national
  short-dial number exists (SA 082 911, 084 124), it is labelled explicitly as
  short-dial / in-country-only, and it must NEVER appear in a satellite-comms
  activation instruction without that label — a short-dial code cannot be routed
  by Iridium or Inmarsat, so the instruction cannot execute in exactly the
  scenario where the satphone is the only channel. Numbers are NEVER converted
  to international format by inference: an unverified international equivalent
  is a fabricated number in a life-safety document (F65 class) and must be
  obtained from the provider. A number that is provider-published but has never
  been DIALLED on the handset that will dial it is recorded as such and carries
  a test-dial action (RA-10) — published is not the same as proven.
- Share-call codes never route by satellite (standing, 25 Jul 2026). A SA
  national share-call number (prefix 0860 / 0861) and the three-digit /
  short-dial codes (112 / 082 911 / 084 124) work only on an in-country
  mobile network. They CANNOT be routed by Iridium or Inmarsat, and
  prepending +27 to a share-call number does NOT create a dialable form
  (the rejected +27 860 555 505 was exactly this error). Where a provider's
  only published emergency line is share-call, that is recorded as a GAP
  with an action to obtain a geographic (+27 landline) equivalent — never
  papered over with a fabricated international form.
- A provider named in the library is not a provider the crew is a MEMBER of
  (standing, 25 Jul 2026). A medevac or assistance number must state the
  membership/cover position beside it. A number the crew cannot claim
  against is an own-cost line and is labelled as such (Netcare 911, demoted
  25 Jul) — otherwise it reads as cover it is not. R29 remains the
  provider-evaluation guide; naming a provider there is comparison, not a
  decision, until one is chosen (OI-43).
- Controlled Duplication Register (standing, adopted 23 Jul 2026, seeded
  24 Jul). A short list, held in the Open Items Register, of facts permitted to
  exist in more than one document, each with ONE named OWNER and a mandatory
  PROPAGATION LIST. Any change to a registered fact triggers a sweep of that
  entire list in the SAME session — not a follow-up item. Seed rows: emergency
  numbers (OWNER R29 §6 → R10, R1, R16, R38); SOS activation steps (OWNER R29 §7
  → R10, R16); GVM/kerb/payload (OWNER Facts → R11, R12, R29, R28); licence &
  disc dates (OWNER Facts → R29, R39); daily house load in Ah/day (OWNER R5
  breakdown table → R7, LiFePO4 Calculator — added 26 Jul 2026 BECAUSE IT HAD
  ALREADY FAILED: 77 Ah/day stood in three documents while two of those
  documents' own component breakdowns summed to 65.3. Any change sweeps the
  list in the SAME session, INCLUDING the derived values it drives — daily
  DoD %, wild-camp autonomy, 30%-alarm reserve hours, solar-deficit
  arithmetic). This is the same move as the R30
  owner-flag column, and it converts a latent seam into a lookup.
  EMERGENCY-NUMBERS ROW, STATE AT 25 JUL 2026: the fact grew a PERSONAL-
  COVER set (Genesis/Neil, Bonitas-AZOZA/Mary-Ann, Santam) and Netcare 911
  was DEMOTED to a non-member own-cost line; both changes were swept across
  the whole propagation list in the same session, R29 §6 first. R38 carries
  no numbers and needs no edit. Two rejected numbers (+27 11 991 8000,
  +27 860 555 505) live in the R29 §6 provenance note so they are not
  re-introduced; satphone-dialable Santam/Genesis equivalents are OI-47.
- Two-tier content placement — pattern vs incident (standing, 21 Jul 2026).
  Durable PATTERNS live in the topic reference doc; location-dated,
  perishable INCIDENTS live in the relevant country/terrain profile (the R35
  scam-pattern vs profile-incident split; mirrors R14/R40 strategy-vs-spanner
  and R14/TPMS strategy-vs-tool). Applies to any perishable, dated,
  place-specific observation.
- Section renumbering is decided by MEASUREMENT, never by rule (standing,
  24 Jul 2026). Before inserting a section into an established document, sweep
  the corpus for external §-references and anchor links into that document. Zero
  references → insert and renumber (the OI-36 profile case). References exist →
  append and point (the R35 §12 and R29 §13 cases). Both outcomes arose from the
  same test two days apart. Neither "always append" nor "always renumber" is a
  safe default; the sweep is cheap and settles it.
- Project-knowledge currency (standing, adopted 27 Jul 2026, from OI-49).
  Project knowledge carries NO duplicate of a governance file that lives on
  GitHub. Facts, the Library Register and the Project Instructions were all
  found there five to six weeks stale, the PI fossil at 7% the size of the
  real file, and were removed. If such a copy is ever added back, it is
  refreshed in the SAME session as the GitHub edit or it is deleted — never
  left to age. THE REASONING IS THE GENERAL ONE THIS LIBRARY KEEPS
  RELEARNING: a stale duplicate is a seam that reports no error. Absence is
  safer than staleness, because absence forces the correct behaviour (fetch
  live, SHA-pinned) while a wrong copy invites the incorrect one. NOTE THE
  DISTINCTION that makes this safe: the Project Instructions FIELD is a
  different mechanism from a project-knowledge FILE — the field is injected
  automatically into every session and is what actually governs behaviour,
  so it must be kept current; the file was pure duplication and is gone.
- Archives are not reconstructed, and a redacted archive says so
  (standing; promoted 29 Jul 2026 from the twentieth/nineteenth-session
  narratives ahead of their archiving). The Open Items Register and
  LC76_Session_History.txt are ARCHIVES. Their narrative is never altered,
  removed or rewritten after the fact — a correction is an ANNOTATION
  against the original sentence, not a replacement of it, because a
  reconstructed record reads as contemporaneous when it is not (the F65
  class applied to governance). TWO COROLLARIES. (i) Withdrawing a VALUE is
  a different act from altering a RECORD, but a reader who finds a pointer
  where a number used to be must be told why: an archive that has been
  redacted carries a DATED REDACTION NOTICE naming the class of data
  withdrawn, stating that no narrative was altered, and noting that prior
  commits still hold the originals. Otherwise the redaction itself becomes
  the invisible seam. (ii) A missing session is recorded as a LABELLED,
  DATED GAP and sessions are NEVER RENUMBERED to close it — renumbering
  would silently re-point every existing cross-reference to the wrong
  ordinal.
- Precedence between the two narrative files (standing; promoted 29 Jul
  2026). LC76_Project_Instructions.txt is authoritative for all doctrine
  and working conventions. WHERE THIS FILE AND LC76_Session_History.txt
  DISAGREE, THIS FILE WINS. Nothing in the history file is a live rule.
  Note this sits ALONGSIDE, and does not contradict, the register rule
  above: the Open Items Register wins on an item's STATUS; this file wins
  over the history file on NARRATIVE.
- The cover / medical-scheme cluster is Class P — RETAIN (standing ruling,
  28 Jul 2026; promoted 29 Jul 2026). The insurer and both medical schemes
  appear in ~230 instances across the R29/R10/R1/R16 emergency stack and
  are IDENTIFYING, but they are deliberately NOT redacted: a paramedic or a
  bystander holding the laminated R1 card needs to know which scheme to
  claim against, and stripping the cluster would trade a nuisance call
  against a delayed evacuation. The residual is controlled BEHAVIOURALLY,
  in R35's standing habits, not by redaction. A future security pass must
  not re-litigate this as an oversight. NOTE THE METHOD, which generalises:
  this cluster was provisionally graded Class R (redact) in the opening
  review and regraded only after MEASURING what the strings do — a
  classification made before measurement is a hypothesis.
- A hard 404 on the repo is a working-method BLOCKER, not a transient
  (standing, 26/27 Jul 2026; promoted 29 Jul 2026). If github.com/
  Overlandslabs/Consumption returns a hard 404 on the repo page, BOTH
  branches of raw content and the Atom feed, while the org page loads
  normally, that is GitHub's mask for private/renamed/deleted — not the
  familiar unauthenticated-API 403. THE CORRECT BEHAVIOUR IS TO STOP, not
  to fall back on project-knowledge or recalled copies and carry on: a
  session that cannot fetch SHA-pinned cannot edit. Resume only when
  access returns, re-pinned.
- Symptom triage for "the site looks old" (standing; promoted 29 Jul 2026).
  Three layers fail differently and are routinely confused. Stale content
  in the owner's browser while the repo is current = the PWA
  SERVICE-WORKER CACHE serving offline-first. A 404 in a FRESH browser
  while the SHA-pinned raw source returns 200 = the GITHUB PAGES
  DEPLOYMENT layer. A 404 on raw content and the Atom feed = the REPO
  itself (see the blocker rule above). Diagnose which layer before
  proposing any fix; a CACHE bump does not repair a Pages deployment and a
  redeploy does not clear a service worker.
- Publish trips retrospectively (standing, adopted 28 Jul 2026, Gate C of
  the Karoo date-exposure pass). A trip itinerary published BEFORE or
  DURING the trip carries RELATIVE DAY NUMBERING (Day 1, Day 2 …) or a
  season ("winter 2026"); exact calendar dates are held in the private
  annex and go into the document only once the crew is home. THE REASONING:
  a dated day-by-day schedule, set against a named crew, a distinctive
  vehicle and a known region, publishes precisely which nights a South
  African property stands empty and for how long. Note the scope — this is
  NOT a two-date problem. The Karoo itinerary carried 22 distinct date
  tokens across 55 occurrences; generalising only departure and return
  would have left every intermediate leg dated. Anyone scoping this class
  of edit as a two-string change has mis-scoped it.
  CRITICALLY, THIS IS A TEMPORARY SUPPRESSION WITH A DEFINED RESTORATION
  POINT, not a permanent redaction of the RC1 class. Dates are dangerous
  BEFORE the fact and harmless AFTER it, and afterwards they carry real
  documentary value — they are the record of a real trip and the basis of
  the R23 §7 post-trip review. Annex sections holding trip dates therefore
  carry a RESTORE-AFTER note, which is the OPPOSITE instruction from the
  permanent §1/§2/§3 and MUST NOT be copied from them by pattern. The
  restoration moment is the post-trip review session itself.

- Personal-data discipline (adopted 20 Jul 2026; EXTENDED AND PARTLY
  SUPERSEDED 28 Jul 2026 by the RC1 / name reduction pass). SA ID numbers
  and other personal identifiers NEVER appear in committed files — the
  repo is public. Library documents carry blank fill-in lines; completed
  versions (e.g. the affidavit print master) are held offline only, never
  committed.
  SUPERSESSION, 28 Jul 2026. The former rule read: "Vehicle identifiers
  (VIN, engine, registration) are already public-facing and may appear."
  That is now SPLIT, because it was being read to cover the registration
  CERTIFICATE as well as the vehicle:
  · STILL PUBLIC — VIN, engine serial, chassis code, the CURRENT
    registration plate, and the tare figure. Each is stamped on the
    vehicle, visible on it, or a technical weight with live RA-1 value.
  · NOW OFFLINE (PRIVATE ANNEX §1) — the registration-certificate
    fields: register number, series name as registered, vehicle
    description, first-licensed date, PREVIOUS licence number, and
    certificate issue date/office. None is visible on the vehicle; all
    exist only on a document a stranger should not hold; together with
    the registered owner's full legal name they corroborate a false
    provenance story.
  · FULL LEGAL NAMES (PRIVATE ANNEX §2) do not appear in committed
    files. FIRST NAMES are retained deliberately throughout the library
    — a first name is not a join key, and removing ~280 instances would
    cost readability for no security gain.
  REASONING, recorded so it is not re-litigated: individually each of
  these facts is minor. Assembled with a distinctive vehicle, a region
  and a dated absence window they form a usable package for registration
  fraud and absence targeting. The test applied to each fact was: does a
  reader in the field need this specific VALUE to do something, and what
  could a stranger do with it that he could not do without it.
  RESIDUAL EXPOSURE, ACCEPTED KNOWINGLY: the repo history is public and
  was NOT rewritten (owner ruling, 28 Jul 2026). A filter-repo rewrite
  would rewrite every SHA and break the SHA-pinning convention the whole
  working method rests on, for material that is either self-expiring or
  visible on the vehicle. Redaction is forward-looking only.
- A redaction record describes the withdrawn value, never reproduces it
  (standing, adopted 28 Jul 2026, from a live defect in the pass that
  created it). When a session records WHY a value was withdrawn, the
  narrative must name the CLASS of data ("an abbreviated initial-plus-
  surname form of the owner name"), never quote the string itself. The
  28 Jul RC1 pass wrote its own session block and closed-log entry quoting
  the abbreviated name form it had just removed, reintroducing the surname
  into two governance files at the moment they recorded its removal. The
  hazard had been named explicitly earlier in the same session for the
  register number and avoided there; it recurred in a different field.
  COROLLARY FOR THE SWEEP: the zero-assert stale sweep must run AFTER the
  governance narrative is written, not before — a sweep run before the
  session block exists cannot see what the session block reintroduces.

- GPS coordinate policy: individual rock art panel locations (Tsodilo Hills),
  specific Himba village coordinates, black rhino sighting locations, and
  Maasai boma locations must never appear in public-facing documents. Country
  profiles use public town / border / park-gateway coordinates only. (The
  experimental route map uses public park/town/border coordinates only.)

SESSION EXECUTION & VALIDATION (every HTML session)
- Fetch SHA-pinned (see SOURCE OF TRUTH below), then edit surgically. Diff
  the LIVE Project Instructions header at HEAD against any conversation-
  supplied copy BEFORE drafting a spec — a pasted PI is a project-knowledge-
  class copy, not the source of truth (lesson, 21 Jul 2026).
- Edits: Python str.replace with an anchor guard (assert count == 1) on every
  old string. No blind or global replacements.
- Validation gates before staging: div balance open == close (run a depth walk
  — final depth 0, min depth 0, no negative events — not just a tag count;
  STRIP all <script> blocks and HTML comments from the document before
  walking — convention adopted 12 Jul 2026);
  node --check on every inline <script>; stale-term sweep to zero (documented
  exemptions stand) — and the sweep MUST run WIDER than the change spec: the
  instances you list are not the instances that exist, so sweep the whole
  corpus for the term, not only the sites named in the change spec (18th
  session — a planned 18-anchor '77' sweep found five more, including a table
  total that would otherwise have shipped under a corrected headline);
  anchor/id uniqueness. For tag-balance repairs, walk spans
  too.
- Working papers (LC76_Library_Audit_WorkingPapers.xlsx): fetch live; edit
  append-only with openpyxl; NEVER run recalc.py; preserve the COUNTIF/COUNTIFS
  formula-cell baseline = 9 (Summary!B10–B14, B17–B20; owner-adjudicated
  12 Jul 2026, BASE-ADJ-1 in EX_Register). The baseline counts FORMULA CELLS,
  not substring occurrences (the retired substring metric of 13 double-counted
  two cells + a prose mention). Clone cell styles via copy.copy(src._style);
  verify testzip() == None; never load data_only=True.
- Known do-not-re-flag items: Service_Checklist has zero horizontal scrollers →
  vacuously compliant on the overscroll check; R5 carries two dead sticky
  selectors (.hdr and .masthead,.hero) from an earlier style swap — only the
  live .sticky-wrap is patched. CORRECTED 22 Jul 2026 — the note that
  Angola/Namibia profiles carry "legitimate negative depth-walk values from
  JS template strings" was a MISDIAGNOSIS and cost a real defect a month of
  invisibility. Both files carried genuine stray closing tags that survive
  script-stripping: Angola root-caused and fixed 12 Jul (now 126/126),
  Namibia found and fixed 22 Jul (was 156/157, now 167/167) — both from the
  same superseded multi-stripe masthead flag construct left behind when the
  SVG flag replaced it. STANDING RULE: a negative depth walk is treated as a
  real defect until root-caused in the static HTML with scripts and comments
  stripped. If a walk goes negative, find the tag. Facts §11 carries ONE intentional "later 2031" string
  — the 17 Jul supersession reference, exempt from the stale sweep. R28's
  "2,850" is the WHEELBASE in mm, not a stale GVM; the Fuel Log Analyser's is
  odometer data inside an embedded CSV; the Tyre Pressure Calculator offers
  both 265/70 and 265/75 by design (size selector, 265/70 marked "Current").
  All three were adjudicated as false positives in the Tier 5 sweep, 21 Jul
  2026 — do not re-raise them. The intentional hi-lift doctrine strings in R3
  and R9 (documented no-hi-lift doctrine statements) are stale-sweep exempt —
  a 'hi-lift' sweep will hit them and must not treat them as defects.
- Reconciliation discipline: sw.js / HTML fixes can land on GitHub outside a
  Claude session. Session start must diff live HEAD against this file's DEFECT
  FIX log and the CACHE version line, not just against the EX_Register. Commit-state is a claim like any other:
  when the owner reports files as committed, verify with a cache-busted raw
  fetch of the main-branch copy (byte count + expected markers) before editing
  against that HEAD — do not edit on a reported-but-unverified commit (14th
  session: reported-committed files had not landed).

MODEL ROUTING
- Opus 4.8 for: new document builds, structural or cross-file changes,
  large-file surgical edits, architecture or cross-reference work, all EX
  sessions, anything where an introduced error propagates quietly.
- Sonnet 4.6 for: established single-file updates with an unambiguous
  change spec, changelog entries, dual index + register updates.
- Haiku 4.5 for: quick lookups — R-number checks, Facts.txt queries,
  fast Q&A — only where correctness consequences are low and recoverable.

SOURCE OF TRUTH / PROJECT KNOWLEDGE POLICY
- GitHub is the single source of truth for all HTML documents. Project
  knowledge holds only the lightweight authoritative texts. Never edit a
  document against a project-knowledge copy — always fetch live, SHA-PINNED:
  take the HEAD SHA from the repo Atom feed
  (github.com/Overlandslabs/Consumption/commits/main.atom — the first
  /commit/<40-hex>), then fetch
  raw.githubusercontent.com/Overlandslabs/Consumption/<SHA>/<filename>.

SURGICAL EDIT CHANGE-SPEC FORMAT
- Surgical edits must follow the standard change-spec format before
  execution: (1) target file and R-number, (2) exact old string to be
  replaced, (3) exact new string, (4) propagation map. Claude proposes the
  full spec; Neil confirms before any edit is made. Pre-approved handover
  packs are specs in themselves — execute autonomously within their scope.

▲▲▲ END — PROJECT INSTRUCTIONS ▲▲▲
