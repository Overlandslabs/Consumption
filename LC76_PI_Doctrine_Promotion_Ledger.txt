LC76 OVERLAND REFERENCE LIBRARY
PROJECT-INSTRUCTIONS DOCTRINE PROMOTION LEDGER
===============================================================================
Prepared: 27 July 2026 (nineteenth session) · Workstream 1 of the PI restructure
Source: LC76_Project_Instructions.txt @ HEAD 952e0305 (SHA-pinned, raw, 200,595 chars)
Split measured: header 117,735 (58.7%) · body 82,859 (41.3%)

PURPOSE. Guarantee the PI split (Workstream 2) loses no load-bearing rule.
Every doctrine signal in the 117,735-char header narrative was swept and each
rule tested for presence in the operational body. This ledger is the audit
evidence and the human review control (pack §7): a shape-perfect split can still
promote a rule into the wrong section, so these classifications need owner
sign-off BEFORE any content moves.

METHOD. (a) STANDING/DOCTRINE signal set. (b) pack's named worked-examples.
(c) broader §5(c) sweep — NEVER/ALWAYS/must/do-not-re-raise/exemption, 41
rule-marker sentences adjudicated. Body-presence tested on whitespace-
normalised text to avoid line-wrap false-absents.

RESULT SUMMARY
───────────────────────────────────────────────────────────────────────────────
  Doctrine hits examined ................ 17 signal + 41 rule-marker sentences
  Already present in body (verified) ....  ~12 core rules (see §B)
  Classified NARRATIVE / SUPERSEDED .....  balance (library-content doctrine
                                           owned by R-docs; past-tense record)
  LIVE rules ABSENT from body (PROMOTE) .  5   (rows 1–5, all pre-accepted)
  OPTIONAL exemption ABSENT (owner ruling)  1  (row E2)

  >>> NO new load-bearing standing rule was found beyond the five already
      surfaced to the owner. The full sweep confirms the first-pass finding.

§A — PROMOTION ROWS (apply to body BEFORE the split)
===============================================================================

ROW 1  [LIVE — ABSENT]   source session: 18th (26 Jul 2026)
───────────────────────────────────────────────────────────────────────────────
  SOURCE (verbatim, from header narrative):
    THE SWEEP EARNED ITS PLACE — STANDING LESSON. The planned anchor list
    covered 18 instances of "77"; a post-edit corpus sweep found FIVE MORE,
    including R5's load-breakdown TABLE TOTAL. Had the session stopped at the
    planned edits, R5 would have carried a 65.3 headline above a table still
    totalling 77. The instances you LIST are not the in
  DESTINATION:
    SESSION EXECUTION & VALIDATION → validation gates, the 'stale-term sweep to zero' clause
  PROPOSED PROMOTION (verbatim-in-substance):
    Extend the stale-sweep gate: "...stale-term sweep to zero (documented
    exemptions stand) — and the sweep MUST run WIDER than the change spec. The
    instances you list are not the instances that exist: sweep the whole
    corpus for the term, not only the sites named in the spec (18th session —
    a planned 18-anchor '77' sweep found five more, including a table total
    that would otherwise have shipped under a corrected headline)."
  BODY-PRESENCE TEST: ABSENT (confirmed, normalised search).

ROW 2  [LIVE — ABSENT]   source session: 18th (26 Jul 2026)
───────────────────────────────────────────────────────────────────────────────
  SOURCE (verbatim, from header narrative):
    STANDING RULE RESTORED AND SHARPENED: governance files are delivered as
    COMPLETE FILES, never as patch instructions; where the file is large, the
    protection against drift is anchored programmatic editing with count==1
    asserts and a before/after structural reconciliation — not
    hand-application by the owner. The two patch files must be dele
  DESTINATION:
    WORKING CONVENTIONS → adjacent to 'Roll-forward reconciliation on governance files'
  PROPOSED PROMOTION (verbatim-in-substance):
    New bullet: "Governance files delivered whole, never as patches (standing,
    26 Jul 2026). Governance .txt files (PI, OIR, Facts, Register) are
    delivered as COMPLETE FILES, never as patch/diff documents for the owner
    to hand-apply — a committed patch file is an orphan of the OI-32/OI-42
    class and updates neither the GitHub file nor the PI field. For large
    files the protection against silent drift is anchored programmatic
    str.replace with count==1 asserts plus a before/after structural
    reconciliation, not hand-application."
  BODY-PRESENCE TEST: ABSENT (confirmed, normalised search).

ROW 3  [LIVE — ABSENT]   source session: 9th (21 Jul 2026)
───────────────────────────────────────────────────────────────────────────────
  SOURCE (verbatim, from header narrative):
    Standing rule reinforced: Claude states what it has actually executed in
    THIS session, verified against tool output, and never reports a session as
    complete from expectation. F65 SELF-INTRODUCED DEFECT (disclosed): the
    Session-A R18 Victron charging note asserted "30A fuse on the alternator
    cable" for the Orion. R26 — the OWNING document
  DESTINATION:
    WORKING CONVENTIONS → verify-before-you-report cluster (numeric-traceability/self-audit bullet)
  PROPOSED PROMOTION (verbatim-in-substance):
    New clause/bullet: "Report only what was actually executed (standing, 21
    Jul 2026, 9th session). Claude states what it has ACTUALLY executed in the
    current session, verified against tool output, and never reports a session
    or step as complete from expectation. (The 9th session opened with a prior
    turn wrongly asserting completed work that had not been done.)"
  BODY-PRESENCE TEST: ABSENT (confirmed, normalised search).

ROW 4  [LIVE — ABSENT]   source session: 14th (24 Jul 2026)
───────────────────────────────────────────────────────────────────────────────
  SOURCE (verbatim, from header narrative):
    COMMIT-STATE VERIFICATION IS NOW PART OF RE-PINNING. Between the two
    sessions the owner reported the Session 2 files as committed; a
    cache-busted check of the raw main-branch copy showed they were NOT yet
    landed — same byte count as the original, none of the four expected
    markers present. The session waited rather than editing against a s
  DESTINATION:
    SESSION EXECUTION & VALIDATION → 'Reconciliation discipline:' bullet (append)
  PROPOSED PROMOTION (verbatim-in-substance):
    Append: "Commit-state is a claim like any other: when the owner reports
    files as committed, verify with a cache-busted raw fetch of the
    main-branch copy (byte count + expected markers) before editing against
    that HEAD — do not edit on a reported-but-unverified commit (14th session:
    reported-committed Session-2 files had not landed)."
  BODY-PRESENCE TEST: ABSENT (confirmed, normalised search).

ROW 5  [LIVE — ABSENT (owner-confirmed general)]   source session: 1st (21 Jul 2026)
───────────────────────────────────────────────────────────────────────────────
  SOURCE (verbatim, from header narrative):
    TWO-TIER DOCTRINE adopted: the scam PATTERN (durable) lives in R35; the
    location-dated INCIDENT (perishable) lives in the country profile —
    mirrors the R14/R40 strategy-vs-spanner split. GATES resolved: (1b)
    committed files name stations GENERICALLY — a single-source forum report
    does not support a brand accusation in a public repo (the p
  DESTINATION:
    WORKING CONVENTIONS → near 'Section renumbering is decided by MEASUREMENT'
  PROPOSED PROMOTION (verbatim-in-substance):
    New bullet: "Two-tier content placement — pattern vs incident (standing,
    21 Jul 2026). Durable PATTERNS live in the topic reference doc;
    location-dated, perishable INCIDENTS live in the relevant country/terrain
    profile (the R35 scam-pattern vs profile-incident split; mirrors R14/R40
    strategy-vs-spanner and R14/TPMS strategy-vs-tool). Applies to any
    perishable, dated, place-specific observation."
  BODY-PRESENCE TEST: ABSENT (confirmed, normalised search).

ROW E2  [OPTIONAL exemption — ABSENT (owner ruling)]   source session: 12th (22 Jul 2026)
───────────────────────────────────────────────────────────────────────────────
  SOURCE (verbatim, from header narrative):
    2 residual hi-lift strings are intentional doctrine statements (documented
    exemption, mirrors R3). R9 masthead bumped 12 Jul; validation clean. R40
    back-links now 4 of 4 — R40 follow-up item fully CLEARED. (4) VALIDATOR
    REFINEMENT follow-up CLOSED: standard depth-walk now strips <script>
    blocks and HTML comments before walking (adopted as
  DESTINATION:
    SESSION EXECUTION & VALIDATION → 'Known do-not-re-flag items' list (append)
  PROPOSED PROMOTION (verbatim-in-substance):
    Append to the do-not-re-flag list: "the intentional hi-lift doctrine
    strings in R3 and R9 (documented no-hi-lift doctrine statements) are
    stale-sweep exempt — a 'hi-lift' sweep will hit them and must not treat
    them as defects."
  BODY-PRESENCE TEST: ABSENT (confirmed, normalised search).

§B — VERIFIED ALREADY PRESENT (no action; recorded so future sweeps skip them)
===============================================================================
  · idempotence: assert count==1 protects the ANCHOR not IDEMPOTENCE (16th)
  · encode-before-truncate: encode to bytes first, then write binary (14th)
  · RA-1 NEVER re-attach a date (11th)
  · script-stripped depth-walk convention (12th)
  · do-not-re-flag: R28 2,850 = wheelbase; Fuel-Log CSV odometer; Tyre-calc 265/75 (21st)
  · numeric-specification traceability to Facts/owning-R-doc/manual — F65 (9th/10th)
  · colour follows SECTION; red = Emergency & Safety only (21 Jul)
  · diff LIVE PI header vs pasted copy before drafting a spec (21 Jul)
  · formal documents describe the vehicle EXACTLY as the RC1 (20 Jul)
  · content-currency badge is never normalised/bumped (12th/changelog discipline)
  · measure-first: append-vs-renumber decided by corpus sweep, never by rule (24 Jul)
  · mobile v1.7: overflow-x CLIP on body, never HIDDEN (11 Jul)

§C — ACCEPTANCE TEST (pack §5)
===============================================================================
  For every rule classified LIVE, a string search of the operational body must
  return a hit AFTER promotion. §A rows currently FAIL (correctly — that is why
  they are promotions). On application, re-run the search and assert PRESENT for
  all five (+E2 if ruled in) before the split proceeds.

§D — EXECUTION NOTE
===============================================================================
  Promotions applied by anchored str.replace, count==1 on each destination
  anchor, new-content-absent assert before write, encode-to-bytes-then-binary.
  Each promotion is a declared movement in the roll-forward reconciliation; the
  operational body is byte-identical EXCEPT for these declared insertions.
  THEN, and only then, Workstream 2 (the history split) proceeds.

END OF LEDGER
