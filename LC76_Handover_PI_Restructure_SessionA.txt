LC76 OVERLAND REFERENCE LIBRARY
HANDOVER — PROJECT INSTRUCTIONS RESTRUCTURE, SESSION A (MECHANICAL MOVE)
===============================================================================
Prepared: 24 September 2026, end of the sixty-first session (the chat that ran
the 59th–61st sessions: dashboard visibility fix + OI-8 closed; oil-logging
ruling + Library Register pass; PI restructure spec written and APPROVED).
For: a FRESH chat in the LC76 project. Model: Opus-class (governance
restructure — see spec D5).
Governing document: LC76_PI_Restructure_Spec.txt (APPROVED 24 Sep 2026). This
handover is the executable brief for its Session A; the spec is the authority
wherever the two differ.

THIS SESSION IS NUMBERED THE SIXTY-SECOND. The sixty-first produced and
approved the spec only; it did not write a PI header block. Session A's header
records that in one line.


0. WHAT THE OWNER DOES FIRST
-------------------------------------------------------------------------------
Commit these five files (delivered 24 Sep 2026) to GitHub BEFORE opening
Session A, then start the fresh chat and paste or upload this handover:

   LC76_Project_Instructions.txt     249,895 bytes  sha256 01f6bcca7be92134…
   LC76_Open_Items_Register.txt      222,441 bytes  sha256 5380d6850b77f36d…
   LC76_Open_Items_Dashboard.html    178,109 bytes  sha256 df22d6776658c5e1…
   LC76_Library_Register.txt          62,704 bytes  sha256 016fac4c14f245d3…
   LC76_PI_Restructure_Spec.txt       41,594 bytes  sha256 a1f5abe5ece1c487…

Also paste the committed LC76_Project_Instructions.txt into the project
instructions FIELD, so the field and GitHub agree before the restructure.


1. SCOPE
-------------------------------------------------------------------------------
IN  (Session A — mechanical, zero information loss):
  - Pre-flight verification of the committed state.
  - The approved ADDITIONS to the PI body: promotion rows P1–P7, the two
    do-not-re-flag additions, model routing (D5), and the header-housekeeping
    standing rule.
  - Open obligations O1–O3 captured in the OIR; the O2 IERCC fix.
  - VERBATIM moves: archived header blocks -> Session History; programme
    charters -> NEW LC76_Programmes_Reference.txt; closed workstreams ->
    Session History.
  - Deletion of the PI's Library Register mirror, replaced by a pointer.
  - Two declared corrections: C1 (RA-1 weigh date) and C2 (interval annotation).
  - Governance close: OIR, dashboard, Library Register, PI header.

OUT (do NOT do in Session A):
  - ANY condensing or rewording of rules. That is Session B, with an owner-
    reviewed diff. If a rule "obviously" needs tightening, note it for B.
  - The Vehicle-section and working-convention condensing (Session B).
  - Any library HTML other than the Open Items Dashboard.
  - The OneX mirror and Manjaro worklist themselves (owner ruling: a separate
    post-Karoo session he will initiate). Session A only opens a placeholder.
  - Working-papers edits (O3 is OPENED as an item, not executed).


2. PRE-FLIGHT GATE (stop on any failure — report, do not work around)
-------------------------------------------------------------------------------
P-1  Take the HEAD SHA from github.com/Overlandslabs/Consumption/commits/
     main.atom. Fetch everything below SHA-pinned from raw.githubusercontent.
     com/Overlandslabs/Consumption/<SHA>/<file>. (web_fetch cannot open a
     constructed URL; use curl in bash.)
P-2  Verify the five files in §0 match by byte count AND sha256 prefix. If any
     differs, STOP: the owner has not committed the delivered version, and
     editing against it would lose the 59th–61st session work.
P-3  Verify the project FIELD (injected PI) begins "Last updated: 24 September
     2026, sixtieth session". If it shows an older session, tell the owner the
     field needs pasting; proceed on the GitHub copy, which is authoritative.
P-4  Fetch also: LC76_Session_History.txt, LC76_Facts.txt,
     LC76_PI_Doctrine_Promotion_Ledger.txt, LC76_OIR_Validator.py,
     LC76_Fact_Validator.py, lc76_fact_gate.py, lc76_fact_release.py,
     lc76_facts_lib.py, lc76_currency_gate.py, lc76_supersessions.txt.
P-5  Baselines BEFORE any edit, saved to disk: OIR validator output and warn
     count (expected 11 warns, 70 open, next-free OI-110); fact validator +
     gate output (expected PASS; the only warns are the 3 OI-108 OVERRUN
     warns); a snapshot copy of every file to be edited.


3. EXECUTION STEPS
-------------------------------------------------------------------------------
Edit discipline throughout: anchored str.replace with assert count == 1;
idempotence assert (a distinctive string from the NEW text is absent before
writing); encode to bytes before truncating the file; whole-file delivery.

A1  PROMOTIONS INTO THE PI BODY (approved additions; spec §6 carries the text)
    P1 cost is not a materiality test          -> end of OWNER VALUES bullets
    P2 install facts confirmed on the vehicle  -> append to the "Numeric
                                                  traceability…" bullet
    P3 index-card house rule (no live figures) -> append to the "Index card
                                                  placement" bullet
    P4 R8 hybrid regeneration                  -> append to the "Service entry
                                                  workflow" bullet
    P5 ledger is VAT-inclusive; P6 build vs service by primary purpose
                                               -> append to the "Service
                                                  ledger cost conventions"
                                                  bullet (after the SEPARATE-
                                                  FLUID RULE)
    P7 presence is not visibility              -> append to the SESSION
                                                  EXECUTION "Validation gates"
                                                  bullet
    DO-NOT-RE-FLAG additions -> "Known do-not-re-flag items" bullet:
      (a) LC76_How_To_Use_This_Library.html carries a Last-updated line by
          DELIBERATE DIVERGENCE (infrastructure class) — do not "correct" it;
      (b) mary-ann-70/ is an intentional non-library artifact (Library
          Register) — out of scope for audits and sweeps.
    MODEL ROUTING (D5): replace the three bullets with the class-based rule
      as worded in the spec's RULINGS block (Opus-class / Sonnet / Haiku).
    STANDING RULE (spec §10): "At every session close the PI header keeps
      the current block plus the two most recent priors; older blocks move
      verbatim to LC76_Session_History.txt in the SAME session." -> WORKING
      CONVENTIONS, next to "Project Instructions upkeep".
    Before inserting each promotion, search the body (whitespace-normalised)
    to confirm it is not already present — the ledger precedent's body-
    presence test. Record the result per row.

A2  OPEN OBLIGATIONS -> OIR (next free is OI-110; confirm at P-5)
    OI-110  Post-Karoo snag session — OneX rear-view mirror + Manjaro worklist
            (owner-initiated). Section A. WHAT: the loose OneX mirror (field-
            fix holding) and the Manjaro worklist (extra shelf etc.) are dealt
            with in a separate post-Karoo session the owner will start.
            Meta: NEIL · owner-initiated post-Karoo session · PI fiftieth /
            fifty-first session narrative (15 Sep 2026), now in
            LC76_Session_History.txt. STATUS: OPEN. CONFIRMED 24 Sep 2026
            (owner ruling) · NEXT-DUE: owner starts the post-Karoo session.
    OI-111  Working-papers housekeeping — EX_Register RA-1 / TA-002 status
            flip to CLOSED (weighbridge done 10 Aug 2026; recorded only in
            closed-log prose). Section B, CLAUDE. Also record here the O4
            option: an R43 red-header Emergency & Safety index card was
            DECLINED 24 Sep 2026 (option noted, not lost). STATUS: OPEN.
    OI-112  IERCC contradiction reconciled — BORN-CLOSED this session (O2).
            Owner decision 8 Aug 2026 (recorded on OI-3): satphone emergencies
            route through the crew's own SA providers; IERCC registration is
            DROPPED for the satphone; the inReach SOS -> Garmin IERCC path is a
            separate device channel, unchanged. FIX: (i) Facts §11C — the
            IERCC-callback purpose and the "OUTSTANDING ACTIONS: IERCC
            registration…" line reconciled to that decision (read OI-3's
            current wording FIRST and mirror it; do not invent); (ii) the PI
            VEHICLE "Satellite comms" entry — its "IERCC registration +
            Assistance Button config + SOS test outstanding" and "SOS
            registration is via the Garmin IERCC Registration Portal" lines
            corrected to the same decision (the inReach IERCC path may stay,
            labelled as the inReach's). Closed-log entry with evidence (E2
            grep before/after; E4 the 8 Aug ruling).
    Facts is edited -> run the fact validator and gate after; no fact VALUE
    may change (assert the [fact:…] block is byte-identical).

A3  NEW FILE — LC76_Programmes_Reference.txt
    Header (write fresh, short): what it is (programme CHARTERS and
    definitions: EX-1..12, RA-1..10, route change map M1–M4, profile §06
    retrofit charter, Audit of Truth running order); authority (STATUS is
    mastered by LC76_Open_Items_Register.txt and the working-papers
    EX_Register; doctrine by the PI; where they disagree, those win);
    governance file — GitHub only, NOT precached, NO CACHE bump, NOT in the
    project field.
    Body: the PI body blocks moved VERBATIM, in this order:
      "EXPEDITION READINESS PROGRAMME (EX) …"  up to "AUDIT OF TRUTH —"
      "AUDIT OF TRUTH — ASSURANCE PROGRAMME …" up to "READINESS ACTIONS"
      "READINESS ACTIONS (RA-1–RA-10) …"       up to "ROUTE EVALUATION"
      "ROUTE EVALUATION CHANGE MAP M1–M4 …"    up to "PROFILE §06 RETROFIT"
      "PROFILE §06 RETROFIT + FRESHNESS MARKER …" up to "R29 EVALUATION"
    Then C1 as a SEPARATE, declared edit AFTER the hash check: in the RA-1
    block, "DONE 15 Sep 2026 (E1)" -> "DONE — weighed 10 Aug 2026, recorded
    15 Sep 2026 (E1)". (Check the block for any other 15-Sep weigh-date
    phrasing and correct only weigh-DATE statements.)
    In the PI, the removed span is replaced by ONE pointer paragraph naming
    the file and what it holds.

A4  CLOSED WORKSTREAMS -> SESSION HISTORY
    The PI body span from "R29 EVALUATION CHANGE MAP M1–M5" up to (not
    including) "DESIGN SYSTEM" moves VERBATIM to the history file (below the
    moved header blocks, as a labelled "CLOSED WORKSTREAMS (moved from the PI
    body, 24 Sep 2026)" section). BUT the "OPEN R40 / MISC follow-ups" line in
    that span is live: verify OI-7 and OI-17 are open in the OIR; if both
    are, it may move; if either is not, STOP and report.

A5  LIBRARY REGISTER MIRROR -> DELETE, REPLACE WITH POINTER
    Span: from "LIBRARY REGISTER (authoritative source: LC76_Library_
    Register.txt)" up to (not including) "EXPEDITION READINESS PROGRAMME".
    PRESENCE TEST FIRST: extract every filename in the span (regex for
    LC76_*.html / *.txt / *.xlsx / *.py and the profile file names) and
    assert each appears in LC76_Library_Register.txt. Also confirm the
    Register records: the user guide's deliberate Last-updated divergence,
    the affidavit's design-system exemption, and the experimental-route
    artifacts' non-library status. Any miss: STOP, report, and add it to the
    Library Register before deleting. Then replace the span with a pointer
    paragraph: the Register is the master of what exists; next free R-number
    is R44; R-numbers are authoritative and checked there before any index
    work. (The "next free R44" also goes in the new header's current-state
    line.)

A6  HEADER BLOCKS -> SESSION HISTORY
    First write the new current block ("Last updated: 24 September 2026
    [or the actual date], sixty-second session (PI RESTRUCTURE SESSION A …)")
    and demote the sixtieth block to "PRIOR (…)". The header then keeps:
    sixty-second (current) + sixtieth + fifty-ninth. EVERYTHING OLDER — the
    fifty-eighth back to the twenty-sixth, 33 blocks — moves VERBATIM.
    Destination: LC76_Session_History.txt, inserted newest-first ABOVE the
    existing "PRIOR (29 July 2026, twenty-fifth session)" block. Add an
    "EXTENDED <date> (PI restructure, Session A)" paragraph to the file's
    intro, after the 31 Jul EXTENDED note, stating: which blocks moved; that
    the doctrine sweep and promotions ran FIRST (spec §6, rows P1–P7, applied
    this session); that no narrative was altered, removed or reconstructed.
    ARCHIVE INDEX: the PI header's "SESSION HISTORY — ARCHIVED SESSIONS" list
    moves to the history file's intro, extended with one line per newly moved
    session, built MECHANICALLY from each block's own date, ordinal and
    capitalised title phrase (no new summarising). The PI header keeps one
    pointer line to it.
    PERSONAL-DATA CHECK on the moved text: moved verbatim, so no new classes
    of data may appear; assert zero hits for SA ID-number patterns and for
    the RC1/full-legal-name classes (the policy number and broker contact
    already stand in the PI by owner ruling and move unchanged).

A7  C2 — INTERVAL CONFIRMATION (annotation only)
    Owner confirmed 24 Sep 2026: gearbox, transfer case, front and rear diff
    oils 40,000 km; engine oil 5,000 km — as already recorded in Facts §2
    and the Service Checklist. Record in the OIR header update and the PI
    current block that the "interval query" raised in the forty-seventh OIR
    update / sixtieth PI session is RESOLVED, no figures changed.


4. RECONCILIATION (the heart of Session A — all must pass before delivery)
-------------------------------------------------------------------------------
R-1  HASH PER MOVED BLOCK: for every block moved (33 header blocks, 5
     programme blocks, the closed-workstreams span), sha256 of the text as it
     stood in the old PI == sha256 of the same text in its destination. List
     them. C1 is applied AFTER this check and declared separately.
R-2  BYTE ACCOUNTING: old PI bytes = new PI bytes + moved-to-history +
     moved-to-programmes + deleted Register mirror − added text (promotions,
     new header block, pointers, O2/C2 edits). Every term declared; the
     residual is exactly zero.
R-3  PI STRUCTURE: exactly one "Last updated:" line at line 4; exactly one
     START and one END marker; exactly two "PRIOR (" blocks in the header;
     parenthesis balance within each header block; header next-free equals
     OIR max + 1 (expected OI-113, after opening OI-110/111/112), scoped to
     the CURRENT header block only.
R-4  OIR: validator SCHEMA PASS; open count 70 -> 72 (OI-110, OI-111 opened;
     OI-112 born-closed); any change in the warn count explained.
R-5  FACTS: fact validator PASS; gate PASS with only the 3 OI-108 OVERRUN
     warns; the [fact:…] block byte-identical.
R-6  DASHBOARD regenerated from the register: OI-110 and OI-111 cards added
     (OI-110 tagged neil; OI-111 claude), KPI tiles, chips, masthead count
     and the "About This View" provenance line updated; 72 cards reconcile
     1:1 with the open set; depth walks div/span/article/button/header/nav
     0/0; node --check clean; ids unique; TOC 5/5. CHECK VISIBILITY TOO (P7):
     each new card's band, tags and title read correctly.
R-7  CURRENCY GATE over the regenerated dashboard: PASS.
R-8  PI size reported before/after (expected roughly 250 KB -> ~90 KB after
     Session A; Session B takes it to ~51 KB).


5. DELIVERABLES (whole files, never patches)
-------------------------------------------------------------------------------
  LC76_Project_Instructions.txt       (restructured; the owner ALSO pastes it
                                        into the project field)
  LC76_Session_History.txt            (extended)
  LC76_Programmes_Reference.txt       (NEW)
  LC76_Open_Items_Register.txt        (OI-110/111 open, OI-112 closed, header)
  LC76_Open_Items_Dashboard.html      (regenerated)
  LC76_Facts.txt                      (§11C, O2)
  LC76_Library_Register.txt           (registers LC76_Programmes_Reference.txt
                                        and LC76_PI_Restructure_Spec.txt as
                                        governance files; date bump)
CACHE STAYS v28 — governance files are never precached; the dashboard is an
existing precached file, edited not added. index.html: no change unless a
library file changes (none should).


6. STOP CONDITIONS
-------------------------------------------------------------------------------
- Any pre-flight mismatch (P-2).
- A presence test fails (A4 OI-7/OI-17; A5 Register contents).
- Any hash mismatch or non-zero byte residual (R-1, R-2).
- The fact block changes, or the fact gate shows new drift.
- The session runs short of context before R-1..R-8 are complete: deliver
  NOTHING partial. Report where it stopped; the next session restarts from
  the committed state. (A half-moved PI is worse than an unmoved one.)


7. KNOWN PROBE PITFALLS (each has already produced a false reading once)
-------------------------------------------------------------------------------
- Header blocks are single very long lines separated by blank lines, starting
  "PRIOR (" at line start. Older blocks use two paren forms: "PRIOR (6 August
  2026, forty-fourth session) (…)" vs "PRIOR (22 September 2026, fifty-
  seventh session (…))". Split on line-start "PRIOR (", not on a paren count.
- "Last updated" also appears inside the body (the DG date-form rule); anchor
  on line 4 / line-start only.
- "next free OI-nn" appears in every archived block — scope the next-free
  probe to the CURRENT header block.
- The Session History file also contains "PRIOR (" lines; insert by anchor
  on the exact twenty-fifth-session line, asserted count == 1.
- The OIR preamble contains the words "CLOSED LOG" at line 7; the real
  section header is line-start "CLOSED LOG (most recent first".
- Section headings in the PI body contain lower case (e.g. "LIBRARY REGISTER
  (authoritative source: …)"); find spans by their exact heading text, not
  by an all-caps regex.
- When a probe disagrees with the file, suspect the probe first.


8. WHAT SESSION B WILL DO (for orientation only — not this session)
-------------------------------------------------------------------------------
Condense the VEHICLE section to identity + pointers (every POINTER row in the
spec's landing map; VERIFY-before-cut on U12/U13 in R19/R40), condense the
large working conventions to rule + one-line why (stories to history), run
the rule-count reconciliation (spec §4 B2), and give the owner a readable
diff to approve. Target ~51 KB.
===============================================================================
